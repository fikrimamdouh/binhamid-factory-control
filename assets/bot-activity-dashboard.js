(function(){
  'use strict';
  const TOKEN_KEY='binhamid_cloud_access_token',esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
  const token=()=>String(localStorage.getItem(TOKEN_KEY)||'').trim();
  const fmt=value=>{try{return new Date(value).toLocaleString('ar-SA',{dateStyle:'short',timeStyle:'short'});}catch{return String(value||'—');}};
  async function load(){const response=await fetch('/api/dashboard?persistAlerts=false',{headers:{Authorization:`Bearer ${token()}`},cache:'no-store'}),data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'تعذر قراءة سجل البوت');return data.botActivity;}
  function table(rows,columns){if(!rows?.length)return'<p class="bh-empty">لا توجد بيانات بعد.</p>';return`<div class="bh-table-wrap"><table class="bh-table"><thead><tr>${columns.map(column=>`<th>${esc(column.label)}</th>`).join('')}</tr></thead><tbody>${rows.map(row=>`<tr>${columns.map(column=>`<td>${esc(column.value(row))}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;}
  async function render(){const root=document.getElementById('bhBotActivityRoot');if(!root)return;root.innerHTML='<div class="bh-card"><h2>سجل وتحليلات البوت</h2><p class="bh-note">يعرض نمط الاستخدام والأوامر دون كشف نصوص المستخدمين في لوحة الإحصاءات.</p><p>جارٍ التحميل…</p></div>';try{const a=await load();if(!a){root.innerHTML='<div class="bh-card"><h2>سجل وتحليلات البوت</h2><p class="bh-note">يلزم دخول مدير معتمد لعرض التحليلات.</p></div>';return;}root.innerHTML=`<div class="bh-grid"><div class="bh-c12 bh-kpis"><div class="bh-kpi"><b>${a.total||0}</b><span>رسائل 30 يومًا</span></div><div class="bh-kpi"><b>${a.incoming||0}</b><span>واردة من المستخدمين</span></div><div class="bh-kpi"><b>${a.outgoing||0}</b><span>ردود البوت</span></div><div class="bh-kpi"><b>${a.activeUsers||0}</b><span>مستخدمون نشطون</span></div></div><div class="bh-card bh-c6"><h3>أكثر المستخدمين تعاملًا</h3>${table(a.topUsers,[{label:'المستخدم',value:r=>r.name},{label:'الدور',value:r=>r.role},{label:'العمليات',value:r=>r.count},{label:'آخر تفاعل',value:r=>fmt(r.lastAt)}])}</div><div class="bh-card bh-c6"><h3>أكثر إجراءات البوت</h3>${table(a.topActions,[{label:'الإجراء',value:r=>r.action},{label:'التكرار',value:r=>r.count}])}</div><div class="bh-card bh-c12"><h3>آخر عمليات مُسجلة</h3>${table(a.recentActions,[{label:'الوقت',value:r=>fmt(r.at)},{label:'الإجراء',value:r=>r.action},{label:'الاتجاه',value:r=>r.direction==='outgoing'?'رد البوت':'مستخدم'},{label:'المستخدم',value:r=>r.senderName||r.senderExternalId||'—'},{label:'النوع',value:r=>r.messageType}])}</div></div>`;}catch(error){root.innerHTML=`<div class="bh-card"><h2>سجل وتحليلات البوت</h2><p class="bh-note warn">${esc(error.message)}</p></div>`;}}
  function open(){document.querySelectorAll('.pane').forEach(pane=>pane.classList.remove('on'));document.getElementById('p-bot-activity')?.classList.add('on');document.querySelectorAll('#tabs button').forEach(button=>button.classList.toggle('on',button.id==='bhBotActivityTab'));render();}
  function install(){const tabs=document.getElementById('tabs'),wrap=document.querySelector('.wrap');if(!tabs||!wrap)return false;if(!document.getElementById('bhBotActivityTab')){const button=document.createElement('button');button.id='bhBotActivityTab';button.innerHTML='<span class="ic">◉</span>سجل البوت';button.onclick=open;tabs.appendChild(button);}if(!document.getElementById('p-bot-activity')){const pane=document.createElement('div');pane.id='p-bot-activity';pane.className='pane';pane.innerHTML='<div id="bhBotActivityRoot"></div>';wrap.appendChild(pane);}window.bhBotActivityOpen=open;return true;}
  const timer=setInterval(()=>{if(install())clearInterval(timer);},250);setTimeout(()=>clearInterval(timer),20000);
})();
