import { config } from './config.js';
import { select, insert, upsert, patch, rpc } from './supabase.js';
import { sendMessage, keyboard, sendVoiceBuffer } from './telegram.js';
import { synthesize } from './ai.js';
import { allowed, extractPlate } from './domain.js';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),now=()=>new Date().toISOString();
export async function getBotSession(chatId,userId){return(await select('bot_sessions',`channel=eq.telegram&chat_id=eq.${encodeURIComponent(String(chatId))}&external_user_id=eq.${encodeURIComponent(String(userId))}&select=*&limit=1`))?.[0]||null;}
async function setSession(chatId,userId,state,context={}){const current=await getBotSession(chatId,userId),aiHistory=current?.context?.aiHistory||[];return upsert('bot_sessions',[{channel:'telegram',chat_id:String(chatId),external_user_id:String(userId),state,context:{aiHistory,...context},updated_at:now()}],'channel,chat_id,external_user_id');}
async function clearSession(chatId,userId){const current=await getBotSession(chatId,userId);return patch('bot_sessions',`channel=eq.telegram&chat_id=eq.${encodeURIComponent(String(chatId))}&external_user_id=eq.${encodeURIComponent(String(userId))}`,{state:'idle',context:{aiHistory:current?.context?.aiHistory||[]},updated_at:now()});}
async function reply(chatId,text,voice=false,extra={}){const sent=await sendMessage(chatId,text,extra);if(voice&&config.openaiKey){try{const audio=await synthesize(text.replace(/<[^>]+>/g,''));if(audio)await sendVoiceBuffer(chatId,audio);}catch(error){console.error('tts',error.message);}}return sent;}
async function findVehicle(plate){const rows=await select('vehicles','active=eq.true&select=external_id,plate_no,asset_no,vehicle_type,make,driver_external_id&limit=1000'),p=String(plate||'').replace(/\s+/g,'').toLowerCase();return(rows||[]).filter(v=>[v.plate_no,v.asset_no].some(x=>String(x||'').replace(/\s+/g,'').toLowerCase().includes(p)||p.includes(String(x||'').replace(/\s+/g,'').toLowerCase())));}
export async function createMaintenanceDraft({chatId,messageId,identity,text,plate,voicePath}){
  const matches=plate?await findVehicle(plate):[];
  if(plate&&matches.length>1){await setSession(chatId,identity.external_id,'choose_vehicle',{problem:text,plate,vehicleIds:matches.map(x=>x.external_id),voicePath});return reply(chatId,'فهمت أن الرسالة بلاغ صيانة. وجدت أكثر من مركبة مطابقة؛ اختر المركبة الصحيحة:',true,keyboard(matches.slice(0,8).map(v=>[{text:`${v.plate_no||v.asset_no} — ${v.vehicle_type||v.make||'مركبة'}`,callback_data:`vehicle:${v.external_id}`}])));}
  if(!plate||!matches.length){await setSession(chatId,identity.external_id,'waiting_plate',{problem:text,plate,voicePath});return reply(chatId,plate?'فهمت بلاغ العطل، لكن اللوحة غير موجودة في السجل. أرسل الرقم الصحيح أو صورة واضحة.':'فهمت أن الرسالة عطل ومسارها الورشة، لكن رقم اللوحة غير واضح. أرسل رقم اللوحة أو آخر أربعة أرقام.',true);}
  const vehicle=matches[0],ref=await rpc('next_document_no',{p_prefix:'RO'}),reference=Array.isArray(ref)?ref[0]?.next_document_no||ref[0]:ref,rows=await insert('maintenance_orders',[{reference_no:String(reference),vehicle_external_id:vehicle.external_id,plate_snapshot:vehicle.plate_no||vehicle.asset_no,problem:text,status:'draft',priority:/حرج|خطر|فرامل|متوقف/.test(text)?'urgent':'normal',vehicle_stopped:/متوقف|واقفة|مش هتشتغل|لا تعمل/.test(text),reported_by:identity.user_id,source_channel:'telegram',source_chat_id:String(chatId),source_message_id:String(messageId),voice_path:voicePath||null,reported_at:now()}]),order=rows?.[0];
  await setSession(chatId,identity.external_id,'confirm_maintenance',{maintenanceId:order.id});
  return reply(chatId,`فهمت البلاغ وسجلته مؤقتًا في مسار الورشة.\n\n<b>تأكيد بلاغ عطل</b>\nأمر مؤقت: <b>${esc(order.reference_no)}</b>\nالمركبة: <b>${esc(vehicle.plate_no||vehicle.asset_no)}</b>\nالعطل: ${esc(text)}\nالحالة: لم يُفتح رسميًا بعد.`,true,keyboard([[{text:'تأكيد فتح أمر إصلاح',callback_data:`maint_confirm:${order.id}`}],[{text:'إلغاء البلاغ',callback_data:`maint_cancel:${order.id}`}]]));
}
export async function continueWaitingPlate(message,identity,session,text,voicePath=''){return createMaintenanceDraft({chatId:message.chat.id,messageId:message.message_id,identity,text:session.context?.problem||'بلاغ عطل',plate:extractPlate(text)||text,voicePath:session.context?.voicePath||voicePath});}
export async function confirmMaintenance(message,id,identity,role){if(!allowed(role,'maintenance')&&!allowed(role,'approve'))return sendMessage(message.chat.id,'ليست لديك صلاحية تأكيد أمر الإصلاح.');const rows=await patch('maintenance_orders',`id=eq.${encodeURIComponent(id)}&status=eq.draft`,{status:'reported',confirmed_at:now(),confirmed_by:identity.user_id}),order=rows?.[0];if(!order)return sendMessage(message.chat.id,'تم التعامل مع البلاغ من قبل أو لم يعد متاحًا.');await insert('maintenance_updates',[{maintenance_id:id,status:'reported',note:'تم تأكيد فتح أمر الإصلاح من Telegram',created_by:identity.user_id,source_channel:'telegram',source_chat_id:String(message.chat.id),source_message_id:String(message.message_id)}]);await clearSession(message.chat.id,identity.external_id);return reply(message.chat.id,`تم فتح أمر الإصلاح رسميًا: <b>${esc(order.reference_no)}</b>\nالحالة: بانتظار الفحص والتشخيص.`,role==='mechanic');}
export async function cancelMaintenance(message,id,identity){const rows=await patch('maintenance_orders',`id=eq.${encodeURIComponent(id)}&status=eq.draft`,{status:'cancelled',cancelled_at:now(),cancelled_by:identity.user_id});await clearSession(message.chat.id,identity.external_id);return sendMessage(message.chat.id,rows?.length?'تم إلغاء البلاغ المؤقت.':'تعذر الإلغاء لأن حالة البلاغ تغيرت.');}
export async function chooseVehicle(message,id,from,identity){const session=await getBotSession(message.chat.id,from.id);if(session?.state!=='choose_vehicle')return sendMessage(message.chat.id,'انتهت جلسة اختيار المركبة. أرسل البلاغ مرة أخرى.');const vehicles=await select('vehicles',`external_id=eq.${encodeURIComponent(id)}&select=plate_no,asset_no&limit=1`);return createMaintenanceDraft({chatId:message.chat.id,messageId:message.message_id,identity,text:session.context?.problem||'بلاغ عطل',plate:vehicles?.[0]?.plate_no||vehicles?.[0]?.asset_no,voicePath:session.context?.voicePath||''});}
