import { randomUUID } from 'node:crypto';
import { body,errorResponse,json,method } from '../http.js';
import { requireCapability } from '../permissions.js';
import { insert,patch,rpc,select,upsert } from '../supabase.js';
import { normalizePlate } from '../master-data-workbook.js';

const clean=(value,max=500)=>String(value??'').trim().slice(0,max);
const object=value=>value&&typeof value==='object'&&!Array.isArray(value)?value:{};
const now=()=>new Date().toISOString();
const today=()=>now().slice(0,10);
const actorOf=identity=>identity.fullName||identity.appUserId||identity.actor||'system';
const ASSET_STATUSES=new Set(['in_service','stopped']);
const WORK_STATUSES=new Set(['working','holiday','leave','suspended']);
const ASSET_TYPES=new Set(['vehicle','equipment','fixed_asset']);
const CENTERS=new Set(['general','block','concrete']);

function referenceId(asset){const ref=object(object(asset).metadata).erpReference;return clean(ref.externalId||ref.externalKey,200);}
function employeeStatus(employee){const metadata=object(employee.metadata);return clean(metadata.manualWorkStatus||metadata.workStatus||'working',40);}
function linkedTelegram(employeeExternalId,assignments,usersById){
  const rows=(assignments||[]).filter(row=>clean(row.employee_external_id)===clean(employeeExternalId)&&row.active!==false).sort((a,b)=>String(b.updated_at||'').localeCompare(String(a.updated_at||''))),assignment=rows[0]||null,user=assignment?usersById.get(clean(assignment.app_user_id))||null:null;
  return{assignment,user};
}
function canonicalProjection(asset,erp){
  const metadata=object(asset.metadata),overrides=object(metadata.canonicalOverrides),ref=object(metadata.erpReference),linked=Boolean(erp||referenceId(asset));
  return{
    canonical_external_id:asset.external_id,
    diesel_external_id:asset.diesel_expected===true?asset.external_id:null,
    erp_external_id:erp?.external_id||referenceId(asset)||null,
    source_type:linked?'diesel_erp':asset.diesel_expected===true?'diesel':'erp',
    linked,
    diesel_expected:asset.diesel_expected===true,
    plate_no:clean(overrides.plateNo||asset.plate_no||erp?.plate_no||ref.newPlate||ref.oldPlate),
    asset_no:clean(overrides.assetNo||erp?.asset_no||ref.assetNo||asset.asset_no),
    asset_name:clean(overrides.assetName||erp?.asset_name||ref.assetName||asset.asset_name||asset.asset_type),
    asset_type:clean(overrides.assetType||asset.asset_type||erp?.asset_type||ref.assetType||'vehicle'),
    make:clean(overrides.make||erp?.make||ref.make||asset.make),
    model:clean(overrides.model||erp?.model||ref.model||asset.model),
    operational_status:clean(overrides.operationalStatus||asset.operational_status||erp?.operational_status||ref.operationalStatus||'in_service'),
    employee_external_id:clean(asset.assigned_employee_external_id||erp?.assigned_employee_external_id),
    cost_center_code:clean(asset.cost_center_code||erp?.cost_center_code||metadata.costCenterCode),
    purchase_cost:Number(overrides.purchaseCost??object(erp?.metadata).purchaseCost??ref.purchaseCost??metadata.purchaseCost??0),
    source_rows:linked?2:1
  };
}
async function optional(table,query){try{return await select(table,query)||[];}catch(error){console.warn('[canonical master optional]',table,error?.message||error);return[];}}
async function loadRegistry(){
  const[assets,employees,vehicles,assignments,users,channels,centers,employeeCenters,workSites]=await Promise.all([
    select('unified_assets','active=eq.true&select=external_id,asset_type,asset_name,plate_no,asset_no,assigned_employee_external_id,operational_status,diesel_expected,make,model,cost_center_code,metadata&order=diesel_expected.desc,asset_name.asc&limit=10000'),
    select('employees','active=eq.true&select=external_id,employee_no,national_id,full_name,phone,role,site,metadata&order=full_name.asc&limit=10000'),
    optional('vehicles','active=eq.true&select=external_id,plate_no,asset_no,vehicle_type,make,model,driver_external_id,status&limit=10000'),
    optional('employee_assignments','active=eq.true&select=app_user_id,employee_external_id,site_id,vehicle_external_id,job_title,shift_name,active,updated_at&order=updated_at.desc&limit=5000'),
    optional('app_users','active=eq.true&select=id,full_name,role,active,employee_external_id&limit=5000'),
    optional('user_channels','channel=eq.telegram&active=eq.true&select=user_id,external_id,external_username,active&limit=5000'),
    optional('cost_centers','active=eq.true&code=in.(general,block,concrete)&select=id,code,name_ar&order=code.asc'),
    optional('employee_cost_assignments','active=eq.true&select=employee_external_id,cost_center_id,allocation_percent,effective_from,updated_at&order=updated_at.desc&limit=10000'),
    optional('work_sites','active=eq.true&select=id,name,code,active&order=name.asc&limit=500')
  ]);
  const assetById=new Map((assets||[]).map(row=>[clean(row.external_id),row])),referenced=new Set(),canonicalAssets=[];
  for(const asset of assets||[]){if(asset.diesel_expected!==true)continue;const erpId=referenceId(asset),erp=erpId?assetById.get(erpId)||null:null;if(erpId)referenced.add(erpId);canonicalAssets.push(canonicalProjection(asset,erp));}
  for(const asset of assets||[]){if(asset.diesel_expected===true||referenced.has(clean(asset.external_id)))continue;canonicalAssets.push(canonicalProjection(asset,null));}
  const usersById=new Map((users||[]).map(row=>[clean(row.id),row])),channelByUser=new Map((channels||[]).map(row=>[clean(row.user_id),row])),centerById=new Map((centers||[]).map(row=>[clean(row.id),row])),employeeCenterById=new Map();
  for(const row of employeeCenters||[]){const id=clean(row.employee_external_id);if(id&&!employeeCenterById.has(id))employeeCenterById.set(id,row);}
  const linkedUserIds=new Set(),canonicalEmployees=(employees||[]).map(employee=>{
    const{assignment,user}=linkedTelegram(employee.external_id,assignments,usersById);if(user)linkedUserIds.add(clean(user.id));const center=centerById.get(clean(employeeCenterById.get(clean(employee.external_id))?.cost_center_id)),metadata=object(employee.metadata),vehicleId=clean(assignment?.vehicle_external_id)||clean(canonicalAssets.find(asset=>clean(asset.employee_external_id)===clean(employee.external_id))?.canonical_external_id);
    return{external_id:employee.external_id,employee_no:employee.employee_no||null,national_id:employee.national_id||null,full_name:employee.full_name,phone:employee.phone||null,role:employee.role||assignment?.job_title||'employee',site:employee.site||null,site_id:assignment?.site_id||null,work_status:employeeStatus(employee),cost_center_code:center?.code||metadata.costCenterCode||null,vehicle_external_id:vehicleId||null,telegram:user?{id:user.id,full_name:user.full_name,role:user.role,username:channelByUser.get(clean(user.id))?.external_username||null,job_title:assignment?.job_title||null,shift_name:assignment?.shift_name||null,site_id:assignment?.site_id||null,vehicle_external_id:assignment?.vehicle_external_id||null}:null};
  }),unlinkedTelegramUsers=(users||[]).filter(user=>!linkedUserIds.has(clean(user.id))&&!clean(user.employee_external_id)).map(user=>({id:user.id,full_name:user.full_name,role:user.role,username:channelByUser.get(clean(user.id))?.external_username||null}));
  return{assets:assets||[],assetById,vehicles:vehicles||[],employees:employees||[],canonicalAssets,canonicalEmployees,unlinkedTelegramUsers,assignments:assignments||[],users:users||[],channels:channels||[],centers:centers||[],workSites:workSites||[]};
}
function resolveCanonical(registry,id){
  const direct=registry.assetById.get(clean(id));if(!direct)return null;
  if(direct.diesel_expected===true){const erpId=referenceId(direct);return{canonical:direct,erp:erpId?registry.assetById.get(erpId)||null:null};}
  const parent=(registry.assets||[]).find(row=>row.diesel_expected===true&&referenceId(row)===clean(direct.external_id));return parent?{canonical:parent,erp:direct}:{canonical:direct,erp:null};
}
async function audit(identity,action,entityType,entityId,details){await insert('audit_log',[{actor_type:'web',actor_id:actorOf(identity),action,entity_type:entityType,entity_id:clean(entityId,200),details}],{prefer:'return=minimal'}).catch(error=>console.error('[canonical master audit]',error));}
async function findCenter(code){if(!code)return null;if(!CENTERS.has(code))throw Object.assign(new Error('مركز التكلفة يجب أن يكون عام أو بلوك أو خرسانة.'),{status:400,code:'CANONICAL_COST_CENTER_INVALID'});const center=(await select('cost_centers',`code=eq.${encodeURIComponent(code)}&active=eq.true&select=id,code,name_ar&limit=1`))?.[0];if(!center)throw Object.assign(new Error('مركز التكلفة غير موجود أو غير نشط.'),{status:409,code:'CANONICAL_COST_CENTER_NOT_READY'});return center;}
async function assignEmployeeCostCenter(identity,employeeExternalId,costCenterCode){
  if(!costCenterCode)return;const center=await findCenter(costCenterCode),stamp=now(),effective=today(),actor=actorOf(identity);await patch('employee_cost_assignments',`employee_external_id=eq.${encodeURIComponent(employeeExternalId)}&active=eq.true`,{active:false,effective_to:effective,updated_at:stamp});await upsert('employee_cost_assignments',[{employee_external_id:employeeExternalId,cost_center_id:center.id,allocation_percent:100,effective_from:effective,effective_to:null,active:true,created_by:actor,updated_at:stamp}],'employee_external_id,cost_center_id,effective_from');
}
async function assignAssetCostCenter(identity,assetExternalId,assetType,costCenterCode,linkedErpId){
  if(!costCenterCode)return;const center=await findCenter(costCenterCode),stamp=now(),effective=today(),actor=actorOf(identity);await patch('asset_cost_center_assignments',`asset_external_id=eq.${encodeURIComponent(assetExternalId)}&active=eq.true`,{active:false,effective_to:effective,updated_at:stamp});await upsert('asset_cost_center_assignments',[{asset_external_id:assetExternalId,asset_type:assetType,cost_center_id:center.id,effective_from:effective,effective_to:null,active:true,operational_exception:false,exception_reason:null,created_by:actor,updated_at:stamp}],'asset_external_id,effective_from,cost_center_id');await patch('unified_assets',`external_id=eq.${encodeURIComponent(assetExternalId)}`,{cost_center_code:costCenterCode,updated_at:stamp});if(linkedErpId)await patch('unified_assets',`external_id=eq.${encodeURIComponent(linkedErpId)}`,{cost_center_code:costCenterCode,updated_at:stamp}).catch(error=>console.error('[canonical master cost mirror]',error));
}
async function assignVehicle(registry,employeeExternalId,assetExternalId){
  const stamp=now(),employeeId=clean(employeeExternalId,200),assetId=clean(assetExternalId,200);if(employeeId){await patch('unified_assets',`assigned_employee_external_id=eq.${encodeURIComponent(employeeId)}&active=eq.true`,{assigned_employee_external_id:null,updated_at:stamp}).catch(()=>[]);await patch('vehicles',`driver_external_id=eq.${encodeURIComponent(employeeId)}&active=eq.true`,{driver_external_id:null,updated_at:stamp}).catch(()=>[]);}
  if(assetId){const resolved=resolveCanonical(registry,assetId);if(!resolved)throw Object.assign(new Error('المركبة المختارة غير موجودة.'),{status:404,code:'CANONICAL_VEHICLE_NOT_FOUND'});const type=clean(object(resolved.canonical.metadata).canonicalOverrides?.assetType||resolved.canonical.asset_type||resolved.erp?.asset_type);if(type==='fixed_asset')throw Object.assign(new Error('لا يمكن ربط موظف بأصل ثابت. غيّر نوعه إلى سيارة أو معدة أولًا.'),{status:409,code:'CANONICAL_FIXED_ASSET_LINK'});for(const row of [resolved.canonical,resolved.erp].filter(Boolean))await patch('unified_assets',`external_id=eq.${encodeURIComponent(row.external_id)}`,{assigned_employee_external_id:employeeId||null,updated_at:stamp});for(const id of [resolved.canonical.external_id,resolved.erp?.external_id].filter(Boolean))await patch('vehicles',`external_id=eq.${encodeURIComponent(id)}`,{driver_external_id:employeeId||null,updated_at:stamp}).catch(()=>[]);}
  if(employeeId)await patch('employee_assignments',`employee_external_id=eq.${encodeURIComponent(employeeId)}&active=eq.true`,{vehicle_external_id:assetId||null,updated_at:stamp}).catch(()=>[]);
}
async function assignTelegram(registry,employee,telegramUserId,siteId,vehicleExternalId,identity){
  const selected=clean(telegramUserId,200),current=registry.canonicalEmployees.find(row=>clean(row.external_id)===clean(employee.external_id))?.telegram?.id||'',stamp=now();if(!selected&&current){await patch('employee_assignments',`app_user_id=eq.${encodeURIComponent(current)}`,{active:false,vehicle_external_id:null,updated_at:stamp}).catch(()=>[]);await patch('app_users',`id=eq.${encodeURIComponent(current)}`,{employee_external_id:null,updated_at:stamp}).catch(()=>[]);return;}
  if(!selected||selected===current)return;const user=registry.users.find(row=>clean(row.id)===selected);if(!user)throw Object.assign(new Error('حساب Telegram المختار غير موجود أو غير نشط.'),{status:404,code:'CANONICAL_TELEGRAM_NOT_FOUND'});const channel=registry.channels.find(row=>clean(row.user_id)===selected);if(!channel)throw Object.assign(new Error('حساب Telegram لا يحتوي قناة صالحة.'),{status:409,code:'CANONICAL_TELEGRAM_CHANNEL_MISSING'});if(!siteId)throw Object.assign(new Error('اختر موقع العمل قبل ربط Telegram.'),{status:400,code:'CANONICAL_TELEGRAM_SITE_REQUIRED'});if(current&&current!==selected){await patch('employee_assignments',`app_user_id=eq.${encodeURIComponent(current)}`,{active:false,updated_at:stamp}).catch(()=>[]);await patch('app_users',`id=eq.${encodeURIComponent(current)}`,{employee_external_id:null,updated_at:stamp}).catch(()=>[]);}await patch('employee_assignments',`app_user_id=eq.${encodeURIComponent(selected)}&active=eq.true`,{active:false,updated_at:stamp}).catch(()=>[]);const assignment={app_user_id:selected,employee_external_id:employee.external_id,site_id:siteId,vehicle_external_id:vehicleExternalId||null,job_title:employee.role||user.role||null,shift_name:null,active:true,updated_at:stamp};await upsert('employee_assignments',[assignment],'app_user_id');await rpc('approve_telegram_user',{p_external_id:channel.external_id,p_full_name:employee.full_name||user.full_name||channel.external_username||channel.external_id,p_role:user.role,p_active:true,p_employee_external_id:employee.external_id});await audit(identity,'canonical_employee_telegram_linked','employee',employee.external_id,{appUserId:selected,siteId,vehicleExternalId:vehicleExternalId||null});
}
async function saveEmployee(req,input){
  const identity=await requireCapability(req,'assets.manage'),registry=await loadRegistry(),externalId=clean(input.employeeExternalId,200)||`emp-${randomUUID()}`,fullName=clean(input.fullName,240),nationalId=clean(input.nationalId,40).replace(/\D/g,''),employeeNo=clean(input.employeeNo,80),phone=clean(input.phone,80),role=clean(input.role,160),site=clean(input.site,160),siteId=clean(input.siteId,200),workStatus=clean(input.workStatus,40)||'working',costCenterCode=clean(input.costCenterCode,40),vehicleExternalId=clean(input.vehicleExternalId,200),telegramUserId=clean(input.telegramUserId,200);
  if(!fullName)throw Object.assign(new Error('اسم الموظف مطلوب.'),{status:400,code:'CANONICAL_EMPLOYEE_NAME_REQUIRED'});if(!WORK_STATUSES.has(workStatus))throw Object.assign(new Error('حالة الموظف غير صحيحة.'),{status:400,code:'CANONICAL_EMPLOYEE_STATUS_INVALID'});const duplicate=registry.employees.find(row=>nationalId&&clean(row.national_id).replace(/\D/g,'')===nationalId&&clean(row.external_id)!==externalId);if(duplicate)throw Object.assign(new Error(`رقم الهوية مسجل للموظف ${duplicate.full_name}.`),{status:409,code:'CANONICAL_EMPLOYEE_DUPLICATE_ID'});const existing=registry.employees.find(row=>clean(row.external_id)===externalId),metadata={...object(existing?.metadata),workStatus,manualWorkStatus:workStatus,costCenterCode:costCenterCode||object(existing?.metadata).costCenterCode||null,canonicalUpdatedAt:now(),canonicalUpdatedBy:actorOf(identity)},values={external_id:externalId,national_id:nationalId||null,employee_no:employeeNo||null,full_name:fullName,phone:phone||null,role:role||null,site:site||null,active:true,metadata,updated_at:now()};await upsert('employees',[values],'external_id');await assignEmployeeCostCenter(identity,externalId,costCenterCode);const updatedRegistry=await loadRegistry();await assignVehicle(updatedRegistry,externalId,vehicleExternalId);await assignTelegram(updatedRegistry,values,telegramUserId,siteId,vehicleExternalId,identity);await audit(identity,existing?'canonical_employee_updated':'canonical_employee_created','employee',externalId,{fullName,role,vehicleExternalId:vehicleExternalId||null,telegramUserId:telegramUserId||null,costCenterCode:costCenterCode||null});return{employeeExternalId:externalId,created:!existing};
}
async function deleteEmployee(req,input){
  const identity=await requireCapability(req,'assets.manage'),registry=await loadRegistry(),id=clean(input.employeeExternalId,200),employee=registry.employees.find(row=>clean(row.external_id)===id);if(!employee)throw Object.assign(new Error('الموظف غير موجود أو محذوف.'),{status:404,code:'CANONICAL_EMPLOYEE_NOT_FOUND'});const linked=registry.canonicalEmployees.find(row=>clean(row.external_id)===id)?.telegram;if(linked?.role==='admin')throw Object.assign(new Error('لا يمكن حذف الموظف المرتبط بحساب مدير النظام.'),{status:409,code:'CANONICAL_EMPLOYEE_ADMIN_PROTECTED'});const stamp=now();await assignVehicle(registry,id,'');await patch('employee_assignments',`employee_external_id=eq.${encodeURIComponent(id)}&active=eq.true`,{active:false,vehicle_external_id:null,updated_at:stamp}).catch(()=>[]);await patch('app_users',`employee_external_id=eq.${encodeURIComponent(id)}`,{employee_external_id:null,updated_at:stamp}).catch(()=>[]);await patch('employees',`external_id=eq.${encodeURIComponent(id)}`,{active:false,metadata:{...object(employee.metadata),workStatus:'terminated',manualWorkStatus:'terminated',deletedAt:stamp,deletedBy:actorOf(identity)},updated_at:stamp});await audit(identity,'canonical_employee_deleted','employee',id,{fullName:employee.full_name});return{employeeExternalId:id,deleted:true};
}
async function saveAsset(req,input){
  const identity=await requireCapability(req,'assets.manage'),registry=await loadRegistry(),requestedId=clean(input.assetExternalId,200),existing=requestedId?resolveCanonical(registry,requestedId):null,externalId=existing?.canonical.external_id||`asset-${randomUUID()}`,plateNo=clean(input.plateNo,100),assetNo=clean(input.assetNo,120),assetName=clean(input.assetName,300),assetType=clean(input.assetType,80)||'vehicle',make=clean(input.make,160),model=clean(input.model,160),status=clean(input.operationalStatus,50)||'in_service',employeeExternalId=clean(input.employeeExternalId,200),costCenterCode=clean(input.costCenterCode,40),dieselExpected=input.dieselExpected===true,erpExternalId=clean(input.erpExternalId,200);
  if(!ASSET_TYPES.has(assetType))throw Object.assign(new Error('نوع الأصل يجب أن يكون سيارة أو معدة أو أصل ثابت.'),{status:400,code:'CANONICAL_ASSET_TYPE_INVALID'});if(!ASSET_STATUSES.has(status))throw Object.assign(new Error('الحالة يجب أن تكون يعمل أو واقف.'),{status:400,code:'CANONICAL_ASSET_STATUS_INVALID'});if(!assetName&&!plateNo&&!assetNo)throw Object.assign(new Error('أدخل اسم الأصل أو اللوحة أو رقم الأصل.'),{status:400,code:'CANONICAL_ASSET_FIELDS_REQUIRED'});if(plateNo){const key=normalizePlate(plateNo),ids=new Set([externalId,existing?.erp?.external_id].filter(Boolean)),collision=registry.canonicalAssets.find(row=>normalizePlate(row.plate_no)===key&&!ids.has(row.canonical_external_id));if(collision)throw Object.assign(new Error(`اللوحة ${plateNo} مرتبطة بأصل آخر بالفعل.`),{status:409,code:'CANONICAL_PLATE_DUPLICATE'});}if(employeeExternalId&&!registry.employees.some(row=>clean(row.external_id)===employeeExternalId))throw Object.assign(new Error('الموظف المختار غير موجود.'),{status:404,code:'CANONICAL_EMPLOYEE_NOT_FOUND'});const canonical=existing?.canonical||null,erp=existing?.erp||null,stamp=now(),metadata={...object(canonical?.metadata),canonicalOverrides:{plateNo,assetNo,assetName,assetType,make,model,operationalStatus:status,updatedAt:stamp,updatedBy:actorOf(identity)}},assigned=assetType==='fixed_asset'?'':employeeExternalId,values={external_id:externalId,asset_type:assetType,asset_name:assetName||canonical?.asset_name||erp?.asset_name||externalId,plate_no:plateNo||null,asset_no:assetNo||null,make:make||null,model:model||null,operational_status:status,diesel_expected:canonical?canonical.diesel_expected===true:dieselExpected,assigned_employee_external_id:assigned||null,cost_center_code:costCenterCode||null,active:true,metadata,updated_at:stamp};await upsert('unified_assets',[values],'external_id');let linkedErp=erp;if(erpExternalId&&(!linkedErp||clean(linkedErp.external_id)!==erpExternalId)){const candidate=registry.assetById.get(erpExternalId);if(!candidate||candidate.diesel_expected===true)throw Object.assign(new Error('أصل ERP المختار غير صالح.'),{status:404,code:'CANONICAL_ERP_NOT_FOUND'});linkedErp=candidate;const ref={externalId:candidate.external_id,assetNo:candidate.asset_no||assetNo||null,oldPlate:candidate.plate_no||plateNo||null,newPlate:plateNo||candidate.plate_no||null,assetName:candidate.asset_name||assetName||null,assetType:candidate.asset_type||assetType,make:candidate.make||make||null,model:candidate.model||model||null,purchaseCost:Number(object(candidate.metadata).purchaseCost||0),operationalStatus:status};await patch('unified_assets',`external_id=eq.${encodeURIComponent(externalId)}`,{metadata:{...metadata,erpReference:ref,manualErpReference:ref,erpReferenceMode:'manual',erpReferenceUpdatedAt:stamp},updated_at:stamp});}
  if(!erpExternalId&&canonical&&referenceId(canonical)){await patch('unified_assets',`external_id=eq.${encodeURIComponent(externalId)}`,{metadata:{...metadata,erpReference:null,manualErpReference:null,erpReferenceMode:'manual',erpReferenceUpdatedAt:stamp},updated_at:stamp});linkedErp=null;}
  if(linkedErp){await patch('unified_assets',`external_id=eq.${encodeURIComponent(linkedErp.external_id)}`,{asset_type:assetType,asset_name:values.asset_name,plate_no:values.plate_no,asset_no:values.asset_no,make:values.make,model:values.model,operational_status:status,assigned_employee_external_id:assigned||null,cost_center_code:costCenterCode||null,updated_at:stamp});}
  if(assetType==='fixed_asset'){for(const id of [externalId,linkedErp?.external_id].filter(Boolean))await patch('vehicles',`external_id=eq.${encodeURIComponent(id)}`,{active:false,driver_external_id:null,updated_at:stamp}).catch(()=>[]);}else{for(const id of [externalId,linkedErp?.external_id].filter(Boolean))await upsert('vehicles',[{external_id:id,plate_no:values.plate_no,asset_no:values.asset_no,vehicle_type:values.asset_name,make:values.make,model:values.model,driver_external_id:assigned||null,status,active:true,updated_at:stamp}],'external_id').catch(error=>console.error('[canonical vehicle upsert]',error));}
  await assignAssetCostCenter(identity,externalId,assetType,costCenterCode,linkedErp?.external_id||null);const refreshed=await loadRegistry();if(assetType!=='fixed_asset')await assignVehicle(refreshed,assigned,externalId);await audit(identity,existing?'canonical_asset_updated':'canonical_asset_created','unified_asset',externalId,{plateNo:values.plate_no,assetNo:values.asset_no,assetName:values.asset_name,assetType,status,employeeExternalId:assigned||null,erpExternalId:linkedErp?.external_id||null,costCenterCode:costCenterCode||null});return{canonicalExternalId:externalId,created:!existing,erpExternalId:linkedErp?.external_id||null};
}
async function deleteAsset(req,input){
  const identity=await requireCapability(req,'assets.manage'),registry=await loadRegistry(),resolved=resolveCanonical(registry,input.assetExternalId);if(!resolved)throw Object.assign(new Error('الأصل غير موجود أو محذوف.'),{status:404,code:'CANONICAL_ASSET_NOT_FOUND'});const stamp=now(),ids=[resolved.canonical.external_id,resolved.erp?.external_id].filter(Boolean);for(const id of ids){await patch('unified_assets',`external_id=eq.${encodeURIComponent(id)}`,{active:false,assigned_employee_external_id:null,metadata:{...object(registry.assetById.get(id)?.metadata),deletedAt:stamp,deletedBy:actorOf(identity)},updated_at:stamp});await patch('vehicles',`external_id=eq.${encodeURIComponent(id)}`,{active:false,driver_external_id:null,updated_at:stamp}).catch(()=>[]);await patch('employee_assignments',`vehicle_external_id=eq.${encodeURIComponent(id)}&active=eq.true`,{vehicle_external_id:null,updated_at:stamp}).catch(()=>[]);}await audit(identity,'canonical_asset_deleted','unified_asset',resolved.canonical.external_id,{linkedErpExternalId:resolved.erp?.external_id||null});return{canonicalExternalId:resolved.canonical.external_id,deleted:true};
}
async function linkErp(req,input){const identity=await requireCapability(req,'assets.manage'),registry=await loadRegistry(),diesel=registry.assetById.get(clean(input.dieselExternalId)),erp=registry.assetById.get(clean(input.erpExternalId));if(!diesel||diesel.diesel_expected!==true)throw Object.assign(new Error('اختر سجل الديزل الصحيح.'),{status:404,code:'CANONICAL_DIESEL_NOT_FOUND'});if(!erp||erp.diesel_expected===true)throw Object.assign(new Error('اختر أصل ERP مستقلًا.'),{status:404,code:'CANONICAL_ERP_NOT_FOUND'});const owner=registry.assets.find(row=>row.diesel_expected===true&&referenceId(row)===clean(erp.external_id)&&clean(row.external_id)!==clean(diesel.external_id));if(owner)throw Object.assign(new Error('أصل ERP مرتبط بسيارة أخرى بالفعل.'),{status:409,code:'CANONICAL_ERP_ALREADY_LINKED'});const metadata=object(diesel.metadata),erpMetadata=object(erp.metadata),reference={externalId:erp.external_id,assetNo:erp.asset_no||null,oldPlate:erp.plate_no||null,newPlate:erp.plate_no||null,assetName:erp.asset_name||null,assetType:erp.asset_type||null,make:erp.make||null,model:erp.model||null,purchaseCost:Number(erpMetadata.purchaseCost||0),operationalStatus:erp.operational_status||null};await patch('unified_assets',`external_id=eq.${encodeURIComponent(diesel.external_id)}`,{metadata:{...metadata,erpReference:reference,manualErpReference:reference,erpReferenceMode:'manual',erpReferenceUpdatedAt:now()},updated_at:now()});await audit(identity,'canonical_erp_linked','unified_asset',diesel.external_id,{erpExternalId:erp.external_id});return{dieselExternalId:diesel.external_id,erpExternalId:erp.external_id};}
async function unlinkErp(req,input){const identity=await requireCapability(req,'assets.manage'),registry=await loadRegistry(),diesel=registry.assetById.get(clean(input.dieselExternalId));if(!diesel||diesel.diesel_expected!==true)throw Object.assign(new Error('سجل الديزل غير موجود.'),{status:404,code:'CANONICAL_DIESEL_NOT_FOUND'});const metadata=object(diesel.metadata),previous=referenceId(diesel);await patch('unified_assets',`external_id=eq.${encodeURIComponent(diesel.external_id)}`,{metadata:{...metadata,erpReference:null,manualErpReference:null,erpReferenceMode:'manual',erpReferenceUpdatedAt:now()},updated_at:now()});await audit(identity,'canonical_erp_unlinked','unified_asset',diesel.external_id,{previousErpExternalId:previous||null});return{dieselExternalId:diesel.external_id,previousErpExternalId:previous||null};}
async function autoLink(req){const identity=await requireCapability(req,'assets.manage'),registry=await loadRegistry(),erpByPlate=new Map();for(const asset of registry.assets){if(asset.diesel_expected===true)continue;const key=normalizePlate(asset.plate_no);if(!key)continue;const list=erpByPlate.get(key)||[];list.push(asset);erpByPlate.set(key,list);}let linked=0,ambiguous=0,unmatched=0;for(const diesel of registry.assets.filter(row=>row.diesel_expected===true)){if(referenceId(diesel))continue;const matches=erpByPlate.get(normalizePlate(diesel.plate_no))||[];if(matches.length===1){await linkErp(req,{dieselExternalId:diesel.external_id,erpExternalId:matches[0].external_id});linked++;}else if(matches.length>1)ambiguous++;else unmatched++;}await audit(identity,'canonical_exact_plate_autolink','master_data','vehicle-registry',{linked,ambiguous,unmatched});return{linked,ambiguous,unmatched};}
async function responsePayload(){const registry=await loadRegistry();return{canonicalAssets:registry.canonicalAssets,canonicalEmployees:registry.canonicalEmployees,unlinkedTelegramUsers:registry.unlinkedTelegramUsers,telegramUsers:registry.users.map(user=>({id:user.id,full_name:user.full_name,role:user.role,employee_external_id:user.employee_external_id||null,username:registry.channels.find(channel=>clean(channel.user_id)===clean(user.id))?.external_username||null})),employees:registry.employees.map(row=>({external_id:row.external_id,full_name:row.full_name,role:row.role||null})),erpCandidates:registry.assets.filter(row=>row.diesel_expected!==true&&!registry.canonicalAssets.some(item=>item.erp_external_id===row.external_id)).map(row=>({external_id:row.external_id,asset_no:row.asset_no||null,plate_no:row.plate_no||null,asset_name:row.asset_name||null,make:row.make||null,model:row.model||null})),centers:registry.centers,workSites:registry.workSites,counts:{assets:registry.canonicalAssets.length,linkedAssets:registry.canonicalAssets.filter(row=>row.linked).length,employees:registry.canonicalEmployees.length,telegramLinked:registry.canonicalEmployees.filter(row=>row.telegram).length,unlinkedTelegram:registry.unlinkedTelegramUsers.length,employeesWithVehicle:registry.canonicalEmployees.filter(row=>row.vehicle_external_id).length,workingAssets:registry.canonicalAssets.filter(row=>row.operational_status==='in_service').length,stoppedAssets:registry.canonicalAssets.filter(row=>row.operational_status!=='in_service').length}};}
export async function canonicalMasterData(req,res){
  if(!method(req,res,['GET','POST']))return;
  try{
    if(req.method==='GET'){await requireCapability(req,'assets.view');return json(res,200,{ok:true,...await responsePayload(),generatedAt:now()});}
    const input=await body(req),action=clean(input.action,80);let result;
    if(action==='save_employee')result=await saveEmployee(req,input);
    else if(action==='delete_employee')result=await deleteEmployee(req,input);
    else if(action==='save_asset'||action==='update_asset')result=await saveAsset(req,input);
    else if(action==='delete_asset')result=await deleteAsset(req,input);
    else if(action==='link_erp')result=await linkErp(req,input);
    else if(action==='unlink_erp')result=await unlinkErp(req,input);
    else if(action==='auto_link_exact_plate')result=await autoLink(req);
    else throw Object.assign(new Error('إجراء السجل الموحد غير معروف.'),{status:400,code:'CANONICAL_ACTION_UNKNOWN'});
    return json(res,200,{ok:true,result,...await responsePayload(),generatedAt:now()});
  }catch(error){errorResponse(res,error);}
}
