import { config } from './config.js';
import { generateCumulativeDailyPdfs } from './daily-cumulative-pdf.js';
import { loadProjectedCumulativeDailyReport } from './daily-cumulative-report-data.js';
import { generateAvailablePortfolioPdfs } from './customer-portfolio-batch.js';
import { persistPortfolioReportSnapshot } from './customer-portfolio-snapshot.js';
import { loadCustomerAnalytics } from './bot-customer-report-data.js';
import { buildReportActivityIndex, indexCustomerAnalytics, lookupActivity, lookupCustomer, settleCustomerAccount } from './customer-settlement.js';
import { insert, select } from './supabase.js';
import { keyboard, sendDocumentBuffer, sendMessage } from './telegram.js';

const MANAGER_TELEGRAM_ID=String(process.env.TELEGRAM_MANAGER_ID||'6870312376').trim();
const clean=(value,max=1000)=>String(value??'').trim().slice(0,max);
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const norm=value=>clean(value,3000).toLowerCase().replace(/[أإآ]/g,'ا').replace(/ة/g,'ه').replace(/ى/g,'ي').replace(/[ًٌٍَُِّْـ]/g,'').replace(/[_-]+/g,' ').replace(/\s+/g,' ');
const money=value=>`${Number(value||0).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})} ر.س`;
const customerKey=(code,name)=>{const normalized=norm(code);return normalized?`code:${normalized}`:`name:${norm(name)||'unknown'}`;};
const compactButtonName=value=>{let text=clean(value,120)||'عميل';while(Buffer.byteLength(text,'utf8')>58)text=text.slice(0,-1);return text;};

export function erpSaleType(row={}){const text=[row.salesType,row.sales_type,row.kind,row.type,row.segment,row.item,row.itemName,row.item_name,row.product].map(norm).filter(Boolean).join(' ');if(/بلوك|بلك|block/.test(text))return'block';if(/خرسان|concrete|ready\s*mix|readymix|rmc/.test(text))return'concrete';return'';}
export function erpTelegramRecipients(ownerId=config.telegramOwnerId,managerId=MANAGER_TELEGRAM_ID){return[...new Set([ownerId,managerId].map(value=>String(value||'').trim()).filter(Boolean))];}
function projectionIndex(projection={}){const map=new Map();for(const type of ['block','concrete'])for(const row of projection?.departments?.[type]?.rows||[]){const key=customerKey(row.code||row.customerCode,row.name||row.customerName),entry=map.get(key)||{blockApplied:0,concreteApplied:0,appliedToNew:0,appliedToOld:0,closing:0};entry[`${type}Applied`]+=Number(row.currentApplied||0);entry.appliedToNew+=Number(row.currentAppliedToNew||0);entry.appliedToOld+=Number(row.currentAppliedToOld||0);entry.closing+=Number(row.closingBalance||0);map.set(key,entry);}return map;}
export function buildCollectionDeliveryRows(analysis={},projection={},analytics={},reportDate=''){
  const allocations=projectionIndex(projection),customerIndex=indexCustomerAnalytics(analytics?.rows||[]),activityIndex=buildReportActivityIndex(analysis,'',reportDate),grouped=new Map();
  for(const row of analysis?.collections||[]){const code=clean(row.customerCode||row.accountCode),name=clean(row.customer||row.customerName||row.accountName)||code||'عميل غير محدد',key=customerKey(code,name),entry=grouped.get(key)||{key,code,name,amount:0,treasuries:new Set()};entry.amount+=Number(row.amount??row.debit??row.credit??0);const treasury=[clean(row.treasuryCode),clean(row.treasuryName)].filter(Boolean).join(' — ');if(treasury)entry.treasuries.add(treasury);grouped.set(key,entry);}
  return[...grouped.values()].map(row=>{const base=lookupCustomer(customerIndex,row.code,row.name)||{},activity=lookupActivity(activityIndex,row.code,row.name),settlement=settleCustomerAccount(base,activity,{reportDate}),allocation=allocations.get(row.key)||{};return{...row,treasuries:[...row.treasuries],blockApplied:Number(allocation.blockApplied||0),concreteApplied:Number(allocation.concreteApplied||0),linked:Boolean(base?.key||allocations.has(row.key)),closing:settlement.finalDebt,...settlement};}).sort((a,b)=>b.amount-a.amount||a.name.localeCompare(b.name,'ar'));
}
async function saveCustomerChoices(chatId,rows,reportDate){if(String(chatId).startsWith('-')||!rows.length)return false;const choices=rows.map((row,index)=>({code:row.code,name:row.name,balance:row.finalDebt,index}));await insert('bot_sessions',[{channel:'telegram',chat_id:String(chatId),external_user_id:String(chatId),state:'enterprise_customer_choose',context:{query:`سداد العملاء ${reportDate}`,choices,startedAt:new Date().toISOString()},updated_at:new Date().toISOString()}],{query:'on_conflict=channel,chat_id,external_user_id',prefer:'resolution=merge-duplicates,return=representation'});return true;}
function collectionText(rows,sourceFile,reportDate,page,pages,offset=0){
  const total=rows.reduce((sum,row)=>sum+row.amount,0),body=rows.map((row,index)=>{const sector=[row.blockApplied>0?`بلوك ${money(row.blockApplied)}`:'',row.concreteApplied>0?`خرسانة ${money(row.concreteApplied)}`:''].filter(Boolean).join(' | '),advance=row.finalAdvance>0?`\nدفعة مقدمة: <b>${money(row.finalAdvance)}</b>`:'',alerts=(row.alertLabels||[]).length?`\nتنبيه: ${esc(row.alertLabels.join(' — '))}`:'';return `<b>${offset+index+1}. ${esc(row.name)} — ${esc(row.customerClassLabel)}</b>\nرقم العميل: <code>${esc(row.code||'غير مسجل')}</code>\nالرصيد قبل التقرير: <b>${money(row.previousBalance)}</b>\nمشتريات التقرير: <b>${money(row.reportSales)}</b>\nسداد التقرير: <b>${money(row.reportCollections)}</b>\nمن المشتريات الجديدة: <b>${money(row.paidCurrent)}</b>\nمن الرصيد السابق: <b>${money(row.paidPrevious)}</b>\nالمتبقي النهائي: <b>${money(row.finalDebt)}</b>${advance}\nالحالة: ${esc(row.statusLabel)}${sector?`\nالقطاع: ${sector}`:''}\nالخزينة: ${esc(row.treasuries.join('، ')||'غير محددة')}${alerts}`;}).join('\n\n');
  return `<b>تقرير تسوية سداد العملاء</b>\nالملف: <b>${esc(sourceFile)}</b> | التاريخ: <b>${esc(reportDate)}</b> | الصفحة: <b>${page}/${pages}</b>\nقاعدة السداد: <b>الرصيد السابق أولًا، ثم مشتريات التقرير، ثم دفعة مقدمة.</b>\nإجمالي الصفحة: <b>${money(total)}</b>\n━━━━━━━━━━━━━━\n${body}`;
}
async function sendCollections(chatId,rows,sourceFile,reportDate,errors){if(!rows.length)return{sent:0,customers:0,linked:0};try{await saveCustomerChoices(chatId,rows,reportDate);}catch(error){errors.push(`collection-session:${chatId}:${clean(error?.message||error,180)}`);}const pageSize=4,pages=Math.ceil(rows.length/pageSize);let sent=0;for(let page=0;page<pages;page++){const offset=page*pageSize,part=rows.slice(offset,(page+1)*pageSize),buttons=String(chatId).startsWith('-')?[]:part.map((row,index)=>[{text:compactButtonName(row.name),callback_data:`ent:customer_pick|${offset+index}`}]);try{await sendMessage(chatId,collectionText(part,sourceFile,reportDate,page+1,pages,offset),buttons.length?keyboard(buttons):{});sent++;}catch(error){errors.push(`collections:${chatId}:${clean(error?.message||error,180)}`);}}return{sent,customers:rows.length,linked:rows.filter(row=>row.linked).length};}
async function sendTextToRecipients(recipients,text,errors,label){let sent=0;for(const chatId of recipients){try{await sendMessage(chatId,text);sent++;}catch(error){errors.push(`${label}:${chatId}:${clean(error?.message||error,180)}`);}}return sent;}
async function assertHistoricalCustomerSources(){await Promise.all([select('customers','select=external_id&limit=1'),select('sales_orders','select=reference_no&limit=1'),select('collection_events','select=reference_no&limit=1')]);}
export function buildErpDuplicateNoticeText({reportDate,sourceFile,upgrade}){
  if(upgrade?.upgraded){
    const added=Number(upgrade.salesAdded||0)+Number(upgrade.cashMovementsAdded||0)+Number(upgrade.treasuriesAdded||0)+Number(upgrade.inventoryAdded||0);
    return `<b>تم تحديث تقرير ERP القديم بنجاح</b>\nتم استكمال البيانات الناقصة دون تكرار أي حركة.\nالتاريخ: <b>${esc(reportDate)}</b>\nالملف: <b>${esc(sourceFile)}</b>\nالسجلات المستكملة: <b>${added}</b>\nالفواتير: <b>${Number(upgrade.salesCount||0)}</b> | الحركات المالية: <b>${Number(upgrade.cashMovementCount||0)}</b>\nالخزائن: <b>${Number(upgrade.treasuryCount||0)}</b> | المخزون: <b>${Number(upgrade.inventoryCount||0)}</b>`;
  }
  return `<b>مزامنة تقرير ERP</b>\nالملف موجود ومرحّل سابقًا، ولم تُكرر أي حركة.\nالتاريخ: <b>${esc(reportDate)}</b>\nالملف: <b>${esc(sourceFile)}</b>`;
}
export async function sendErpDuplicateNotice({reportDate,sourceFile,upgrade}){const recipients=erpTelegramRecipients(),errors=[],text=buildErpDuplicateNoticeText({reportDate,sourceFile,upgrade});return{recipients,sent:await sendTextToRecipients(recipients,text,errors,'duplicate'),errors};}
export async function sendErpFailureNotice({reportDate,sourceFile,reason}){const recipients=erpTelegramRecipients(),errors=[],text=`<b>فشل مزامنة تقرير ERP</b>\nلم تُرحّل أي حركة.\nالتاريخ: <b>${esc(reportDate)}</b>\nالملف: <b>${esc(sourceFile)}</b>\n${esc(reason)}`;return{recipients,sent:await sendTextToRecipients(recipients,text,errors,'failure'),errors};}
export async function prepareErpSuccessDelivery({analysis={},sourceFile='daily-report.xlsx',reportDate}){
  const recipients=erpTelegramRecipients(),errors=[];let projection={},reports=[],analytics={rows:[]},analyticsReady=false;
  await loadProjectedCumulativeDailyReport(analysis,reportDate,{currentBatch:true}).then(value=>{projection=value;}).catch(error=>errors.push(`projection:${clean(error?.message||error,180)}`));
  try{await assertHistoricalCustomerSources();analytics=await loadCustomerAnalytics({active:true,role:'admin'},{asOf:reportDate,beforeDate:reportDate});analyticsReady=true;}catch(error){errors.push(`analytics-required-source:${clean(error?.message||error,220)}`);}
  const collections=analyticsReady?buildCollectionDeliveryRows(analysis,projection,analytics,reportDate):[];
  try{reports.push(...await generateCumulativeDailyPdfs(analysis,sourceFile,['block','concrete'],reportDate,{currentBatch:true}));}catch(error){errors.push(`daily-reports:${clean(error?.message||error,220)}`);}
  if(analyticsReady){try{const result=await generateAvailablePortfolioPdfs(analysis,sourceFile,['block','concrete'],{reportDate,currentBatch:true,ownershipAnalytics:analytics});reports.push(...result.reports);for(const item of result.errors)errors.push(`portfolio-${item.type}:${clean(item.message,180)}`);}catch(error){errors.push(`portfolio-reports:${clean(error?.message||error,220)}`);}}
  else errors.push('portfolio-reports:تم منع تقارير التسوية لأن مصادر العملاء التاريخية غير مكتملة.');
  return{recipients,collections,reports,errors,analyticsReady,preparedAt:new Date().toISOString()};
}
export async function sendErpSuccessDelivery({analysis={},sourceFile='daily-report.xlsx',reportDate,posting={},prepared=null}){
  const bundle=prepared?.recipients?prepared:await prepareErpSuccessDelivery({analysis,sourceFile,reportDate}),recipients=bundle.recipients,errors=[...(bundle.errors||[])],summary=analysis.summary||{},title=posting?.upgraded?'تم تحديث تقرير ERP القديم بنجاح':'تم ترحيل تقرير ERP بنجاح',text=`<b>${title}</b>\n━━━━━━━━━━━━━━\nالتاريخ: <b>${esc(reportDate)}</b>\nالملف: <b>${esc(sourceFile)}</b>\nالفواتير: <b>${Number(summary.invoiceCount||0)}</b>\nمبيعات البلوك: <b>${money(summary.blockSales)}</b>\nمبيعات الخرسانة: <b>${money(summary.concreteSales)}</b>\nإجمالي المبيعات: <b>${money(summary.salesTotal)}</b>\nالتحصيلات: <b>${money(summary.collectionTotal)}</b>${posting?.upgraded?'\nتم استكمال البنوك والتحصيلات والمخزون دون تكرار أي حركة.':posting?.duplicate?'\nالترحيل كان موجودًا سابقًا ولم تتكرر الحركة.':''}`;
  if(!posting?.duplicate)for(const report of bundle.reports||[]){if(!report.snapshot)continue;try{report.snapshot.sourceBatchId=clean(posting?.batchId||posting?.batch_id||posting?.id||'');report.snapshotPointer=await persistPortfolioReportSnapshot(report);}catch(error){errors.push(`snapshot:${report.type}:${clean(error?.message||error,180)}`);}}
  const summarySent=await sendTextToRecipients(recipients,text,errors,'summary'),collectionResults=[];for(const chatId of recipients)collectionResults.push(await sendCollections(chatId,bundle.collections||[],sourceFile,reportDate,errors));
  let documentsSent=0;for(const chatId of recipients)for(const report of bundle.reports||[]){try{await sendDocumentBuffer(chatId,report.pdf,report.filename,'application/pdf',report.caption);documentsSent++;}catch(error){errors.push(`document:${chatId}:${report.type||report.filename}:${clean(error?.message||error,180)}`);}}
  return{recipients,summarySent,documentsGenerated:(bundle.reports||[]).length,documentsSent,collectionResults,preparedAt:bundle.preparedAt,errors};
}
