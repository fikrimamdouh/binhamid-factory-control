===== LINES 3-9 =====
00003: <head>
00004: <meta charset="utf-8">
00005: <meta name="viewport" content="width=device-width, initial-scale=1">
00006: <title>نظام العهد والإقرارات — مصنع بن حامد</title>
00007: <style>
00008: :root{
00009:   --gold:#B4893A;
===== LINES 112-118 =====
00112: tbody tr:nth-child(even){background:#FCFAF7}
00113: tbody tr:hover{background:var(--gold-pale)}
00114: .empty{text-align:center;padding:34px 16px;color:var(--muted);font-size:13.5px}
00115: .empty .big{font-size:34px;display:block;margin-bottom:8px;opacity:.35}
00116: 
00117: .chip{display:inline-block;padding:2px 9px;border-radius:99px;font-size:11px;font-weight:600;white-space:nowrap}
00118: .chip.blk{background:#E8F0F5;color:var(--navy)}
===== LINES 862-868 =====
00862: .ops-filter{display:flex;gap:7px;align-items:center;flex-wrap:wrap;margin:0 0 10px}.ops-filter input,.ops-filter select{width:auto;min-width:125px;padding:7px 8px;font-size:11px;background:#fff}
00863: .ops-table-wrap{overflow:auto;border:1px solid var(--line);border-radius:10px}.ops-table{width:100%;border-collapse:collapse;min-width:760px}.ops-table th{background:#eef3f4;color:#2d4c5a;padding:8px 9px;font-size:10.5px;text-align:right;white-space:nowrap;position:sticky;top:0;z-index:1}.ops-table td{padding:8px 9px;border-top:1px solid #e6ecee;font-size:11px;vertical-align:middle}.ops-table tr:hover td{background:#fbfcfc}.ops-table .mono{font-family:Arial,sans-serif;direction:ltr;text-align:right}.ops-table .num{font-family:Arial,sans-serif;font-weight:700}.ops-table .muted{color:var(--muted);font-size:10px}.ops-table .act{white-space:nowrap}
00864: .ops-status{display:inline-flex;align-items:center;gap:4px;border-radius:20px;padding:3px 7px;font-size:9.5px;font-weight:800;background:#e8f3ee;color:#176448;white-space:nowrap}.ops-status:before{content:'';width:5px;height:5px;border-radius:50%;background:currentColor}.ops-status.warn{background:#fff4da;color:#9a6500}.ops-status.bad{background:#fae8e8;color:#a52e2e}.ops-status.info{background:#e5f0f8;color:#246d9a}.ops-status.muted{background:#edf0f1;color:#63747c}
00865: .ops-empty{padding:36px 18px;text-align:center;color:#73858e;font-size:12px}.ops-empty b{display:block;color:#31505e;font-size:15px;margin-bottom:4px}
00866: .ops-alerts{display:grid;gap:7px;max-height:340px;overflow:auto}.ops-alert{display:grid;grid-template-columns:7px 1fr auto;gap:8px;align-items:start;background:#fafcfc;border:1px solid #e3e9eb;border-radius:9px;padding:8px}.ops-alert .bar{width:7px;height:100%;min-height:32px;border-radius:9px;background:#d39a2d}.ops-alert.bad .bar{background:#b83e3e}.ops-alert.info .bar{background:#2673a8}.ops-alert b{display:block;font-size:11px;color:#193947}.ops-alert span{display:block;font-size:9.5px;color:#6b7d86;margin-top:2px}.ops-alert time{font:9.5px Arial;color:#71828a;white-space:nowrap}
00867: .ops-bars{display:grid;gap:8px}.ops-bar{display:grid;grid-template-columns:105px 1fr 45px;gap:8px;align-items:center;font-size:10px}.ops-bar .track{height:8px;border-radius:8px;background:#edf1f2;overflow:hidden}.ops-bar .fill{height:100%;background:#237155;border-radius:8px}.ops-bar b{font:10px Arial;text-align:left}
00868: .ops-master-links{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.ops-master{border:1px solid var(--line);border-radius:10px;padding:11px;cursor:pointer;background:#fbfcfc}.ops-master:hover{border-color:#9db7ae;background:#f3f8f6}.ops-master b{display:block;font-size:12px;color:#163746}.ops-master span{display:block;font-size:10px;color:var(--muted);margin-top:3px}
===== LINES 879-885 =====
00879: .ops-label-print{width:65mm;height:35mm;border:1px solid #111;border-radius:3mm;padding:3mm;display:grid;grid-template-columns:25mm 1fr;gap:3mm;align-items:center;direction:rtl;font-family:Arial}.ops-label-print svg{width:24mm;height:24mm}.ops-label-print b{font-size:11pt;display:block}.ops-label-print span{font-size:7.5pt;display:block;margin-top:2mm;word-break:break-all}
00880: .ops-mobile-nav{display:none}
00881: @media(max-width:1150px){.ops-kpis{grid-template-columns:repeat(3,1fr)}.ops-col-4,.ops-col-5,.ops-col-7,.ops-col-8{grid-column:span 6}}
00882: @media(max-width:820px){.ops-shell{grid-template-columns:1fr}.ops-side{position:static;max-height:none}.ops-nav{display:flex;overflow:auto}.ops-nav button{min-width:max-content}.ops-brand{display:none}.ops-toolbar{top:63px}.ops-grid{display:block}.ops-card{margin-bottom:10px}.ops-kpis{grid-template-columns:repeat(2,1fr)}.ops-master-links{grid-template-columns:1fr}.ops-form-grid{grid-template-columns:1fr}.ops-form-grid .full{grid-column:auto}}
00883: @media print{
00884:   .ops-modal-ov,.ops-drawer,.toast{display:none!important}
00885:   .ops-label-print{page-break-after:always;margin:0}
===== LINES 928-934 =====
00928:   <img id="tbLogo" alt="">
00929:   <div class="t">
00930:     <b id="tbName">مصنع بن حامد للبلوك والخرسانة الجاهزة</b>
00931:     <span>نظام العهد والإقرارات والمسؤوليات الوظيفية</span>
00932:   </div>
00933:   <div class="badge" id="tbSave">محفوظ</div>
00934: </div>
===== LINES 1017-1027 =====
01017:       <table>
01018:         <thead><tr><th>النموذج</th><th>لمن</th><th>يغطي</th></tr></thead>
01019:         <tbody>
01020:           <tr><td><b>إقرار عهدة مركبة</b></td><td>السائق / المشغّل</td><td>الميكسر أو المضخة أو اللودر — بالحالة والملحقات</td></tr>
01021:           <tr><td><b>إقرار محفظة عملاء</b></td><td>مندوب الخرسانة / البلوك</td><td>سقف الائتمان · مهلة السداد · مسؤولية التحصيل</td></tr>
01022:           <tr><td><b>إقرار المحاسب</b></td><td>المحاسب / أمين الصندوق</td><td>العهدة النقدية · الدفاتر · السرية</td></tr>
01023:           <tr><td><b>إقرار مشرف المحطة</b></td><td>مشرف محطة الخلط</td><td>مطابقة الخلطة · الجودة · الشكاوى</td></tr>
01024:           <tr><td><b>إقرار الميكانيكي</b></td><td>الورشة</td><td>العُدد · قطع الغيار · الصيانة الوقائية</td></tr>
01025:           <tr><td><b>إخلاء طرف</b></td><td>عند نهاية الخدمة</td><td>تسليم كل العهد قبل صرف المستحقات</td></tr>
01026:         </tbody>
01027:       </table>
===== LINES 1036-1042 =====
01036:   </div>
01037: 
01038:   <div class="card">
01039:     <h2>إقرار عهدة مركبة / معدة</h2>
01040:     <p class="sub">التعهد ورقة واحدة. أضف محضر الفحص أو سجل الأحداث فقط عند الحاجة.</p>
01041:     <div class="grid g3">
01042:       <div>
===== LINES 1078-1084 =====
01078:   </div>
01079: 
01080:   <div class="card">
01081:     <h2>إقرار مسؤولية محفظة عملاء</h2>
01082:     <p class="sub">يتحدث تلقائيًا من جميع تقارير الحركة اليومية، مع بيانات العميل المعتمدة وسقف الائتمان ومهلة السداد.</p>
01083:     <div class="grid g2">
01084:       <div>
===== LINES 1101-1115 =====
01101:   </div>
01102: 
01103:   <div class="card">
01104:     <h2>الإقرارات الوظيفية</h2>
01105:     <p class="sub">المحاسب · مشرف المحطة · الميكانيكي.</p>
01106:     <div class="grid g2">
01107:       <div>
01108:         <label>نوع الإقرار</label>
01109:         <select id="prKind" onchange="refreshRoleEmployeeOptions()">
01110:           <option value="acct">إقرار المحاسب / أمين الصندوق</option>
01111:           <option value="plant">إقرار مشرف محطة الخلط</option>
01112:           <option value="mech">إقرار الميكانيكي / فني الورشة</option>
01113:           <option value="clear">إخلاء طرف ونهاية خدمة</option>
01114:         </select>
01115:       </div>
===== LINES 1305-1311 =====
01305:   </div>
01306: 
01307:   <div class="card">
01308:     <h2>صيغة الإقرار الختامية</h2>
01309:     <p class="sub">تظهر أسفل كل نموذج فوق التواقيع مباشرة.</p>
01310:     <textarea id="t_ack" rows="4"></textarea>
01311:   </div>
===== LINES 3833-3841 =====
03833:   hij:      {ar:'هجري',                en:'Hijri',              ur:'ہجری'},
03834: 
03835:   /* ---- document titles ---- */
03836:   ttlCust:  {ar:'إقرار استلام عهدة مركبة / معدة',
03837:              en:'Vehicle / Equipment Custody Acknowledgement',
03838:              ur:'گاڑی / مشینری تحویل کا اقرارنامہ'},
03839:   ebCust:   {ar:'عهــدة أصــول',       en:'ASSET CUSTODY',      ur:'اثاثہ تحویل'},
03840:   ttlIns:   {ar:'محضر فحص وتسليم فني',
03841:              en:'Technical Inspection & Handover Report',
===== LINES 3849-3855 =====
03849:   /* ---- section headers ---- */
03850:   secEmp:   {ar:'بيانات المُستلِم',    en:'Recipient Details',  ur:'وصول کنندہ کی تفصیلات'},
03851:   secVeh:   {ar:'بيانات المركبة / المعدة', en:'Vehicle / Equipment Details', ur:'گاڑی / مشینری کی تفصیلات'},
03852:   secTerms: {ar:'الإقرارات والالتزامات', en:'Declarations & Undertakings', ur:'اقرار و وعدے'},
03853:   secAtt:   {ar:'إقـــرار وتعهــد',    en:'ATTESTATION',        ur:'تصدیق'},
03854:   secTyre:  {ar:'فحص الإطارات',        en:'Tyre Inspection',    ur:'ٹائر معائنہ'},
03855:   secMech:  {ar:'الميكانيكا والسوائل', en:'Mechanicals & Fluids', ur:'مکینیکل و سیال'},
===== LINES 3943-3954 =====
03943:   ack: {
03944:     ar:'وبناءً عليه، أُقر أنا الموقّع أدناه بأنني قرأت هذا النموذج وفهمت جميع بنوده فهمًا تامًا، ووقّعت عليه بكامل إرادتي الحرة ودون أي إكراه، وأُقر بأنه يمثل التزامًا نافذًا في مواجهتي، وأن مخالفة أي بند من بنوده تستوجب المساءلة وفق لائحة الجزاءات المعتمدة وأحكام نظام العمل السعودي ولائحته التنفيذية.',
03945:     en:'Accordingly, I the undersigned declare that I have read this document and fully understood all of its clauses, and that I have signed it of my own free will without any coercion. I acknowledge that it constitutes a binding obligation upon me, and that any breach of its clauses shall render me liable under the approved Penalties Schedule and the provisions of the Saudi Labour Law and its Implementing Regulations.',
03946:     ur:'لہٰذا، میں دستخط کنندہ اقرار کرتا ہوں کہ میں نے یہ دستاویز پڑھی اور اس کی تمام شقیں مکمل طور پر سمجھ لی ہیں، اور اپنی مرضی سے بغیر کسی جبر کے دستخط کیے ہیں۔ میں تسلیم کرتا ہوں کہ یہ مجھ پر لازم ہے، اور کسی بھی شق کی خلاف ورزی پر سعودی لیبر لاء اور منظور شدہ جرمانہ فہرست کے تحت کارروائی ہوگی۔'
03947:   },
03948:   ackIns: {
03949:     ar:'أُقر بأنني عاينت المركبة معاينة فعلية ونافية للجهالة، وفحصت جميع بنودها بنفسي، وأن ما أثبتُّه يمثل حالتها الحقيقية لحظة الاستلام. وأُقر بأن أي عيب أو نقص يُكتشف لاحقًا ولم يُدوَّن في هذا المحضر يُعد ناشئًا في فترة عهدتي وأتحمل مسؤوليته كاملةً.',
03950:     en:'I declare that I have physically inspected the vehicle, examined every item personally, and that what I have recorded represents its true condition at the moment of handover. Any defect or shortage discovered later and not recorded herein shall be deemed to have arisen during my custody, and I bear full responsibility for it.',
03951:     ur:'میں اقرار کرتا ہوں کہ میں نے گاڑی کا خود معائنہ کیا اور تمام اشیاء جانچی ہیں، اور جو کچھ درج کیا ہے وہ حوالگی کے وقت اس کی اصل حالت ہے۔ بعد میں دریافت ہونے والا کوئی نقص جو یہاں درج نہیں، میری تحویل کے دوران کا سمجھا جائے گا اور میں اس کا مکمل ذمہ دار ہوں گا۔'
03952:   },
03953:   colo: {
03954:     ar:'حُرِّرت هذه الوثيقة من نسختين أصليتين — نسخة تُحفظ لدى المنشأة وأخرى تُسلَّم للموقّع، ولكلٍّ منهما ذات الحجية القانونية. تخضع لأحكام نظام العمل السعودي ولائحته التنفيذية ولائحة الجزاءات المعتمدة. لا يُعتد بأي تعديل أو شطب ما لم يكن موقّعًا بالأحرف الأولى من جميع الأطراف.',
===== LINES 3996-4002 =====
03996: I undertake to return the vehicle and its accessories in sound condition upon Management's demand or at the end of my employment. My final dues shall not be released before clearance is issued.
03997: I acknowledge that this vehicle is wholly owned by the Company, and that no right of ownership, use or possession accrues to me over it in any capacity.`,
03998: 
03999: ur: `میں اقرار کرتا ہوں کہ میں نے مذکورہ گاڑی/مشینری درست تکنیکی حالت میں، تمام لوازمات اور قانونی کاغذات سمیت وصول کی، اور اس کا مکمل معائنہ کیا ہے۔
04000: میں وعدہ کرتا ہوں کہ گاڑی صرف سرکاری کام کے لیے استعمال کروں گا۔ ذاتی استعمال، کرایہ پر دینا یا کسی اور کو دینا انتظامیہ کی تحریری اجازت کے بغیر ممنوع ہے۔
04001: میں وعدہ کرتا ہوں کہ گاڑی کے زمرے کے مطابق کارآمد ڈرائیونگ لائسنس رکھوں گا، ٹریفک قوانین کی مکمل پابندی کروں گا، اور اپنی تحویل کے دوران تمام ٹریفک جرمانوں کا ذمہ دار ہوں گا۔
04002: میں وعدہ کرتا ہوں کہ روزانہ چلانے سے پہلے معائنہ کروں گا (تیل، پانی، ٹائر، بریک، لائٹس)، اور حفاظت کو خطرے میں ڈالنے والی خرابی کی صورت میں گاڑی نہیں چلاؤں گا۔
===== LINES 4235-4241 =====
04235:       D.cfg = {...D.cfg, ...(p.cfg||{})};
04236:       if(D.cfg.name) D.cfg.name = D.cfg.name.replace(/^مؤسسة\s+/, '');
04237:       D.txt = {...DEF, ...(p.txt||{})};
04238:       D.veh = p.veh||[]; D.emp = p.emp||[]; D.cli = p.cli||[]; D.seq = p.seq||1;
04239:     }
04240:   }catch(e){ console.warn(e); }
04241: }
===== LINES 4281-4297 =====
04281: function rAll(){ rStats(); rVeh(); rEmp(); rCli(); rSel(); }
04282: 
04283: function rStats(){
04284:   const drv = D.emp.filter(e=>e.role.includes('سائق')||e.role.includes('مشغّل')).length;
04285:   const rep = D.emp.filter(e=>e.role.includes('مندوب')||e.role.includes('مسؤول مبيعات')).length;
04286:   const exp = D.cli.reduce((s,c)=>s+Number(c.cap||0),0);
04287:   $('stats').innerHTML = `
04288:     <div class="stat"><b>${D.veh.length}</b><span>مركبة ومعدة</span></div>
04289:     <div class="stat"><b>${D.emp.length}</b><span>موظف مسجّل</span></div>
04290:     <div class="stat"><b>${drv}</b><span>سائق / مشغّل</span></div>
04291:     <div class="stat"><b>${rep}</b><span>مندوب مبيعات</span></div>
04292:     <div class="stat"><b>${D.cli.length}</b><span>عميل</span></div>
04293:     <div class="stat"><b>${money(exp)}</b><span>إجمالي سقوف الائتمان (ر.س)</span></div>`;
04294:   $('cVeh').textContent=D.veh.length; $('cEmp').textContent=D.emp.length; $('cCli').textContent=D.cli.length;
04295: }
04296: 
04297: function rVeh(){
===== LINES 4300-4306 =====
04300:   const L = g ? D.veh.filter(v=>v.grp===g) : D.veh;
04301:   if(!L.length){ t.innerHTML=`<tr><td colspan="8"><div class="empty"><span class="big">🚛</span>${D.veh.length?'لا توجد نتائج في هذه المجموعة':'لا توجد مركبات — اضغط «استيراد أصول ART ERP» أو «إضافة مركبة»'}</div></td></tr>`; return; }
04302:   t.innerHTML = L.map(v=>{
04303:     const d = D.emp.find(e=>e.id===v.drv);
04304:     return `<tr>
04305:       <td><b class="mono">${esc(v.plate||'—')}</b></td>
04306:       <td>${esc(v.type)}</td>
===== LINES 4319-4325 =====
04319: 
04320: function rEmp(){
04321:   const f=$('fEmp').value, t=$('tEmp');
04322:   const L = f? D.emp.filter(e=>e.role===f) : D.emp;
04323:   if(!L.length){ t.innerHTML=`<tr><td colspan="8"><div class="empty"><span class="big">👷</span>لا يوجد موظفون — اضغط «إضافة موظف»</div></td></tr>`; return; }
04324:   t.innerHTML = L.map(e=>`<tr>
04325:     <td><b>${esc(e.name)}</b></td>
===== LINES 4342-4348 =====
04342:   if(r) L=L.filter(c=>c.rep===r);
04343:   if(!L.length){ t.innerHTML=`<tr><td colspan="8"><div class="empty"><span class="big">🧾</span>لا يوجد عملاء — اضغط «إضافة عميل»</div></td></tr>`; return; }
04344:   t.innerHTML = L.map(c=>{
04345:     const rp = D.emp.find(e=>e.id===c.rep);
04346:     const sc = c.seg==='بلوك'?'blk':'con';
04347:     return `<tr>
04348:       <td><b>${esc(c.name)}</b></td>
===== LINES 4362-4371 =====
04362: function roleDeclarationEmployees(kind){
04363:   const active = e=>e.act!==false;
04364:   const role = e=>String(e.role||'');
04365:   if(kind==='clear') return D.emp.slice();
04366:   if(kind==='acct') return D.emp.filter(e=>active(e)&&/(محاسب|صندوق)/.test(role(e)));
04367:   if(kind==='plant') return D.emp.filter(e=>active(e)&&/(مشرف.*محطة|محطة.*خلط)/.test(role(e)));
04368:   if(kind==='mech') return D.emp.filter(e=>active(e)&&/(ميكانيكي|فني.*ورشة)/.test(role(e)));
04369:   return [];
04370: }
04371: 
===== LINES 4380-4387 =====
04380: }
04381: 
04382: function rSel(){
04383:   const reps = D.emp.filter(e=>e.role.includes('مندوب')||e.role.includes('مسؤول مبيعات'));
04384:   const drvs = D.emp.filter(e=>e.role.includes('سائق')||e.role.includes('مشغّل'));
04385: 
04386:   $('fEmp').innerHTML = '<option value="">كل الوظائف</option>'+ROLES.map(r=>`<option ${$('fEmp').value===r?'selected':''}>${r}</option>`).join('');
04387:   $('fRep').innerHTML = '<option value="">كل المندوبين</option>'+reps.map(e=>`<option value="${e.id}" ${$('fRep').value===e.id?'selected':''}>${esc(e.name)}</option>`).join('');
===== LINES 4390-4396 =====
04390:     ? D.veh.map(v=>`<option value="${v.id}">${esc(v.plate)} — ${esc(v.type)}${v.make?' — '+esc(v.make):''}</option>`).join('')
04391:     : '<option value="">— لا توجد مركبات —</option>';
04392:   $('pvEmp').innerHTML = '<option value="">— اتركه فارغًا —</option>'+
04393:     (drvs.length?drvs:D.emp).map(e=>`<option value="${e.id}">${esc(e.name)} — ${esc(e.role)}</option>`).join('');
04394: 
04395:   $('pcEmp').innerHTML = reps.length
04396:     ? reps.map(e=>`<option value="${e.id}">${esc(e.name)} — ${esc(e.role)}</option>`).join('')
===== LINES 4410-4416 =====
04410: 
04411: function vehForm(id){
04412:   const v = id? D.veh.find(x=>x.id===id) : {};
04413:   const drvs = D.emp.filter(e=>e.role.includes('سائق')||e.role.includes('مشغّل'));
04414:   openMo(id?'تعديل مركبة':'إضافة مركبة', `
04415:     <div class="grid g2">
04416:       <div><label>رقم اللوحة *</label><input id="m_plate" value="${esc(v.plate||'')}" placeholder="أ ب ج 1234"></div>
===== LINES 4446-4452 =====
04446: }
04447: 
04448: function empForm(id){
04449:   const e = id? D.emp.find(x=>x.id===id) : {act:true};
04450:   openMo(id?'تعديل موظف':'إضافة موظف', `
04451:     <div class="grid g2">
04452:       <div style="grid-column:1/-1"><label>الاسم الرباعي *</label><input id="m_name" value="${esc(e.name||'')}"></div>
===== LINES 4465-4478 =====
04465:       const o = {id: id||uid(), name:V('name'), nid:V('nid'), nat:V('nat'), role:V('role'),
04466:         no:V('no'), tel:V('tel'), hire:V('hire'), lic:V('lic'), licE:V('licE'),
04467:         cash:V('cash'), act: V('act')==='1'};
04468:       if(id){ D.emp[D.emp.findIndex(x=>x.id===id)]=o; } else D.emp.push(o);
04469:       save(); rAll(); toast(id?'تم التعديل':'تمت الإضافة');
04470:     });
04471: }
04472: 
04473: function cliForm(id){
04474:   const c = id? D.cli.find(x=>x.id===id) : {seg:'خرسانة'};
04475:   const reps = D.emp.filter(e=>e.role.includes('مندوب')||e.role.includes('مسؤول مبيعات'));
04476:   openMo(id?'تعديل عميل':'إضافة عميل', `
04477:     <div class="grid g2">
04478:       <div style="grid-column:1/-1"><label>اسم العميل / المنشأة *</label><input id="m_name" value="${esc(c.name||'')}"></div>
===== LINES 4555-4561 =====
04555:       if(!p.cfg && !p.veh) throw 0;
04556:       if(!confirm('سيتم استبدال كل البيانات الحالية. متابعة؟')) return;
04557:       D.cfg={...D.cfg,...(p.cfg||{})}; D.txt={...DEF,...(p.txt||{})};
04558:       D.veh=p.veh||[]; D.emp=p.emp||[]; D.cli=p.cli||[]; D.seq=p.seq||1;
04559:       save(); fillCfg(); fillTxt(); rAll(); toast('تم الاستيراد بنجاح');
04560:     }catch(x){ toast('الملف غير صالح','err'); } };
04561:   r.readAsText(f); e.target.value='';
===== LINES 4569-4578 =====
04569: /* ================== EXCEL EXPORT ================== */
04570: function xlsx(k){
04571:   const wb = XLSX.utils.book_new();
04572:   const nm  = id => D.emp.find(x=>x.id===id)?.name || '';
04573:   const nid = id => D.emp.find(x=>x.id===id)?.nid  || '';
04574:   const tel = id => D.emp.find(x=>x.id===id)?.tel  || '';
04575:   const rol = id => D.emp.find(x=>x.id===id)?.role || '';
04576: 
04577:   const add = (rows, title, widths) => {
04578:     if(!rows.length) return;
===== LINES 4626-4632 =====
04626:     }));
04627: 
04628:   /* ---- 3. الموظفون ---- */
04629:   const SE = () => D.emp.map(e=>{
04630:     const mine = D.veh.filter(v=>v.drv===e.id);
04631:     const cls  = D.cli.filter(c=>c.rep===e.id);
04632:     return {
===== LINES 4667-4674 =====
04667: 
04668:   /* ---- 5. ملخص ---- */
04669:   const SS = () => {
04670:     const drv  = D.emp.filter(e=>e.role.includes('سائق')||e.role.includes('مشغّل'));
04671:     const rep  = D.emp.filter(e=>e.role.includes('مندوب')||e.role.includes('مسؤول مبيعات'));
04672:     const noDr = D.veh.filter(v=>!v.drv);
04673:     const noRp = D.cli.filter(c=>!c.rep);
04674:     const val  = D.veh.reduce((s,v)=>s+Number(v.val||0),0);
===== LINES 4682-4688 =====
04682:       {'البند':'إجمالي قيمة الأصول (ر.س)', 'القيمة':val},
04683:       {'البند':'مركبات بدون سائق مُسند',   'القيمة':noDr.length},
04684:       {'البند':'', 'القيمة':''},
04685:       {'البند':'إجمالي الموظفين',          'القيمة':D.emp.length},
04686:       {'البند':'السائقون والمشغّلون',      'القيمة':drv.length},
04687:       {'البند':'مندوبو المبيعات',          'القيمة':rep.length},
04688:       {'البند':'', 'القيمة':''},
===== LINES 4873-4879 =====
04873: /* ═══════════ DOC 2 — CLIENT PORTFOLIO ═══════════ */
04874: function clientPortfolioForEmployee(e, seg){
04875:   const role=opsNormArabic(e?.role||''),requested=seg==='خرسانة'?'concrete':seg==='بلوك'?'block':seg==='all'?'':seg==='auto'?(role.includes('خرسان')?'concrete':role.includes('بلوك')?'block':''):'';
04876:   const deliveries=(typeof OPS!=='undefined'&&OPS?.deliveries||[]).filter(d=>d.status!=='returned'&&d.employeeId===e.id&&(!requested||opsSalesKind(d)===requested)),activity=new Map();
04877:   deliveries.forEach(d=>{if(!d.clientId)return;const a=activity.get(d.clientId)||{kinds:new Set(),lastDate:'',sales:0};const kind=opsSalesKind(d);if(kind)a.kinds.add(kind);a.lastDate=String(d.date||'')>a.lastDate?String(d.date||''):a.lastDate;a.sales+=Number(d.amount||0);activity.set(d.clientId,a);});
04878:   return D.cli.filter(c=>{
04879:     const assigned=c.rep===e.id||(Array.isArray(c.repIds)&&c.repIds.includes(e.id)),active=activity.has(c.id);if(!assigned&&!active)return false;if(!requested)return true;const a=activity.get(c.id);if(a?.kinds.has(requested))return true;return assigned&&(c.seg===(requested==='concrete'?'خرسانة':'بلوك')||c.seg==='الاثنين');
===== LINES 4901-4907 =====
04901: 
04902:   const inner = `
04903:     ${mast()}
04904:     ${tbar('محفظــة عمــلاء','إقرار مسؤولية عن محفظة عملاء', R)}
04905: 
04906:     ${sec('١','بيانات المندوب', `<div class="dg">
04907:       ${fv('الاسم', e.name, 'lg', 'w2 hi')}
===== LINES 4932-4938 =====
04932:     <div class="ledn">محدّث تلقائيًا من جميع تقارير الحركة${lastMovement?` حتى ${fmtG(lastMovement)}`:''} — صادر بتاريخ ${fmtG(today())} م — ويحل كل إصدار جديد محل ما قبله</div>`,
04933:     false, `${L.length} عميل`)}
04934: 
04935:     ${sec('٣','الإقرارات والالتزامات', cov(D.txt.cli||DEF.cli, ctx), true)}
04936:     ${clause('مسؤولية التحصيل والائتمان', D.txt.cliX||DEF.cliX, ctx)}
04937:     ${att(ctx)}
04938:     ${exe('المندوب المُقِر', e.name, 'مدير المبيعات', R)}
===== LINES 4945-4958 =====
04945:   const R = ref();
04946:   const ctx = {emp:e?.name};
04947:   const M = {
04948:     acct:  {eb:'عهــدة نقديــة', t:'إقرار مسؤولية — المحاسب / أمين الصندوق',
04949:             who:'المحاسب / أمين الصندوق', mgr:'المدير المالي', lab:'عهدة نقدية',
04950:             txt:D.txt.acct||DEF.acct, crit:null},
04951:     plant: {eb:'مسؤوليــة فنيــة', t:'إقرار مسؤولية — مشرف محطة الخلط',
04952:             who:'مشرف المحطة', mgr:'مدير الإنتاج', lab:'مسؤولية فنية',
04953:             txt:D.txt.plant||DEF.plant,
04954:             crit:{lb:'مسؤولية الجودة والمطابقة', tx:D.txt.plantX||DEF.plantX}},
04955:     mech:  {eb:'عهــدة ورشــة', t:'إقرار مسؤولية — الميكانيكي / فني الورشة',
04956:             who:'الميكانيكي / الفني', mgr:'مدير الورشة', lab:'عهدة ورشة',
04957:             txt:D.txt.mech||DEF.mech, crit:null}
04958:   }[kind];
===== LINES 5006-5015 =====
05006:       ${fv('الرقم الوظيفي', e?.no, 'mono')}
05007:       ${fv('رقم الجوال', e?.tel, 'mono')}
05008:       ${fv('تاريخ التعيين', fmtG(e?.hire), 'mono')}
05009:       ${fv('تاريخ الإقرار', fmtG(today()), 'mono')}
05010:     </div>`, false, e?.nat ? esc(e.nat) : null)}
05011:     ${s2}
05012:     ${sec('٣','الإقرارات والالتزامات', cov(M.txt, ctx), true)}
05013:     ${M.crit ? clause(M.crit.lb, M.crit.tx, ctx) : ''}
05014:     ${att(ctx)}
05015:     ${exe(M.who, e?.name, M.mgr, R)}
===== LINES 5061-5067 =====
05061:       </tr>`).join('')}</tbody>
05062:     </table>`, true, `${rows.length} بند`)}
05063: 
05064:     ${sec('٣','إقرار الموظف', `<ol class="cov">
05065:       <li>أُقر بأنني سلّمت جميع العهد والأصول والمستندات المملوكة للمنشأة والمذكورة أعلاه بحالة سليمة ومطابقة.</li>
05066:       <li>أُقر بعدم وجود أي عهدة أو مبالغ أو مستندات أو أصول لدي لم أُفصح عنها صراحةً في هذه الوثيقة.</li>
05067:       <li>أُقر بمسؤوليتي الكاملة عن أي عهدة يثبت لاحقًا أنها بحوزتي ولم يتم تسليمها، وأتحمل قيمتها كاملةً.</li>
===== LINES 5176-5182 =====
05176: function docVeh(v, empId, L){
05177:   L = L || LG;
05178:   const R = ref();
05179:   const e = D.emp.find(x=>x.id===(empId||v.drv));
05180:   const both = L !== 'ar';
05181:   const acc = (v.acc||'').split(/[·\n,،]/).map(s=>s.trim()).filter(Boolean);
05182: 
===== LINES 5261-5267 =====
05261: function docVehInspect(v, empId, L){
05262:   L = L || LG;
05263:   const R = ref();
05264:   const e = D.emp.find(x=>x.id===(empId||v.drv));
05265:   const both = L !== 'ar';
05266: 
05267:   const tyres = TYRE_POS.ar.map((p,i)=>`<tr>
===== LINES 5339-5345 =====
05339: function docVehLog(v, empId, L){
05340:   L = L || LG;
05341:   const R = ref();
05342:   const e = D.emp.find(x=>x.id===(empId||v.drv));
05343:   const both = L !== 'ar';
05344:   const KINDS = ['kAcc','kBrk','kFine','kMnt','kTyre','kFuel','kOth'];
05345: 
===== LINES 5394-5400 =====
05394: 
05395: /* ================== BULK DRIVER ASSIGNMENT ================== */
05396: function assignForm(){
05397:   const drv = D.emp.filter(e=>e.role.includes('سائق')||e.role.includes('مشغّل'));
05398:   if(!D.veh.length){ toast('لا توجد مركبات','err'); return; }
05399:   if(!drv.length){ toast('سجّل السائقين أولًا من تبويب الموظفين','err'); return; }
05400: 
===== LINES 5535-5541 =====
05535:   const v = D.veh.find(x=>x.id===vid);
05536:   if(!v){ toast('اختر مركبة أولًا','err'); return; }
05537:   const emp = id ? null : ($('pvEmp').value || null);
05538:   const e = D.emp.find(x=>x.id===(emp||v.drv));
05539:   preview(vehSet(v, emp, curLang(), $('pvIns').checked, $('pvLog').checked),
05540:     `عهدة ${v.plate||v.type}${e?' — '+e.name:''}`);
05541: }
===== LINES 5556-5580 =====
05556:   preview(docVehLog(v, $('pvEmp').value||null, curLang()), `سجل أحداث — ${v.plate||v.type}`);
05557: }
05558: function prCli(){
05559:   const e = D.emp.find(x=>x.id===$('pcEmp').value);
05560:   if(!e){ toast('اختر مندوبًا أولًا','err'); return; }
05561:   preview(docCli(e, $('pcSeg').value), `محفظة عملاء — ${e.name}`);
05562: }
05563: function prRole(){
05564:   const k = $('prKind').value;
05565:   const e = D.emp.find(x=>x.id===$('prEmp').value) || null;
05566:   if(!e){ toast('اختر الموظف أولًا','err'); return; }
05567:   if(k!=='clear' && !roleDeclarationEmployees(k).some(x=>x.id===e.id)){
05568:     toast('الموظف لا يطابق نوع الإقرار المختار','err'); return;
05569:   }
05570:   const nm = {acct:'إقرار المحاسب', plant:'إقرار مشرف المحطة',
05571:               mech:'إقرار الميكانيكي', clear:'إخلاء طرف'}[k];
05572:   preview(k==='clear' ? docClear(e) : docRole(k, e), `${nm} — ${e.name}`);
05573: }
05574: function prEverything(){
05575:   const L = curLang(), H = [];
05576:   D.veh.forEach(v=>H.push(vehSet(v, null, L, false, false)));
05577:   D.emp.filter(e=>e.role.includes('مندوب')||e.role.includes('مسؤول مبيعات')).forEach(e=>{ H.push(docCli(e,'auto')); });
05578:   roleDeclarationEmployees('acct').forEach(e=>{ H.push(docRole('acct',e)); });
05579:   roleDeclarationEmployees('plant').forEach(e=>{ H.push(docRole('plant',e)); });
05580:   roleDeclarationEmployees('mech').forEach(e=>{ H.push(docRole('mech',e)); });
===== LINES 5730-5739 =====
05730: /* ---- staging modal: show what will be imported, then commit ---- */
05731: function xlsxStage(found){
05732:   const plan = {emp:[], cli:[], veh:[], upd:[]}, warn = [];
05733:   const nameOf = n => D.emp.find(e=>e.name===n);
05734: 
05735:   const seenNid = new Set(D.emp.map(e=>e.nid).filter(Boolean));
05736:   const seenNm  = new Set(D.emp.map(e=>e.name));
05737:   if(found['الموظفون']) found['الموظفون'].forEach((raw,i)=>{
05738:     const o = mapRow(raw,'emp');
05739:     if(!o.name) return;
===== LINES 5810-5827 =====
05810:     ()=>{
05811:       /* employees first — vehicles/clients link to them by name */
05812:       plan.emp.forEach(o=>{
05813:         D.emp.push({id:uid(), name:o.name, nid:o.nid||'', nat:o.nat||'',
05814:           role:o.role||'أخرى', no:o.no||'', tel:o.tel||'', hire:o.hire||'',
05815:           lic:o.lic||'', licE:o.licE||'', cash:o.cash||'', act:o.act!==false});
05816:       });
05817:       plan.cli.forEach(o=>{
05818:         const rep = D.emp.find(e=>e.name===o._repName);
05819:         D.cli.push({id:uid(), name:o.name, seg:o.seg||'خرسانة', cr:o.cr||'',
05820:           ct:o.ct||'', tel:o.tel||'', cap:o.cap||null, days:o.days||null,
05821:           rep:rep?rep.id:'', addr:o.addr||'', note:o.note||''});
05822:       });
05823:       plan.veh.forEach(o=>{
05824:         const drv = D.emp.find(e=>e.name===o._drvName);
05825:         D.veh.push({id:uid(), plate:o.plate||'', type:o.type||'معدة أخرى',
05826:           make:o.make||'', yr:o.yr||'', vin:o.vin||'', eng:o.eng||'',
05827:           cap:o.cap||'', val:o.val||'', reg:o.reg||'', regE:o.regE||'',
===== LINES 5836-5842 =====
05836:           v[k] = u[k];
05837:         });
05838:         if(u._drvName){
05839:           const drv = D.emp.find(e=>e.name===u._drvName);
05840:           if(drv) v.drv = drv.id;
05841:         }
05842:       });
===== LINES 5924-5930 =====
05924:   const now = new Date().toISOString();
05925:   return {
05926:     meta:{version:3, createdAt:now, updatedAt:now, sequences:{MOV:1,CUS:1,TRP:1,FUL:1,MNT:1,STK:1,PRD:1,DLV:1,COL:1,INC:1,DOC:1,AST:1,CNT:1,SUP:1}, lastSnapshot:''},
05927:     settings:{companyName:'مصنع بن حامد للبلوك والخرسانة الجاهزة', lowStockDefault:5, alertDays:30, currency:'ر.س', backupLimit:10, concreteSalesEmployeeId:'', blockSalesEmployeeId:'', fuelExcludedVehicle:'Renault', fuelExcludedDriver:'فكري ممدوح', fuelPriceVariancePct:10, fuelVolumeVariancePct:50, fuelRapidRefillHours:24},
05928:     locations:[
05929:       {id:'loc-main',name:'المصنع الرئيسي',type:'مصنع',active:true},
05930:       {id:'loc-concrete',name:'محطة الخرسانة',type:'إنتاج',active:true},
===== LINES 5960-5976 =====
05960: function opsMoney(v){ const n=Number(v||0); return n.toLocaleString('en-US',{maximumFractionDigits:2}); }
05961: function opsNum(v){ return Number(v||0).toLocaleString('en-US',{maximumFractionDigits:2}); }
05962: function opsById(arr,id){ return (arr||[]).find(x=>x.id===id); }
05963: function opsEmp(id){ return (D.emp||[]).find(x=>x.id===id); }
05964: function opsSalesResponsibleRole(kind){return kind==='concrete'?'مسؤول مبيعات الخرسانة':'مسؤول مبيعات البلوك';}
05965: function opsSalesResponsibleKey(kind){return kind==='concrete'?'concreteSalesEmployeeId':'blockSalesEmployeeId';}
05966: function opsSalesResponsible(kind){
05967:   const key=opsSalesResponsibleKey(kind),role=opsSalesResponsibleRole(kind),byId=opsEmp(OPS.settings[key]);
05968:   return (byId?.act!==false?byId:null)||(D.emp||[]).find(x=>x.role===role&&x.act!==false)||null;
05969: }
05970: function opsUpsertSalesResponsible(kind,data){
05971:   const key=opsSalesResponsibleKey(kind),role=opsSalesResponsibleRole(kind),old=opsSalesResponsible(kind),id=old?.id||`ops-sales-${kind}`;
05972:   const employee={...(old||{}),id,name:String(data.name||'').trim(),nid:String(data.nid||'').trim(),no:String(data.no||'').trim(),tel:String(data.tel||'').trim(),role,act:true,nat:old?.nat||'',hire:old?.hire||'',lic:old?.lic||'',licE:old?.licE||'',cash:old?.cash||''};
05973:   const index=(D.emp||[]).findIndex(x=>x.id===id);if(index>=0)D.emp[index]=employee;else D.emp.push(employee);
05974:   OPS.settings[key]=id;return employee;
05975: }
05976: function opsVeh(id){ return (D.veh||[]).find(x=>x.id===id); }
===== LINES 5994-6000 =====
05994: function opsSelect(rows,value,placeholder){
05995:   return `<option value="">${opsEsc(placeholder||'— اختر —')}</option>`+(rows||[]).map(x=>`<option value="${opsEsc(x.id)}" ${x.id===value?'selected':''}>${opsEsc(x.label||x.name)}</option>`).join('');
05996: }
05997: function opsEmployees(activeOnly){ return (D.emp||[]).filter(e=>!activeOnly||e.act!==false).map(e=>({id:e.id,label:`${e.name} — ${e.role||''}`})); }
05998: function opsVehicles(){ return (D.veh||[]).map(v=>({id:v.id,label:`${v.plate||v.acct||'—'} — ${v.type||''} ${v.make||''}`})); }
05999: function opsClients(){ return (D.cli||[]).map(c=>({id:c.id,label:c.name})); }
06000: function opsLocations(){ return OPS.locations.filter(x=>x.active!==false).map(x=>({id:x.id,label:`${x.name} — ${x.type}`})); }
===== LINES 6195-6201 =====
06195:     const n=opsDaysUntil(v.regE);
06196:     if(n!==null && n<=days) A.push({level:n<0?'bad':'warn',title:`استمارة المركبة ${v.plate||v.acct||''}`,detail:n<0?`منتهية منذ ${Math.abs(n)} يوم`:`تنتهي خلال ${n} يوم`,date:v.regE,view:'documents'});
06197:   });
06198:   (D.emp||[]).forEach(e=>{
06199:     const n=opsDaysUntil(e.licE);
06200:     if(n!==null && n<=days) A.push({level:n<0?'bad':'warn',title:`رخصة قيادة ${e.name}`,detail:n<0?'منتهية':`تنتهي خلال ${n} يوم`,date:e.licE,view:'documents'});
06201:     if(e.act===false){ const c=OPS.custodies.filter(x=>x.employeeId===e.id&&x.status==='active'); if(c.length) A.push({level:'bad',title:`${e.name} موقوف وعليه ${c.length} عهدة`,detail:'يجب الاسترداد أو النقل',date:'',view:'custody'}); }
===== LINES 6231-6243 =====
06231:       </div>
06232:       <div class="ops-card ops-col-12"><h3>الوصول السريع</h3><div class="ops-master-links">${modules.map(x=>`<div class="ops-master" onclick="opsGo('${x[0]}')"><b>${x[1]}</b><span>${x[2]} سجل</span></div>`).join('')}</div></div>
06233:       <div class="ops-card ops-col-6"><h3>البيانات الأساسية</h3><div class="ops-master-links">
06234:         <div class="ops-master" onclick="go('emp')"><b>الموظفون</b><span>${D.emp.length} موظف — إضافة وتعديل</span></div>
06235:         <div class="ops-master" onclick="go('veh')"><b>المركبات</b><span>${D.veh.length} مركبة ومعدة</span></div>
06236:         <div class="ops-master" onclick="go('cli')"><b>العملاء</b><span>${D.cli.length} عميل</span></div>
06237:       </div></div>
06238:       <div class="ops-card ops-col-6"><h3>سلامة البيانات</h3><div class="ops-bars">
06239:         ${opsHealthBar('مركبات مسندة',D.veh.length?D.veh.filter(x=>x.drv).length/D.veh.length:0)}
06240:         ${opsHealthBar('موظفون بهوية',D.emp.length?D.emp.filter(x=>x.nid).length/D.emp.length:0)}
06241:         ${opsHealthBar('عهد موثقة',OPS.custodies.length?OPS.custodies.filter(x=>(x.attachments||[]).length).length/OPS.custodies.length:0)}
06242:       </div></div>
06243:     </div>`;
===== LINES 6251-6257 =====
06251: }
06252: function opsSearch(q){
06253:   const s=q.toLowerCase(), R=[];
06254:   D.emp.forEach(x=>{if([x.name,x.nid,x.no,x.tel,x.role].some(v=>String(v||'').toLowerCase().includes(s)))R.push({kind:'موظف',label:x.name,detail:`${x.role||''} — ${x.nid||''}`,view:'custody'});});
06255:   D.veh.forEach(x=>{if([x.plate,x.acct,x.vin,x.make,x.type].some(v=>String(v||'').toLowerCase().includes(s)))R.push({kind:'مركبة',label:x.plate||x.acct,detail:`${x.type||''} — ${x.make||''}`,view:'fleet'});});
06256:   D.cli.forEach(x=>{if([x.name,x.cr,x.tel].some(v=>String(v||'').toLowerCase().includes(s)))R.push({kind:'عميل',label:x.name,detail:x.tel||'',view:'deliveries'});});
06257:   OPS.assets.forEach(x=>{if([x.name,x.code,x.serial,x.category].some(v=>String(v||'').toLowerCase().includes(s)))R.push({kind:'أصل',label:x.name,detail:`${x.code||''} — ${x.serial||''}`,view:'custody'});});
===== LINES 6295-6301 =====
06295:    ============================================================ */
06296: function opsRenderCustody(){
06297:   const active=OPS.custodies.filter(x=>x.status==='active'), assets=OPS.assets.slice().sort((a,b)=>a.name.localeCompare(b.name,'ar'));
06298:   opsEl('opsContent').innerHTML=opsHead('الأصول والعهد','سجل التسليم والنقل والاسترداد وإخلاء الطرف',`<button class="ops-btn primary" onclick="opsCustodyIssueForm()">تسليم عهدة</button><button class="ops-btn blue" onclick="opsAssetForm()">إضافة أصل</button><button class="ops-btn ghost" onclick="go('print')">نماذج الإقرارات</button>`)+`
06299:     <div class="ops-kpis">${opsKpi(active.length,'عهد نشطة')}${opsKpi(OPS.custodies.filter(x=>x.status==='returned').length,'عهد مستردة','blue')}${opsKpi(OPS.custodies.filter(x=>x.status==='lost').length,'عهد مفقودة','danger')}${opsKpi(assets.length,'أصول عامة')}${opsKpi(D.veh.length,'مركبات ومعدات','purple')}${opsKpi(active.reduce((s,x)=>s+Number(x.value||0),0).toLocaleString('en-US'),'قيمة العهد النشطة','warn')}</div>
06300:     <div class="ops-card"><h3>العهد <span class="n">${OPS.custodies.length}</span></h3>${OPS.custodies.length?`<div class="ops-table-wrap"><table class="ops-table" id="cusTable"><thead><tr><th>الرقم</th><th>الأصل</th><th>الموظف</th><th>التسليم</th><th>الاستحقاق</th><th>القيمة</th><th>الحالة</th><th>المرفقات</th><th></th></tr></thead><tbody>${OPS.custodies.slice().sort((a,b)=>String(b.issueDate).localeCompare(String(a.issueDate))).map(c=>`<tr><td class="mono">${opsEsc(c.no)}</td><td><b>${opsEsc(c.assetLabel)}</b><div class="muted">${opsEsc(c.conditionOut||'')}</div></td><td>${opsEsc(opsEmp(c.employeeId)?.name||'—')}</td><td>${opsFmtDate(c.issueDate)}</td><td>${opsFmtDate(c.dueDate)}</td><td class="num">${c.value?opsMoney(c.value):'—'}</td><td>${opsStatus(c.status)}</td><td>${opsFileBadges(c.attachments)}</td><td class="act">${c.status==='active'?`<button class="ops-btn blue sm" onclick="opsCustodyTransferForm('${c.id}')">نقل</button> <button class="ops-btn primary sm" onclick="opsCustodyReturnForm('${c.id}')">استرداد</button> <button class="ops-btn red sm" onclick="opsCustodyProblemForm('${c.id}')">فقد/تلف</button>`:''} <button class="ops-btn ghost sm" onclick="opsCustodyDetail('${c.id}')">فتح</button></td></tr>`).join('')}</tbody></table></div>`:opsEmpty('لا توجد عهد مسجلة','استخدم تسليم عهدة لإنشاء أول سجل')}</div>
06301:     <div class="ops-card" style="margin-top:12px"><h3>الأصول العامة <span class="n">${assets.length}</span></h3>${assets.length?`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>الكود</th><th>الأصل</th><th>الفئة</th><th>الرقم التسلسلي</th><th>الموقع</th><th>القيمة</th><th>الحالة</th><th></th></tr></thead><tbody>${assets.map(a=>`<tr><td class="mono">${opsEsc(a.code)}</td><td><b>${opsEsc(a.name)}</b></td><td>${opsEsc(a.category)}</td><td class="mono">${opsEsc(a.serial||'—')}</td><td>${opsEsc(opsLoc(a.locationId)?.name||'—')}</td><td class="num">${a.value?opsMoney(a.value):'—'}</td><td>${opsStatus(a.active===false?'cancelled':'active')}</td><td><button class="ops-btn ghost sm" onclick="opsAssetForm('${a.id}')">تعديل</button> <button class="ops-btn sm" onclick="opsQrLabel('asset','${a.id}')">QR</button></td></tr>`).join('')}</tbody></table></div>`:opsEmpty('لا توجد أصول عامة','أضف الجوالات والأجهزة والعدد والأختام وكروت الوقود')}</div>`;
===== LINES 6311-6317 =====
06311:   </div>`,async fd=>{const o={id:id||opsUid('ast'),name:opsFd(fd,'name'),category:opsFd(fd,'category'),code:opsFd(fd,'code')||(id?a.code:opsNextNo('AST')),serial:opsFd(fd,'serial'),value:Number(opsFd(fd,'value')||0),locationId:opsFd(fd,'locationId'),purchaseDate:opsFd(fd,'purchaseDate'),warrantyDate:opsFd(fd,'warrantyDate'),notes:opsFd(fd,'notes'),active:a.active!==false,createdAt:a.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};if(id)OPS.assets[OPS.assets.findIndex(x=>x.id===id)]=o;else OPS.assets.push(o);await opsPersist(id?'تعديل أصل':'إضافة أصل');opsToast(id?'تم تعديل الأصل':'تمت إضافة الأصل');});
06312: }
06313: function opsCustodyIssueForm(){
06314:   if(!opsAssetsList().length){opsToast('أضف مركبة أو أصلًا أولًا','err');return;} if(!D.emp.length){opsToast('أضف موظفًا أولًا','err');return;}
06315:   opsOpenModal('تسليم عهدة',`<div class="ops-form-grid">
06316:     <div class="full"><label class="ops-required">الأصل</label><select name="assetKey" required>${opsSelect(opsAssetsList(),'','— اختر الأصل —')}</select></div>
06317:     <div><label class="ops-required">الموظف المستلم</label><select name="employeeId" required>${opsSelect(opsEmployees(true),'','— اختر الموظف —')}</select></div><div><label class="ops-required">تاريخ التسليم</label><input type="date" name="issueDate" value="${opsToday()}" required></div>
===== LINES 6378-6384 =====
06378:   const s=String(value||'').trim(),iso=s.match(/^(20\d{2})[-\/]([01]?\d)[-\/]([0-3]?\d)(?:[ T]([0-2]?\d):([0-5]?\d)(?::([0-5]?\d))?)?/);if(iso)return `${iso[1]}-${String(iso[2]).padStart(2,'0')}-${String(iso[3]).padStart(2,'0')}T${String(iso[4]||0).padStart(2,'0')}:${String(iso[5]||0).padStart(2,'0')}:${String(iso[6]||0).padStart(2,'0')}`;
06379:   const dmy=s.match(/^([0-3]?\d)[-\/]([01]?\d)[-\/](20\d{2})(?:[ T]([0-2]?\d):([0-5]?\d)(?::([0-5]?\d))?)?/);if(dmy)return `${dmy[3]}-${String(dmy[2]).padStart(2,'0')}-${String(dmy[1]).padStart(2,'0')}T${String(dmy[4]||0).padStart(2,'0')}:${String(dmy[5]||0).padStart(2,'0')}:${String(dmy[6]||0).padStart(2,'0')}`;return '';
06380: }
06381: function opsFuelEmployeeByName(name){const n=opsNormArabic(name);return (D.emp||[]).find(e=>opsNormArabic(e.name)===n)||null;}
06382: function opsFuelPlateKey(value){const p=opsPlateFingerprint(value);return p.clean||p.digits||opsNormArabic(value);}
06383: function opsFuelGroupKey(f){return opsFuelPlateKey(f?.plateRaw||opsVeh(f?.vehicleId)?.plate||'')||f?.vehicleId||'unknown';}
06384: function opsFuelDisplayPlate(f){return String(f?.plateRaw||opsVeh(f?.vehicleId)?.plate||'—').trim();}
===== LINES 6402-6408 =====
06402:   return {plan,employeeMap,vehicleMap,addedEmployees:[],addedVehicles:[]};
06403: }
06404: function opsQuickEmployeeForm(defaultName){
06405:   opsOpenModal('إضافة موظف مباشرة من التقارير',`<div class="ops-note">يكفي إدخال الاسم والوظيفة الآن، ويمكن استكمال الهوية والجوال وباقي البيانات لاحقًا من سجل الموظفين.</div><div class="ops-form-grid"><div class="full"><label class="ops-required">اسم الموظف</label><input name="name" value="${opsEsc(defaultName||'')}" required autofocus></div><div><label class="ops-required">الوظيفة</label><select name="role" required>${ROLES.map(x=>`<option>${opsEsc(x)}</option>`).join('')}</select></div><div><label>الرقم الوظيفي</label><input name="no"></div><div><label>رقم الجوال</label><input name="tel"></div><div><label>رقم الهوية / الإقامة</label><input name="nid"></div></div>`,async fd=>{const name=opsFd(fd,'name'),same=(D.emp||[]).find(e=>opsNormArabic(e.name)===opsNormArabic(name));if(same)throw new Error('الموظف موجود بالفعل: '+same.name);const employee={id:opsUid('emp'),name,nid:opsFd(fd,'nid'),nat:'',role:opsFd(fd,'role'),no:opsFd(fd,'no'),tel:opsFd(fd,'tel'),hire:'',lic:'',licE:'',cash:'',act:true,sourceReport:true,incomplete:true,createdAt:new Date().toISOString()};D.emp.push(employee);save();rAll();await opsPersist('إضافة موظف مباشرة من التقارير: '+name);opsToast('تمت إضافة الموظف ويمكن استكمال بياناته لاحقًا');},'إضافة الموظف');
06406: }
06407: function opsFuelRowKey(x){return `fuel|${x.receipt}|${String(x.date).slice(0,19)}|${opsPlateFingerprint(x.plate).clean}|${x.liters}|${x.amount}`;}
06408: function opsParseFuelWorkbook(wb,fileName){
===== LINES 6577-6587 =====
06577:   const out=OPS.deliveries.filter(x=>x.status==='out'), amount=OPS.deliveries.filter(x=>x.status!=='returned').reduce((s,x)=>s+Number(x.amount||0),0),col=OPS.collections.reduce((s,x)=>s+Number(x.amount||0),0);
06578:   opsEl('opsContent').innerHTML=opsHead('التوريد والعملاء','أوامر التوريد وحركة العملاء والتحصيل والائتمان',`<button class="ops-btn primary" onclick="opsDeliveryForm()">أمر توريد</button><button class="ops-btn gold" onclick="opsCollectionForm()">تسجيل تحصيل</button><button class="ops-btn ghost" onclick="go('cli')">سجل العملاء</button>`)+`
06579:   <div class="ops-kpis">${opsKpi(OPS.deliveries.length,'أوامر التوريد','blue')}${opsKpi(out.length,'توريدات بالطريق',out.length?'warn':'')}${opsKpi(opsMoney(amount),'قيمة التوريدات','purple')}${opsKpi(opsMoney(col),'التحصيلات')}${opsKpi(opsMoney(amount-col),'رصيد تقديري',amount-col>0?'warn':'')}${opsKpi(D.cli.filter(c=>opsCustomerExposure(c.id)>Number(c.cap??D.cfg.cap)).length,'متجاوزو الائتمان','danger')}</div>
06580:   <div class="ops-card"><h3>التوريدات <span class="n">${OPS.deliveries.length}</span></h3>${OPS.deliveries.length?`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>الرقم</th><th>التاريخ</th><th>العميل</th><th>المنتج</th><th>الكمية</th><th>المركبة</th><th>السائق</th><th>القيمة</th><th>الحالة</th><th></th></tr></thead><tbody>${OPS.deliveries.slice().sort((a,b)=>String(b.date).localeCompare(String(a.date))).map(d=>`<tr><td class="mono">${opsEsc(d.no)}</td><td>${opsFmtDate(d.date)}</td><td><b>${opsEsc(opsCli(d.clientId)?.name||'—')}</b><div class="muted">${opsEsc(d.destination||'')}</div></td><td>${opsEsc(d.product)}</td><td class="num">${opsNum(d.quantity)} ${opsEsc(d.unit)}</td><td>${opsEsc(opsVeh(d.vehicleId)?.plate||'—')}</td><td>${opsEsc(opsEmp(d.employeeId)?.name||'—')}</td><td class="num">${opsMoney(d.amount)}</td><td>${opsStatus(d.status)}</td><td>${d.status==='out'?`<button class="ops-btn primary sm" onclick="opsDeliveryFinishForm('${d.id}','delivered')">تسليم</button> <button class="ops-btn red sm" onclick="opsDeliveryFinishForm('${d.id}','returned')">مرتجع</button>`:''}</td></tr>`).join('')}</tbody></table></div>`:opsEmpty('لا توجد توريدات','سجل أول أمر توريد')}</div>
06581:   <div class="ops-grid" style="margin-top:12px"><div class="ops-card ops-col-7"><h3>التحصيلات <span class="n">${OPS.collections.length}</span></h3>${OPS.collections.length?`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>الرقم</th><th>التاريخ</th><th>العميل</th><th>المبلغ</th><th>الطريقة</th><th>المندوب</th><th>السند</th></tr></thead><tbody>${OPS.collections.slice(0,100).map(c=>`<tr><td class="mono">${opsEsc(c.no)}</td><td>${opsFmtDate(c.date)}</td><td>${opsEsc(opsCli(c.clientId)?.name||'—')}</td><td class="num">${opsMoney(c.amount)}</td><td>${opsEsc(c.method)}</td><td>${opsEsc(opsEmp(c.employeeId)?.name||'—')}</td><td>${opsEsc(c.receipt||'—')}</td></tr>`).join('')}</tbody></table></div>`:opsEmpty('لا توجد تحصيلات','')}</div><div class="ops-card ops-col-5"><h3>مراقبة الائتمان</h3><div class="ops-alerts">${D.cli.length?D.cli.map(c=>{const e=opsCustomerExposure(c.id),cap=Number(c.cap??D.cfg.cap),bad=e>cap;return `<div class="ops-alert ${bad?'bad':'info'}"><span class="bar"></span><div><b>${opsEsc(c.name)}</b><span>الرصيد ${opsMoney(e)} من سقف ${opsMoney(cap)}</span></div><time>${cap?Math.round(e/cap*100):0}%</time></div>`}).join(''):opsEmpty('لا يوجد عملاء','')}</div></div></div>`;
06582: }
06583: function opsDeliveryForm(){if(!D.cli.length){opsToast('أضف العملاء أولًا','err');return;}opsOpenModal('إنشاء أمر توريد',`<div class="ops-form-grid"><div><label class="ops-required">العميل</label><select name="clientId" required>${opsSelect(opsClients(),'','— اختر —')}</select></div><div><label class="ops-required">التاريخ</label><input type="date" name="date" value="${opsToday()}" required></div><div><label class="ops-required">المنتج</label><input name="product" required></div><div><label>المواصفة / الخلطة</label><input name="spec"></div><div><label class="ops-required">الكمية</label><input type="number" step="any" min="0.01" name="quantity" required></div><div><label>الوحدة</label><select name="unit"><option>م³</option><option>قطعة</option><option>طن</option></select></div><div><label>قيمة التوريد</label><input type="number" step="any" name="amount"></div><div><label>رقم طلب العميل</label><input name="clientOrder"></div><div><label>المركبة</label><select name="vehicleId">${opsSelect(opsVehicles(),'','— اختر —')}</select></div><div><label>السائق</label><select name="employeeId">${opsSelect(opsEmployees(true),'','— اختر —')}</select></div><div class="full"><label class="ops-required">مكان التوريد</label><input name="destination" required></div><div class="full"><label>ملاحظات</label><textarea name="notes" rows="2"></textarea></div></div>`,async fd=>{const d={id:opsUid('dlv'),no:opsNextNo('DLV'),clientId:opsFd(fd,'clientId'),date:opsFd(fd,'date'),product:opsFd(fd,'product'),spec:opsFd(fd,'spec'),quantity:Number(opsFd(fd,'quantity')),unit:opsFd(fd,'unit'),amount:Number(opsFd(fd,'amount')||0),clientOrder:opsFd(fd,'clientOrder'),vehicleId:opsFd(fd,'vehicleId'),employeeId:opsFd(fd,'employeeId'),destination:opsFd(fd,'destination'),notes:opsFd(fd,'notes'),status:'out',createdAt:new Date().toISOString()};const cli=opsCli(d.clientId),exposure=opsCustomerExposure(d.clientId)+d.amount,cap=Number(cli.cap??D.cfg.cap);d.creditWarning=cap>0&&exposure>cap;OPS.deliveries.unshift(d);opsAddMovementRecord('DELIVERY_OUT',{date:d.date+'T12:00',employeeId:d.employeeId,entityKind:'client',entityId:d.clientId,quantity:d.quantity,unit:d.unit,amount:d.amount,reference:d.no,notes:`${d.product} إلى ${d.destination}${d.creditWarning?' — تجاوز ائتماني':''}`,module:'delivery',moduleId:d.id});await opsPersist('أمر توريد '+d.no);if(d.creditWarning)opsToast('تم التسجيل مع تنبيه: العميل تجاوز سقف الائتمان','err');},'اعتماد التوريد');}
06584: function opsDeliveryFinishForm(id,status){const d=opsById(OPS.deliveries,id);if(!d)return;opsOpenModal(status==='delivered'?'تأكيد التسليم':'تسجيل مرتجع',`<div class="ops-note">${opsEsc(d.no)} — ${opsEsc(opsCli(d.clientId)?.name||'—')}</div><div class="ops-form-grid"><div><label class="ops-required">التاريخ</label><input type="date" name="date" value="${opsToday()}" required></div><div><label>${status==='returned'?'الكمية المرتجعة':'رقم سند التسليم'}</label><input name="reference"></div><div class="full"><label>ملاحظات</label><textarea name="notes" rows="2"></textarea></div><div class="full"><label>المستندات</label><input type="file" name="files" multiple></div></div>`,async fd=>{const files=await opsSaveFiles(fd.getAll('files'));d.status=status;d.finishDate=opsFd(fd,'date');d.finishReference=opsFd(fd,'reference');d.finishNotes=opsFd(fd,'notes');d.attachments=[...(d.attachments||[]),...files];opsAddMovementRecord(status==='delivered'?'DELIVERY_DONE':'DELIVERY_RETURN',{date:d.finishDate+'T12:00',employeeId:d.employeeId,entityKind:'client',entityId:d.clientId,quantity:status==='returned'?-d.quantity:d.quantity,unit:d.unit,amount:status==='returned'?-d.amount:d.amount,reference:d.finishReference||d.no,notes:d.finishNotes,module:'delivery',moduleId:d.id,attachments:files});await opsPersist((status==='delivered'?'تسليم':'مرتجع')+' توريد '+d.no);});}
06585: function opsCollectionForm(){if(!D.cli.length){opsToast('أضف العملاء أولًا','err');return;}opsOpenModal('تسجيل تحصيل عميل',`<div class="ops-form-grid"><div><label class="ops-required">العميل</label><select name="clientId" required>${opsSelect(opsClients(),'','— اختر —')}</select></div><div><label class="ops-required">التاريخ</label><input type="date" name="date" value="${opsToday()}" required></div><div><label class="ops-required">المبلغ</label><input type="number" step="any" min="0.01" name="amount" required></div><div><label>طريقة التحصيل</label><select name="method"><option>تحويل بنكي</option><option>نقدي</option><option>شيك</option><option>شبكة</option></select></div><div><label>رقم السند / التحويل</label><input name="receipt"></div><div><label>المندوب / المستلم</label><select name="employeeId">${opsSelect(opsEmployees(true),'','— اختر —')}</select></div><div class="full"><label>ملاحظات</label><textarea name="notes" rows="2"></textarea></div><div class="full"><label>صورة السند</label><input type="file" name="files" multiple></div></div>`,async fd=>{const files=await opsSaveFiles(fd.getAll('files'));const c={id:opsUid('col'),no:opsNextNo('COL'),clientId:opsFd(fd,'clientId'),date:opsFd(fd,'date'),amount:Number(opsFd(fd,'amount')),method:opsFd(fd,'method'),receipt:opsFd(fd,'receipt'),employeeId:opsFd(fd,'employeeId'),notes:opsFd(fd,'notes'),attachments:files,createdAt:new Date().toISOString()};OPS.collections.unshift(c);opsAddMovementRecord('COLLECTION',{date:c.date+'T12:00',employeeId:c.employeeId,entityKind:'client',entityId:c.clientId,amount:c.amount,reference:c.receipt||c.no,notes:c.method+(c.notes?' — '+c.notes:''),module:'collection',moduleId:c.id,attachments:files});await opsPersist('تحصيل عميل '+c.no);},'اعتماد التحصيل');}
06586: 
06587: /* ============================================================
===== LINES 6595-6609 =====
06595:    DOCUMENTS AND EXPIRY CONTROL
06596:    ============================================================ */
06597: function opsDocStatus(d){const n=opsDaysUntil(d.expiryDate);return n===null?'active':n<0?'expired':n<=Number(OPS.settings.alertDays||30)?'open':'active';}
06598: function opsRenderDocuments(){const docs=OPS.documents.slice().sort((a,b)=>String(a.expiryDate||'9999').localeCompare(String(b.expiryDate||'9999'))),exp=docs.filter(x=>opsDocStatus(x)==='expired').length,soon=docs.filter(x=>opsDocStatus(x)==='open').length;opsEl('opsContent').innerHTML=opsHead('الوثائق والتنبيهات','الرخص والتأمين والعقود والضمانات وتواريخ الانتهاء',`<button class="ops-btn primary" onclick="opsDocumentForm()">إضافة وثيقة</button><button class="ops-btn blue" onclick="opsLocationForm()">إضافة موقع</button>`)+`<div class="ops-kpis">${opsKpi(docs.length,'إجمالي الوثائق','blue')}${opsKpi(exp,'منتهية',exp?'danger':'')}${opsKpi(soon,'تنتهي قريبًا',soon?'warn':'')}${opsKpi(OPS.locations.length,'المواقع')}${opsKpi(D.veh.filter(x=>x.regE).length,'استمارات مركبات','purple')}${opsKpi(D.emp.filter(x=>x.licE).length,'رخص قيادة')}</div><div class="ops-card"><h3>سجل الوثائق <span class="n">${docs.length}</span></h3>${docs.length?`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>الرقم الداخلي</th><th>الوثيقة</th><th>النوع</th><th>مرتبطة بـ</th><th>رقم الوثيقة</th><th>الإصدار</th><th>الانتهاء</th><th>الحالة</th><th>المرفق</th></tr></thead><tbody>${docs.map(d=>`<tr><td class="mono">${opsEsc(d.no)}</td><td><b>${opsEsc(d.title)}</b></td><td>${opsEsc(d.type)}</td><td>${opsEsc(opsEntityLabel(d.entityKind,d.entityId))}</td><td class="mono">${opsEsc(d.documentNo||'—')}</td><td>${opsFmtDate(d.issueDate)}</td><td>${opsFmtDate(d.expiryDate)}</td><td>${opsStatus(opsDocStatus(d))}</td><td>${opsFileBadges(d.attachments)}</td></tr>`).join('')}</tbody></table></div>`:opsEmpty('لا توجد وثائق','أضف الاستمارات والتأمين والعقود والضمانات')}</div><div class="ops-card" style="margin-top:12px"><h3>المواقع والأقسام</h3><div class="ops-master-links">${OPS.locations.map(l=>`<div class="ops-master"><b>${opsEsc(l.name)}</b><span>${opsEsc(l.type)}</span></div>`).join('')}</div></div>`;}
06599: function opsDocumentForm(){const entities=[...opsAssetsList(),...opsEmployees(false).map(x=>({id:'employee:'+x.id,label:'موظف: '+x.label})),...opsClients().map(x=>({id:'client:'+x.id,label:'عميل: '+x.label}))];opsOpenModal('إضافة وثيقة',`<div class="ops-form-grid"><div><label class="ops-required">اسم الوثيقة</label><input name="title" required></div><div><label>النوع</label><select name="type"><option>استمارة</option><option>رخصة</option><option>تأمين</option><option>عقد</option><option>ضمان</option><option>ترخيص</option><option>وثيقة أخرى</option></select></div><div class="full"><label>مرتبطة بـ</label><select name="entityKey">${opsSelect(entities,'','— بدون ربط —')}</select></div><div><label>رقم الوثيقة</label><input name="documentNo"></div><div><label>الجهة المصدرة</label><input name="issuer"></div><div><label>تاريخ الإصدار</label><input type="date" name="issueDate"></div><div><label>تاريخ الانتهاء</label><input type="date" name="expiryDate"></div><div class="full"><label>نسخة الوثيقة</label><input type="file" name="files" multiple accept="image/*,.pdf"></div></div>`,async fd=>{const p=opsAssetParts(opsFd(fd,'entityKey')),files=await opsSaveFiles(fd.getAll('files'));const d={id:opsUid('doc'),no:opsNextNo('DOC'),title:opsFd(fd,'title'),type:opsFd(fd,'type'),entityKind:p.kind||'',entityId:p.id||'',documentNo:opsFd(fd,'documentNo'),issuer:opsFd(fd,'issuer'),issueDate:opsFd(fd,'issueDate'),expiryDate:opsFd(fd,'expiryDate'),attachments:files,createdAt:new Date().toISOString()};OPS.documents.push(d);opsAddMovementRecord('DOCUMENT',{date:(d.issueDate||opsToday())+'T12:00',entityKind:d.entityKind,entityId:d.entityId,reference:d.documentNo||d.no,notes:d.title,module:'document',moduleId:d.id,attachments:files});await opsPersist('إضافة وثيقة '+d.no);});}
06600: function opsLocationForm(){opsOpenModal('إضافة موقع أو قسم',`<div class="ops-form-grid"><div><label class="ops-required">اسم الموقع</label><input name="name" required></div><div><label>النوع</label><select name="type"><option>مصنع</option><option>إنتاج</option><option>مخزن</option><option>ورشة</option><option>إدارة</option><option>موقع عميل</option><option>أخرى</option></select></div></div>`,async fd=>{OPS.locations.push({id:opsUid('loc'),name:opsFd(fd,'name'),type:opsFd(fd,'type'),active:true});await opsPersist('إضافة موقع');});}
06601: 
06602: /* ============================================================
06603:    REPORTS, PRINT AND EXCEL EXPORT
06604:    ============================================================ */
06605: function opsRange(arr,field,from,to){return (arr||[]).filter(x=>{const d=String(x[field]||'').slice(0,10);return(!from||d>=from)&&(!to||d<=to);});}
06606: function opsSalesDeclarationRows(from,to){const rows=new Map();opsRange(OPS.deliveries,'date',from,to).filter(x=>x.status!=='returned').forEach(d=>{const employeeId=d.employeeId||opsCli(d.clientId)?.rep||'',key=employeeId||'unassigned',r=rows.get(key)||{employeeId,employee:opsEmp(employeeId)?.name||'غير مسند',invoices:0,concrete:0,blocks:0,cash:0,transfer:0,credit:0,total:0};r.invoices++;if(/خرسان/.test(opsNormArabic(d.product)))r.concrete+=Number(d.quantity||0);if(/بل[وؤ]ك/.test(opsNormArabic(d.product)))r.blocks+=Number(d.quantity||0);r.cash+=Number(d.cash||0);r.transfer+=Number(d.transfer||0);r.credit+=Number(d.credit||0);r.total+=Number(d.amount||0);rows.set(key,r);});return [...rows.values()].sort((a,b)=>b.total-a.total);}
06607: function opsSalesKind(d){const n=opsNormArabic(d?.product||'');return /خرسان/.test(n)?'concrete':/بل[وؤ]ك/.test(n)?'block':'';}
06608: function opsDailySalesLines(kind,date,batchId){return OPS.deliveries.filter(d=>d.status!=='returned'&&d.date===date&&opsSalesKind(d)===kind&&(!batchId||d.sourceImportId===batchId)).sort((a,b)=>String(a.clientOrder||'').localeCompare(String(b.clientOrder||''),'ar',{numeric:true}));}
06609: function opsDailySalesTotals(lines){return lines.reduce((o,d)=>{o.quantity+=Number(d.quantity||0);o.cash+=Number(d.cash||0);o.transfer+=Number(d.transfer||0);o.credit+=Number(d.credit||0);o.total+=Number(d.amount||0);return o;},{quantity:0,cash:0,transfer:0,credit:0,total:0});}
===== LINES 6645-6659 =====
06645:   const from=opsEl('repFrom')?.value||opsMonthStart(),to=opsEl('repTo')?.value||opsToday();
06646:   const M=opsRange(OPS.movements,'date',from,to),P=opsRange(OPS.production,'date',from,to),F=opsRange(OPS.fuel,'date',from,to),Dlv=opsRange(OPS.deliveries,'date',from,to),C=opsRange(OPS.collections,'date',from,to),Mt=opsRange(OPS.maintenance,'openDate',from,to);
06647:   const prod=P.reduce((s,x)=>s+Number(x.quantity||0),0),fuel=F.reduce((s,x)=>s+Number(x.liters||0),0),fuelCost=F.reduce((s,x)=>s+Number(x.cost||0),0),sales=Dlv.filter(x=>x.status!=='returned').reduce((s,x)=>s+Number(x.amount||0),0),collections=C.reduce((s,x)=>s+Number(x.amount||0),0),maint=Mt.reduce((s,x)=>s+Number(x.totalCost||0),0),salesRows=opsSalesDeclarationRows(from,to),warehouseRows=opsWarehouseDeclarationRows(from,to),imports=OPS.imports.filter(x=>(!from||x.reportDate>=from)&&(!to||x.reportDate<=to)),manager=opsManagerReportData(from,to);
06648:   opsEl('opsContent').innerHTML=opsHead('التقارير والملخص التنفيذي','اختر الفترة ثم اطبع أو صدّر ملفًا شاملًا',`<button class="ops-btn primary" onclick="opsPrintManagerReport()">تقرير المدير الشامل</button><button class="ops-btn gold" onclick="opsPrintDailySalesDeclaration('concrete')">إقرار الخرسانة اليومي</button><button class="ops-btn gold" onclick="opsPrintDailySalesDeclaration('block')">إقرار البلوك اليومي</button><button class="ops-btn ghost" onclick="opsPrintWarehouseDeclaration()">حركة المخازن</button><button class="ops-btn ghost" onclick="opsShowDieselReport(opsEl('repFrom').value,opsEl('repTo').value,'')">تقرير الديزل</button><button class="ops-btn ghost" onclick="opsQuickEmployeeForm()">إضافة موظف مباشرة</button><button class="ops-btn blue" onclick="opsExportAllExcel()">تصدير Excel شامل</button>`)+`
06649:   <div class="ops-card"><div class="ops-filter"><label>من</label><input id="repFrom" type="date" value="${from}"><label>إلى</label><input id="repTo" type="date" value="${to}"><button class="ops-btn" onclick="opsRenderReports()">تطبيق الفترة</button></div></div>
06650:   <div class="ops-kpis" style="margin-top:12px">${opsKpi(opsMoney(manager.concrete.total),'مبيعات الخرسانة','blue')}${opsKpi(opsMoney(manager.block.total),'مبيعات البلوك','purple')}${opsKpi(opsMoney(collections),'التحصيلات')}${opsKpi(opsMoney(manager.spareValue),'صرف قطع الغيار','warn')}${opsKpi(opsMoney(manager.rawInValue),'مشتريات الخامات','blue')}${opsKpi(opsMoney(fuelCost),'تكلفة الوقود','warn')}</div>
06651:   <div class="ops-grid"><div class="ops-card ops-col-6"><h3>المؤشرات المالية التشغيلية</h3><div class="ops-master-links"><div class="ops-master"><b>${opsMoney(manager.periodBalance)}</b><span>فرق مبيعات وتحصيلات الفترة</span></div><div class="ops-master"><b>${opsMoney(maint)}</b><span>تكلفة الصيانة</span></div><div class="ops-master"><b>${opsMoney(opsStockValue())}</b><span>قيمة المخزون الحالية</span></div></div></div><div class="ops-card ops-col-6"><h3>المؤشرات الرقابية</h3><div class="ops-master-links"><div class="ops-master"><b>${OPS.custodies.filter(x=>x.status==='active').length}</b><span>عهد نشطة</span></div><div class="ops-master"><b>${OPS.trips.filter(x=>x.status==='out').length}</b><span>مركبات بالخارج</span></div><div class="ops-master"><b>${opsAlerts().length}</b><span>تنبيهات</span></div></div></div>
06652:   <div class="ops-card ops-col-12"><h3>ملخص المدير للفترة</h3><div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>البند</th><th>العدد</th><th>الكمية / الساعات</th><th>القيمة</th></tr></thead><tbody><tr><td><b>الخرسانة</b></td><td>${manager.concrete.invoices} فاتورة</td><td>${opsNum(manager.concrete.quantity)} م³</td><td>${opsMoney(manager.concrete.total)} ر.س</td></tr><tr><td><b>البلوك</b></td><td>${manager.block.invoices} فاتورة</td><td>${opsNum(manager.block.quantity)} قطعة</td><td>${opsMoney(manager.block.total)} ر.س</td></tr><tr><td><b>قطع الغيار المصروفة</b></td><td>${manager.spareOut.length} حركة</td><td>${manager.spareRows.length} صنف</td><td>${opsMoney(manager.spareValue)} ر.س</td></tr><tr><td><b>الخامات الواردة</b></td><td>${manager.rawIn.length} حركة</td><td>${manager.rawInRows.length} صنف</td><td>${opsMoney(manager.rawInValue)} ر.س</td></tr><tr><td><b>الإنتاج</b></td><td>${manager.production.length} سجل</td><td>${opsNum(manager.productionQty)} — هالك ${opsNum(manager.waste)}</td><td>توقف ${opsNum(manager.productionDowntime)} دقيقة</td></tr><tr><td><b>الصيانة والوقود</b></td><td>${manager.maintenance.length} أمر / ${manager.trips.length} رحلة</td><td>${opsNum(manager.maintenanceDowntime)} ساعة / ${opsNum(manager.fuelLiters)} لتر</td><td>${opsMoney(manager.maintenanceCost+manager.fuelCost)} ر.س</td></tr></tbody></table></div></div>
06653:   <div class="ops-card ops-col-12"><h3>إقرار المبيعات حسب الموظف</h3>${salesRows.length?`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>الموظف</th><th>الفواتير</th><th>خرسانة م³</th><th>بلوك</th><th>نقدي</th><th>تحويل</th><th>آجل</th><th>الإجمالي</th></tr></thead><tbody>${salesRows.map(r=>`<tr><td><b>${opsEsc(r.employee)}</b></td><td>${r.invoices}</td><td>${opsNum(r.concrete)}</td><td>${opsNum(r.blocks)}</td><td>${opsMoney(r.cash)}</td><td>${opsMoney(r.transfer)}</td><td>${opsMoney(r.credit)}</td><td><b>${opsMoney(r.total)}</b></td></tr>`).join('')}</tbody></table></div>`:opsEmpty('لا توجد مبيعات في الفترة','')}</div>
06654:   <div class="ops-card ops-col-12"><h3>ملخص حركة المخازن</h3>${warehouseRows.length?`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>المخزن</th><th>الصنف</th><th>الوحدة</th><th>وارد</th><th>منصرف</th><th>تسوية</th><th>الصافي</th></tr></thead><tbody>${warehouseRows.map(r=>`<tr><td>${opsEsc(r.location)}</td><td><b>${opsEsc(r.item)}</b></td><td>${opsEsc(r.unit)}</td><td>${opsNum(r.incoming)}</td><td>${opsNum(r.outgoing)}</td><td>${opsNum(r.adjustment)}</td><td><b>${opsNum(r.incoming-r.outgoing+r.adjustment)}</b></td></tr>`).join('')}</tbody></table></div>`:opsEmpty('لا توجد حركة مخزون في الفترة','')}</div>
06655:   <div class="ops-card ops-col-12"><h3>الحركة حسب النوع</h3><div class="ops-bars">${Object.entries(M.reduce((o,x)=>{const k=OPS_MOVEMENT_TYPES[x.type]||x.type;o[k]=(o[k]||0)+1;return o;},{})).sort((a,b)=>b[1]-a[1]).map(([k,v])=>opsHealthBar(k,M.length?v/M.length:0)).join('')||opsEmpty('لا توجد حركات في الفترة','')}</div></div>
06656:   <div class="ops-card ops-col-12"><h3>ملفات الحركة المستوردة</h3>${imports.length?`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>التاريخ</th><th>الموظفون</th><th>الصفوف</th><th>الإقرارات</th></tr></thead><tbody>${imports.map(x=>`<tr><td>${opsFmtDate(x.reportDate)}<div class="muted">${opsEsc(x.fileName)}</div></td><td><b>خرسانة:</b> ${opsEsc(opsEmp(x.concreteEmployeeId)?.name||'—')}<br><b>بلوك:</b> ${opsEsc(opsEmp(x.blockEmployeeId)?.name||'—')}</td><td>${x.salesCount||0} مبيعات<br>${x.stockCount||0} مخزون</td><td><button class="ops-btn sm gold" onclick="opsPrintDailySalesDeclaration('concrete','${x.id}')">إقرار الخرسانة</button> <button class="ops-btn sm gold" onclick="opsPrintDailySalesDeclaration('block','${x.id}')">إقرار البلوك</button></td></tr>`).join('')}</tbody></table></div>`:opsEmpty('لم يتم استيراد ملفات في الفترة','')}</div></div>`;
06657: }
06658: function opsSheetAdd(wb,rows,name,widths){if(!rows.length)return;const ws=XLSX.utils.json_to_sheet(rows);ws['!cols']=Object.keys(rows[0]).map((x,i)=>({wch:widths?.[i]||Math.max(13,x.length+3)}));XLSX.utils.book_append_sheet(wb,ws,name);}
06659: function opsExportMovements(){const wb=XLSX.utils.book_new();opsSheetAdd(wb,OPS.movements.map(m=>({'رقم الحركة':m.no,'النوع':OPS_MOVEMENT_TYPES[m.type]||m.type,'التاريخ':m.date,'الموظف':opsEmp(m.employeeId)?.name||'','المصدر':opsLoc(m.sourceId)?.name||'','الوجهة':opsLoc(m.destinationId)?.name||'','البيان':opsEntityLabel(m.entityKind,m.entityId),'الكمية':m.quantity,'الوحدة':m.unit,'القيمة':m.amount,'المرجع':m.reference,'الملاحظات':m.notes,'الحالة':OPS_STATUS_LABELS[m.status]||m.status})),'الحركات',[18,24,18,25,18,18,28,12,10,14,18,36,12]);XLSX.writeFile(wb,`حركات-مصنع-بن-حامد-${opsToday()}.xlsx`);}
===== LINES 6671-6707 =====
06671:   opsSheetAdd(wb,OPS.stockMoves.map(s=>({'الرقم':s.no,'التاريخ':s.date,'المخزن':opsLoc(s.locationId)?.name||'','كود الصنف':opsItem(s.itemId)?.code||'','الصنف':opsItem(s.itemId)?.name||'','الحركة':s.direction==='in'?'وارد':'منصرف','الكمية':s.quantity,'الوحدة':opsItem(s.itemId)?.unit||'','تكلفة الوحدة':s.unitCost,'القيمة':s.totalCost,'المرجع':s.reference,'الطرف':s.party,'الملاحظات':s.notes})),'حركات المخزون');
06672:   opsSheetAdd(wb,OPS.counts.map(c=>({'الرقم':c.no,'التاريخ':c.date,'المخزن':opsLoc(c.locationId)?.name||'','الصنف':opsItem(c.itemId)?.name||'','الرصيد الدفتري':c.book,'الرصيد الفعلي':c.actual,'الفرق':c.difference,'منفذ الجرد':opsEmp(c.employeeId)?.name||'','الملاحظات':c.notes})),'الجرد والتسويات');
06673:   opsSheetAdd(wb,OPS.production.map(p=>({'الرقم':p.no,'التاريخ':p.date,'الوردية':p.shift,'المنتج':p.product,'المواصفة':p.spec,'المستهدف':p.target,'الفعلي':p.quantity,'الوحدة':p.unit,'الهالك':p.waste,'دقائق التوقف':p.downtimeMinutes,'سبب التوقف':p.downtimeReason,'المشرف':opsEmp(p.supervisorId)?.name||''})),'الإنتاج');
06674:   opsSheetAdd(wb,OPS.deliveries.map(d=>({'الرقم':d.no,'التاريخ':d.date,'الفاتورة':d.clientOrder,'العميل':opsCli(d.clientId)?.name||'','الموظف / المندوب':opsEmp(d.employeeId||opsCli(d.clientId)?.rep)?.name||'غير مسند','المنتج':d.product,'المواصفة':d.spec,'الكمية':d.quantity,'الوحدة':d.unit,'نقدي':d.cash||0,'تحويل':d.transfer||0,'آجل':d.credit||0,'القيمة':d.amount,'المركبة':opsVeh(d.vehicleId)?.plate||'','المكان':d.destination,'الحالة':OPS_STATUS_LABELS[d.status]||d.status})),'التوريدات والمبيعات');
06675:   opsSheetAdd(wb,opsSalesDeclarationRows('',opsToday()).map(r=>({'الموظف':r.employee,'عدد الفواتير':r.invoices,'خرسانة م³':r.concrete,'بلوك قطعة':r.blocks,'نقدي':r.cash,'تحويل':r.transfer,'آجل':r.credit,'الإجمالي':r.total})),'إقرار المبيعات');
06676:   opsSheetAdd(wb,OPS.collections.map(c=>({'الرقم':c.no,'التاريخ':c.date,'العميل':opsCli(c.clientId)?.name||'','المبلغ':c.amount,'الطريقة':c.method,'السند':c.receipt,'المندوب':opsEmp(c.employeeId)?.name||''})),'التحصيلات');
06677:   opsSheetAdd(wb,OPS.incidents.map(i=>({'الرقم':i.no,'التاريخ':i.date,'النوع':i.type,'الموقع':opsLoc(i.locationId)?.name||'','الموظف':opsEmp(i.employeeId)?.name||'','الوصف':i.description,'الخطورة':i.severity,'الإصابة':i.injury,'التكلفة':i.cost,'الإجراء':i.action||'','الحالة':OPS_STATUS_LABELS[i.status]||i.status})),'الحوادث');
06678:   opsSheetAdd(wb,OPS.documents.map(d=>({'الرقم':d.no,'الوثيقة':d.title,'النوع':d.type,'مرتبطة بـ':opsEntityLabel(d.entityKind,d.entityId),'رقم الوثيقة':d.documentNo,'المصدر':d.issuer,'تاريخ الإصدار':d.issueDate,'تاريخ الانتهاء':d.expiryDate,'الحالة':OPS_STATUS_LABELS[opsDocStatus(d)]||opsDocStatus(d)})),'الوثائق');
06679:   opsSheetAdd(wb,OPS.visits.map(v=>({'الرقم':v.no,'التاريخ':v.date,'الجهة / المستشفى':v.place,'الموظف':opsEmp(v.employeeId)?.name||''})),'الزيارات');
06680:   opsSheetAdd(wb,OPS.imports.map(i=>({'تاريخ التقرير':i.reportDate,'اسم الملف':i.fileName,'المخزن':opsLoc(i.warehouseId)?.name||'','موظف الخرسانة':opsEmp(i.concreteEmployeeId)?.name||'','فواتير الخرسانة':i.concreteSalesCount||0,'موظف البلوك':opsEmp(i.blockEmployeeId)?.name||'','فواتير البلوك':i.blockSalesCount||0,'صفوف المبيعات':i.salesCount,'قيمة المبيعات':i.salesValue,'صفوف المخزون':i.stockCount,'وقت الاستيراد':i.importedAt})),'ملفات الحركة المستوردة');
06681:   XLSX.writeFile(wb,`نظام-رقابة-مصنع-بن-حامد-${opsToday()}.xlsx`);opsToast('تم تصدير التقرير الشامل');
06682: }
06683: function opsPrintReport(title,fields,rows,qrPayload,signatures,options={}){
06684:   opsCloseModal();
06685:   const qr=qrPayload&&typeof qrSVG==='function'?qrSVG(`BHFC|${qrPayload}`,110):'';
06686:   const table=rows?.length?`<table><thead><tr>${Object.keys(rows[0]).map(k=>`<th>${opsEsc(k)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${Object.values(r).map(v=>`<td>${opsEsc(v)}</td>`).join('')}</tr>`).join('')}</tbody></table>`:'';
06687:   const sigs=signatures?.length?signatures:[{label:'إعداد'},{label:'مراجعة'},{label:'اعتماد'},{label:'ختم المنشأة',type:'stamp'}],sigHtml=`<div style="margin-top:12mm;display:grid;grid-template-columns:repeat(${Math.min(sigs.length,4)},1fr);gap:5mm;break-inside:avoid;page-break-inside:avoid">${sigs.map(s=>`<div class="${s.type==='stamp'?'ops-stamp-space':''}" style="border:1px solid #9aa7ad;border-radius:2mm;padding:3mm;min-height:30mm;text-align:${s.type==='thumbprint'?'center':'right'}"><b style="display:block;color:#0b2233">${opsEsc(s.label||'التوقيع')}</b>${s.name?`<span style="display:block;margin-top:2mm">${opsEsc(s.name)}</span>`:''}${s.type==='thumbprint'?'<div style="width:18mm;height:23mm;border:1px solid #777;border-radius:50%;margin:3mm auto 0"></div>':s.type==='stamp'?'<div style="height:17mm;display:grid;place-items:center;color:#866a3b">مساحة الختم</div>':'<div style="height:10mm;border-bottom:1px solid #555;margin-top:2mm"></div><small>التاريخ: ____ / ____ / ______</small>'}</div>`).join('')}</div>`;
06688:   opsEl('sheet').innerHTML=`<div class="ops-print-report ${options.landscape?'ops-landscape':''}"><div style="display:grid;grid-template-columns:1fr 34mm;gap:8mm;align-items:start;border-bottom:2px solid #0b2233;padding-bottom:5mm;margin-bottom:5mm"><div><h1>${opsEsc(title)}</h1><b>${opsEsc(OPS.settings.companyName)}</b><div>تاريخ إصدار التقرير: ${opsFmtDate(opsToday())}</div></div><div style="text-align:center;border:1px solid #ccd5d9;border-radius:2mm;padding:2mm">${qr}<small style="display:block">رمز تحقق مستقل</small></div></div>${fields?.length?`<table style="margin-bottom:6mm"><tbody>${fields.map(x=>`<tr><th style="width:35mm">${opsEsc(x[0])}</th><td class="${x[0]==='نص الإقرار'?'ops-declaration':''}">${opsEsc(x[1])}</td></tr>`).join('')}</tbody></table>`:''}${options.beforeTable||''}${table}${sigHtml}</div>`;
06689:   setTimeout(()=>window.print(),120);
06690: }
06691: function opsPrintExecutive(){const from=opsEl('repFrom')?.value||'',to=opsEl('repTo')?.value||opsToday(),M=opsRange(OPS.movements,'date',from,to);const counts=M.reduce((o,x)=>{const k=OPS_MOVEMENT_TYPES[x.type]||x.type;o[k]=(o[k]||0)+1;return o;},{});opsPrintReport('التقرير التنفيذي لحركة المصنع',[['الفترة',`${opsFmtDate(from)} إلى ${opsFmtDate(to)}`],['إجمالي الحركات',M.length],['العهد النشطة',OPS.custodies.filter(x=>x.status==='active').length],['المركبات بالخارج',OPS.trips.filter(x=>x.status==='out').length],['أوامر الصيانة المفتوحة',OPS.maintenance.filter(x=>x.status==='open').length],['التنبيهات',opsAlerts().length]],Object.entries(counts).map(([k,v])=>({'نوع الحركة':k,'العدد':v})),'report:'+from+':'+to);}
06692: function opsPrintManagerReport(){
06693:   const from=opsEl('repFrom')?.value||opsMonthStart(),to=opsEl('repTo')?.value||opsToday(),r=opsManagerReportData(from,to),ref=`MGR-${String(from||'ALL').replace(/-/g,'')}-${String(to).replace(/-/g,'')}`;
06694:   opsPrintReport('تقرير المدير الشامل لحركة المصنع',[['رقم التقرير',ref],['الفترة',`${opsFmtDate(from)} إلى ${opsFmtDate(to)}`],['إجمالي المبيعات',`${opsMoney(r.salesTotal)} ر.س`],['إجمالي التحصيلات',`${opsMoney(r.collectionTotal)} ر.س`],['فرق مبيعات وتحصيلات الفترة',`${opsMoney(r.periodBalance)} ر.س`],['قيمة المخزون الحالية',`${opsMoney(opsStockValue())} ر.س`],['ملاحظة','يعرض التقرير الحركات المسجلة داخل الفترة، ويستبعد المرتجعات وتسويات مطابقة الأرصدة من الاستهلاك الفعلي.']],opsManagerReportRows(r),`manager:${from}:${to}`,[{label:'إعداد التقرير'},{label:'مراجعة الحسابات'},{label:'اعتماد المدير العام'},{label:'ختم المنشأة',type:'stamp'}]);
06695: }
06696: function opsPrintSalesDeclaration(){const from=opsEl('repFrom')?.value||opsMonthStart(),to=opsEl('repTo')?.value||opsToday(),rows=opsSalesDeclarationRows(from,to),total=rows.reduce((s,x)=>s+x.total,0);opsPrintReport('إقرار المبيعات حسب الموظف',[['الفترة',`${opsFmtDate(from)} إلى ${opsFmtDate(to)}`],['عدد الموظفين / المندوبين',rows.length],['إجمالي المبيعات',opsMoney(total)+' ر.س']],rows.map(r=>({'الموظف':r.employee,'الفواتير':r.invoices,'خرسانة م³':opsNum(r.concrete),'بلوك':opsNum(r.blocks),'نقدي':opsMoney(r.cash),'تحويل':opsMoney(r.transfer),'آجل':opsMoney(r.credit),'الإجمالي':opsMoney(r.total)})),'sales:'+from+':'+to);}
06697: function opsPrintDailySalesDeclaration(kind,batchId){
06698:   const batch=batchId?opsById(OPS.imports,batchId):null,date=batch?.reportDate||opsEl('repTo')?.value||opsToday(),lines=opsDailySalesLines(kind,date,batchId),employeeIds=[...new Set(lines.map(x=>x.employeeId).filter(Boolean))],employeeId=(kind==='concrete'?batch?.concreteEmployeeId:batch?.blockEmployeeId)||(employeeIds.length===1?employeeIds[0]:opsSalesResponsible(kind)?.id)||'';
06699:   if(!lines.length){opsToast(`لا توجد مبيعات ${kind==='concrete'?'خرسانة':'بلوك'} في ${opsFmtDate(date)}`,'err');return;}
06700:   const e=opsEmp(employeeId)||{},employee=e.name||'غير مسند',label=kind==='concrete'?'الخرسانة':'البلوك',unit=kind==='concrete'?'م³':'قطعة',t=opsDailySalesTotals(lines),ref=`SAL-${kind==='concrete'?'CON':'BLK'}-${date.replace(/-/g,'')}-${batchId?String(batchId).slice(-6):'DAY'}`,rows=lines.map((d,i)=>({'م':i+1,'الفاتورة':d.clientOrder||d.no,'العميل':opsCli(d.clientId)?.name||'—','الكمية':opsNum(d.quantity),'نقدي':opsMoney(d.cash||0),'تحويل':opsMoney(d.transfer||0),'آجل':opsMoney(d.credit||0),'الإجمالي':opsMoney(d.amount||0)}));
06701:   opsPrintReport(`إقرار مبيعات ${label} اليومي`,[['رقم الإقرار',ref],['تاريخ المبيعات',opsFmtDate(date)],['الموظف المسؤول',employee],['المسمى الوظيفي',e.role||opsSalesResponsibleRole(kind)],['الرقم الوظيفي',e.no||'—'],['الهوية / الإقامة',e.nid||'—'],['عدد الفواتير',lines.length],['إجمالي الكمية',`${opsNum(t.quantity)} ${unit}`],['إجمالي النقدي',`${opsMoney(t.cash)} ر.س`],['إجمالي التحويل',`${opsMoney(t.transfer)} ر.س`],['إجمالي الآجل',`${opsMoney(t.credit)} ر.س`],['إجمالي المبيعات',`${opsMoney(t.total)} ر.س`],['نص الإقرار',`أقر أنا ${employee} بأنني راجعت جميع فواتير ومبيعات ${label} الموضحة في هذا الإقرار عن يوم ${opsFmtDate(date)}، وأن الكميات والقيم وطرق السداد صحيحة ومطابقة للمستندات المسلمة للحسابات، وأتحمل مسؤولية أي نقص أو اختلاف يظهر بالمراجعة.`]],rows,`daily-sales:${kind}:${date}:${batchId||employeeId}`,[{label:'اسم وتوقيع الموظف',name:employee},{label:'بصمة الموظف',type:'thumbprint'},{label:'مراجعة المحاسب'},{label:'اعتماد الإدارة والختم',type:'stamp'}]);
06702: }
06703: function opsOpenImportDeclarations(batchId){const b=opsById(OPS.imports,batchId);if(!b)return;opsOpenModal('تم تجهيز الإقرارات اليومية',`<div class="ops-note"><b>تم توزيع المبيعات حسب النوع والموظف.</b> اطبع الإقرارين الآن أو من سجل ملفات الحركة في التقارير.</div><div class="ops-grid" style="margin-top:12px"><div class="ops-card ops-col-6"><h3>إقرار الخرسانة</h3><p>${opsEsc(opsEmp(b.concreteEmployeeId)?.name||'—')} — ${b.concreteSalesCount||0} فاتورة</p><button type="button" class="ops-btn gold" onclick="opsPrintDailySalesDeclaration('concrete','${b.id}')">طباعة إقرار الخرسانة</button></div><div class="ops-card ops-col-6"><h3>إقرار البلوك</h3><p>${opsEsc(opsEmp(b.blockEmployeeId)?.name||'—')} — ${b.blockSalesCount||0} فاتورة</p><button type="button" class="ops-btn gold" onclick="opsPrintDailySalesDeclaration('block','${b.id}')">طباعة إقرار البلوك</button></div></div>`,()=>false,'إغلاق');opsEl('opsModalSave').onclick=opsCloseModal;}
06704: function opsPrintWarehouseDeclaration(){const from=opsEl('repFrom')?.value||opsMonthStart(),to=opsEl('repTo')?.value||opsToday(),rows=opsWarehouseDeclarationRows(from,to);opsPrintReport('إقرار حركة المخازن',[['الفترة',`${opsFmtDate(from)} إلى ${opsFmtDate(to)}`],['عدد الأصناف المتحركة',rows.length]],rows.map(r=>({'المخزن':r.location,'الصنف':r.item,'الوحدة':r.unit,'وارد':opsNum(r.incoming),'منصرف':opsNum(r.outgoing),'تسوية':opsNum(r.adjustment),'الصافي':opsNum(r.incoming-r.outgoing+r.adjustment)})),'warehouse:'+from+':'+to);}
06705: 
06706: /* ============================================================
06707:    DAILY ART MOVEMENT REPORT IMPORT
===== LINES 6768-6792 =====
06768:   try{
06769:     const wb=XLSX.read(new Uint8Array(await file.arrayBuffer()),{type:'array',cellDates:false}),plan=opsParseMovementWorkbook(wb,file.name);
06770:     const total=plan.sales.length+plan.stock.length;
06771:     const salesValue=plan.sales.reduce((s,x)=>s+Number(x.amount||0),0),concrete=plan.sales.filter(x=>/خرسان/.test(opsNormArabic(x.item))),blocks=plan.sales.filter(x=>/بل[وؤ]ك/.test(opsNormArabic(x.item))),stockIn=plan.stock.filter(x=>x.direction==='in'),stockOut=plan.stock.filter(x=>x.direction==='out'),defaultDate=plan.sales[0]?.date||plan.stock[0]?.date||opsToday(),concreteEmployee=opsSalesResponsible('concrete'),blockEmployee=opsSalesResponsible('block');
06772:     if(concrete.length&&!concreteEmployee)throw new Error('أكمل بيانات مسؤول مبيعات الخرسانة من الإعدادات أولًا');
06773:     if(blocks.length&&!blockEmployee)throw new Error('أكمل بيانات مسؤول مبيعات البلوك من الإعدادات أولًا');
06774:     opsOpenModal(`استيراد تقرير الحركة — ${total} صف`,`${total?'<div class="ops-note"><b>راجع الملخص وحدد تاريخ التقرير والمخزن قبل الاعتماد.</b> النظام يمنع تكرار الصف نفسه لنفس التاريخ.</div>':'<div class="ops-note warn">لا توجد صفوف قابلة للاستيراد.</div>'}
06775:       <div class="ops-form-grid"><div><label class="ops-required">تاريخ التقرير</label><input type="date" name="reportDate" value="${defaultDate}" required></div><div><label class="ops-required">المخزن الذي تمت منه الحركة</label><select name="warehouseId" required>${opsSelect(opsLocations(),'loc-store','— اختر المخزن —')}</select></div><div><label>مسؤول مبيعات الخرسانة المعتمد</label><div class="ops-note"><b>${opsEsc(concreteEmployee?.name||'لا توجد فواتير خرسانة')}</b><br>${opsEsc(concreteEmployee?.no?'رقم وظيفي: '+concreteEmployee.no:'يُعدل من الإعدادات')}</div></div><div><label>مسؤول مبيعات البلوك المعتمد</label><div class="ops-note"><b>${opsEsc(blockEmployee?.name||'لا توجد فواتير بلوك')}</b><br>${opsEsc(blockEmployee?.no?'رقم وظيفي: '+blockEmployee.no:'يُعدل من الإعدادات')}</div></div></div>
06776:       <div class="ops-kpis" style="margin:12px 0">${opsKpi(plan.sales.length,'فواتير مبيعات','blue')}${opsKpi(opsMoney(salesValue),'قيمة المبيعات','purple')}${opsKpi(opsNum(concrete.reduce((s,x)=>s+x.quantity,0)),'م³ خرسانة')}${opsKpi(opsNum(blocks.reduce((s,x)=>s+x.quantity,0)),'قطعة بلوك')}${opsKpi(stockIn.length,'أصناف وارد','blue')}${opsKpi(stockOut.length,'أصناف منصرف','warn')}</div>
06777:       ${plan.sales.length?`<h3>مبيعات — ${plan.sales.length}</h3><div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>التاريخ</th><th>الفاتورة</th><th>العميل</th><th>الصنف</th><th>الكمية</th><th>القيمة</th></tr></thead><tbody>${plan.sales.slice(0,80).map(x=>`<tr><td>${opsFmtDate(x.date)}</td><td>${opsEsc(x.invoice)}</td><td>${opsEsc(x.customer)}</td><td>${opsEsc(x.item)}</td><td>${opsNum(x.quantity)}</td><td>${opsMoney(x.amount)}</td></tr>`).join('')}</tbody></table></div>`:''}
06778:       ${plan.stock.length?`<h3 style="margin-top:12px">حركة مخزون — ${plan.stock.length}</h3><div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>التاريخ</th><th>القسم</th><th>الصنف</th><th>الحركة</th><th>الكمية</th><th>المخزن</th></tr></thead><tbody>${plan.stock.slice(0,80).map(x=>`<tr><td>${opsFmtDate(x.date)}</td><td>${opsEsc(x.section)}</td><td>${opsEsc(x.item)}</td><td>${x.direction==='in'?'وارد':'منصرف'}</td><td>${opsNum(x.quantity)}</td><td>${opsEsc(x.warehouse)}</td></tr>`).join('')}</tbody></table></div>`:''}
06779:       ${plan.warnings.length?`<div class="ops-note warn" style="margin-top:12px">${plan.warnings.map(opsEsc).join('<br>')}</div>`:''}`,async()=>{
06780:         if(!total)return false;
06781:         const reportDate=document.querySelector('#opsForm [name="reportDate"]').value,warehouseId=document.querySelector('#opsForm [name="warehouseId"]').value,concreteEmployeeId=concreteEmployee?.id||'',blockEmployeeId=blockEmployee?.id||'',seen=new Set(OPS.imports.flatMap(x=>x.rowKeys||[]));
06782:         const salesRows=plan.sales.map(x=>({...x,date:reportDate,key:opsImportRowKey(x,reportDate,warehouseId)})).filter(x=>!seen.has(x.key)),stockRows=plan.stock.map(x=>({...x,date:reportDate,key:opsImportRowKey(x,reportDate,warehouseId)})).filter(x=>!seen.has(x.key));
06783:         if(!salesRows.length&&!stockRows.length)throw new Error('هذا التقرير مستورد سابقًا لنفس التاريخ والمخزن');
06784:         const batch={id:opsUid('imp'),fileName:file.name,reportDate,warehouseId,concreteEmployeeId,blockEmployeeId,concreteSalesCount:salesRows.filter(x=>/خرسان/.test(opsNormArabic(x.item))).length,blockSalesCount:salesRows.filter(x=>/بل[وؤ]ك/.test(opsNormArabic(x.item))).length,importedAt:new Date().toISOString(),salesCount:salesRows.length,stockCount:stockRows.length,salesValue:salesRows.reduce((s,x)=>s+Number(x.amount||0),0),rowKeys:[]};
06785:         salesRows.forEach(x=>{const cli=opsFindOrCreateClient(x.customer,x.customerCode),amount=Number(x.amount||0),product=String(x.item),isConcrete=/خرسان/.test(opsNormArabic(product)),employeeId=isConcrete?concreteEmployeeId:blockEmployeeId,d={id:opsUid('dlv'),no:opsNextNo('DLV'),clientId:cli.id,customerCode:x.customerCode||cli.code||'',date:x.date,product,spec:'',quantity:x.quantity,unit:isConcrete?'م³':'قطعة',amount,cash:0,transfer:0,credit:amount,clientOrder:x.invoice,vehicleId:'',employeeId,destination:cli.addr||'',notes:`مستورد من ${file.name} — قيمة المبيعات كاملة مديونية حتى تسجيل سند سداد مستقل`,sourceDeclaredCash:x.declaredCash||0,sourceDeclaredTransfer:x.declaredTransfer||0,sourceDeclaredCredit:x.declaredCredit||0,sourceRowKey:x.key,accountingRule:'FULL_DEBT_UNTIL_COLLECTION',status:'delivered',sourceImportId:batch.id,createdAt:new Date().toISOString()};OPS.deliveries.unshift(d);opsAddMovementRecord('DELIVERY_DONE',{date:d.date+'T12:00',employeeId:d.employeeId,entityKind:'client',entityId:d.clientId,quantity:d.quantity,unit:d.unit,amount:d.amount,reference:d.clientOrder||d.no,notes:d.product+' — بيع آجل لحين تسجيل التحصيل',module:'daily-import',moduleId:d.id});batch.rowKeys.push(x.key);});
06786:         const reconciles=new Map();
06787:         stockRows.forEach(x=>{const n=opsNormArabic(x.item),cat=/اسمنت|رمل|بحص|بودر|خام/.test(n)?'مواد خام':/فلتر|زيت|قطع/.test(n)?'قطع غيار':/بل[وؤ]ك|خرسان/.test(n)?'منتج تام':'أخرى',item=opsFindOrCreateItem(x.item,cat,{code:x.code,unit:x.unit});let loc=x.warehouse?OPS.locations.find(l=>opsNormArabic(l.name)===opsNormArabic(x.warehouse)):opsLoc(warehouseId);if(!loc&&x.warehouse){loc={id:opsUid('loc'),name:x.warehouse,type:'مخزن',active:true};OPS.locations.push(loc);}if(!loc)loc=opsLoc('loc-store')||OPS.locations[0];const unitCost=x.value&&x.quantity?x.value/x.quantity:Number(item.unitCost||0),s={id:opsUid('stm'),no:opsNextNo('STK'),direction:x.direction,itemId:item.id,locationId:loc.id,date:x.date,quantity:x.quantity,unitCost,totalCost:x.value,reference:x.reference||'',party:x.party,employeeId:'',notes:`${x.section} — مستورد من ${file.name}`,status:'confirmed',sourceImportId:batch.id,createdAt:new Date().toISOString()};OPS.stockMoves.unshift(s);opsAddMovementRecord(x.direction==='in'?'STOCK_IN':'STOCK_OUT',{date:s.date+'T12:00',sourceId:x.direction==='out'?loc.id:'',destinationId:x.direction==='in'?loc.id:'',entityKind:'item',entityId:item.id,quantity:s.quantity,unit:item.unit,amount:s.totalCost,reference:s.reference||s.no,notes:s.notes,module:'daily-import',moduleId:s.id});if(x.closing!==null)reconciles.set(item.id+'|'+loc.id,{item,loc,actual:Number(x.closing),opening:x.opening});batch.rowKeys.push(x.key);});
06788:         reconciles.forEach(({item,loc,actual,opening})=>{const book=opsStock(item.id,loc.id),diff=actual-book,c={id:opsUid('cnt'),no:opsNextNo('CNT'),date:reportDate,locationId:loc.id,itemId:item.id,book,actual,difference:diff,employeeId:'',notes:`مطابقة تلقائية مع رصيد تقرير الحركة${opening!==null?' — افتتاحي '+opening:''}`,sourceImportId:batch.id,createdAt:new Date().toISOString()};OPS.counts.unshift(c);if(diff!==0){const a={id:opsUid('stm'),no:opsNextNo('STK'),direction:diff>0?'in':'out',itemId:item.id,locationId:loc.id,date:reportDate,quantity:Math.abs(diff),unitCost:Number(item.unitCost||0),totalCost:Math.abs(diff)*Number(item.unitCost||0),reference:c.no,party:'تسوية تقرير الحركة',employeeId:'',notes:c.notes,status:'confirmed',sourceImportId:batch.id,createdAt:new Date().toISOString()};OPS.stockMoves.unshift(a);opsAddMovementRecord('STOCK_ADJUST',{date:reportDate+'T12:00',sourceId:diff<0?loc.id:'',destinationId:diff>0?loc.id:'',entityKind:'item',entityId:item.id,quantity:diff,unit:item.unit,amount:diff*Number(item.unitCost||0),reference:c.no,notes:`مطابقة الرصيد الدفتري ${book} مع رصيد التقرير ${actual}`,module:'daily-import',moduleId:a.id});}});
06789:         OPS.imports.unshift(batch);save();rAll();await opsPersist('استيراد تقرير الحركة '+file.name);opsToast(`تم الاستيراد وتجهيز إقراري الخرسانة والبلوك`);setTimeout(()=>opsOpenImportDeclarations(batch.id),250);
06790:       },total?'اعتماد الاستيراد':'إغلاق');
06791:       if(!total){opsEl('opsModalSave').onclick=opsCloseModal;opsEl('opsModalSave').textContent='إغلاق';}
06792:   }catch(e){console.error(e);opsToast('تعذر قراءة تقرير الحركة: '+(e.message||''),'err');}
===== LINES 6827-6834 =====
06827: async function opsEncryptText(text,password){const enc=new TextEncoder(),salt=crypto.getRandomValues(new Uint8Array(16)),iv=crypto.getRandomValues(new Uint8Array(12)),base=await crypto.subtle.importKey('raw',enc.encode(password),'PBKDF2',false,['deriveKey']),key=await crypto.subtle.deriveKey({name:'PBKDF2',salt,iterations:180000,hash:'SHA-256'},base,{name:'AES-GCM',length:256},false,['encrypt']),data=await crypto.subtle.encrypt({name:'AES-GCM',iv},key,enc.encode(text));return JSON.stringify({format:'BINHAMID_ENCRYPTED_BACKUP',version:1,salt:opsBytesToBase64(salt),iv:opsBytesToBase64(iv),data:opsBytesToBase64(data)});}
06828: async function opsDecryptText(wrapper,password){const enc=new TextEncoder(),base=await crypto.subtle.importKey('raw',enc.encode(password),'PBKDF2',false,['deriveKey']),key=await crypto.subtle.deriveKey({name:'PBKDF2',salt:opsBase64ToBytes(wrapper.salt),iterations:180000,hash:'SHA-256'},base,{name:'AES-GCM',length:256},false,['decrypt']),data=await crypto.subtle.decrypt({name:'AES-GCM',iv:opsBase64ToBytes(wrapper.iv)},key,opsBase64ToBytes(wrapper.data));return new TextDecoder().decode(data);}
06829: async function opsExportBackup(encrypted){const password=opsEl('opsBackupPassword')?.value||'';if(encrypted&&password.length<6){opsToast('اكتب كلمة نسخ احتياطي من 6 أحرف على الأقل','err');return;}opsSetSave('تجهيز النسخة');try{const data=await opsBuildBackup(),plain=JSON.stringify(data),text=encrypted?await opsEncryptText(plain,password):plain;opsDownload(new Blob([text],{type:'application/json'}),`نسخة-مصنع-بن-حامد-${opsToday()}${encrypted?'-مشفرة':''}.json`);await opsSnapshot('نسخة يدوية قبل التصدير');opsSetSave('تم تنزيل النسخة','ok');opsToast('تم إنشاء النسخة الاحتياطية');}catch(e){console.error(e);opsSetSave('فشل النسخ','err');opsToast('تعذر إنشاء النسخة','err');}}
06830: async function opsRestoreBackupFile(file){if(!file)return;const password=opsEl('opsRestorePassword')?.value||'';let text=await file.text(),obj=JSON.parse(text);if(obj.format==='BINHAMID_ENCRYPTED_BACKUP'){if(!password)throw new Error('اكتب كلمة مرور النسخة المشفرة');text=await opsDecryptText(obj,password);obj=JSON.parse(text);}if(obj.format!=='BINHAMID_FACTORY_BACKUP'||!obj.ops)throw new Error('الملف ليس نسخة صالحة للنظام');await opsSnapshot('قبل استعادة نسخة خارجية');if(obj.legacy){D.cfg={...D.cfg,...(obj.legacy.cfg||{})};D.txt={...DEF,...(obj.legacy.txt||{})};D.veh=obj.legacy.veh||[];D.emp=obj.legacy.emp||[];D.cli=obj.legacy.cli||[];D.seq=obj.legacy.seq||1;save();fillCfg();fillTxt();rAll();}OPS=opsMergeState(obj.ops);if(OPS_DB){const old=await opsDbAll('files');for(const f of old)await opsDbDelete('files',f.id);for(const f of obj.files||[]){await opsDbPut('files',{id:f.id,name:f.name,type:f.type,size:f.size,createdAt:f.createdAt,blob:new Blob([opsBase64ToBytes(f.data)],{type:f.type})});}}await opsPersist('استعادة نسخة احتياطية');opsRender();opsToast('تمت استعادة النسخة بنجاح');}
06831: async function opsRestoreSnapshot(id){const s=await opsDbGet('snapshots',id);if(!s)return;const obj=JSON.parse(s.data);if(obj.legacy){D.cfg={...D.cfg,...(obj.legacy.cfg||{})};D.txt={...DEF,...(obj.legacy.txt||{})};D.veh=obj.legacy.veh||[];D.emp=obj.legacy.emp||[];D.cli=obj.legacy.cli||[];D.seq=obj.legacy.seq||1;save();fillCfg();fillTxt();rAll();}OPS=opsMergeState(obj.ops);await opsPersist('استعادة نقطة محفوظة');opsRender();opsToast('تمت استعادة النقطة المحفوظة');}
06832: function opsInjectFuelControlSettings(){
06833:   const grid=opsEl('opsContent')?.querySelector('.ops-grid');if(!grid)return;
06834:   const card=document.createElement('div');card.className='ops-card ops-col-12';card.innerHTML=`<h3>إعدادات رقابة الديزل</h3><div class="ops-note"><b>الاستبعاد داخلي فقط.</b> لا يظهر اسم المركبة أو الموظف في المعاينة أو الطباعة أو التصدير.</div><div class="ops-form-grid"><div><label>المركبة / الماركة المستبعدة</label><input id="opsFuelExcludedVehicle" value="${opsEsc(OPS.settings.fuelExcludedVehicle||'')}"></div><div><label>الموظف المستبعد</label><input id="opsFuelExcludedDriver" value="${opsEsc(OPS.settings.fuelExcludedDriver||'')}"></div><div><label>اختلاف سعر اللتر المسموح %</label><input type="number" min="0" step="1" id="opsFuelPriceVariance" value="${Number(OPS.settings.fuelPriceVariancePct||10)}"></div><div><label>زيادة كمية التعبئة المسموحة %</label><input type="number" min="0" step="1" id="opsFuelVolumeVariance" value="${Number(OPS.settings.fuelVolumeVariancePct||50)}"></div><div><label>حد التعبئة المتقاربة بالساعات</label><input type="number" min="1" step="1" id="opsFuelRapidHours" value="${Number(OPS.settings.fuelRapidRefillHours||24)}"></div></div><button class="ops-btn primary" style="margin-top:10px" onclick="opsSaveSettings()">حفظ إعدادات الرقابة</button>`;grid.appendChild(card);
===== LINES 6839-6845 =====
06839:   opsEl('opsContent').innerHTML=opsHead('النسخ الاحتياطي والإعدادات','نسخة كاملة تشمل البيانات القديمة والحركات والمرفقات',`<button class="ops-btn primary" onclick="opsSnapshot('نسخة يدوية').then(()=>opsRenderBackup())">إنشاء نقطة حفظ</button>`)+`
06840:   <div class="ops-grid"><div class="ops-card ops-col-6"><h3>تصدير نسخة كاملة</h3><div class="ops-note">النسخة تشمل ${fileCount} مرفقًا، وسجل الحركة، والموظفين والمركبات والعملاء والنماذج القديمة.</div><div class="ops-form-grid"><div class="full"><label>كلمة تشفير النسخة</label><input type="password" id="opsBackupPassword" placeholder="6 أحرف على الأقل"></div></div><div class="ops-actions" style="margin-top:10px"><button class="ops-btn primary" onclick="opsExportBackup(true)">تنزيل نسخة مشفرة</button><button class="ops-btn ghost" onclick="opsExportBackup(false)">نسخة غير مشفرة</button></div></div>
06841:   <div class="ops-card ops-col-6"><h3>استعادة نسخة</h3><div class="ops-note warn">تُنشأ نقطة حفظ تلقائية قبل الاستبدال.</div><div class="ops-form-grid"><div class="full"><label>ملف النسخة</label><input type="file" id="opsRestoreFile" accept=".json"></div><div class="full"><label>كلمة المرور إذا كانت مشفرة</label><input type="password" id="opsRestorePassword"></div></div><button class="ops-btn gold" style="margin-top:10px" onclick="opsRestoreBackupFile(opsEl('opsRestoreFile').files[0]).catch(e=>opsToast(e.message,'err'))">استعادة الملف</button></div>
06842:   <div class="ops-card ops-col-12"><h3>مسؤولو المبيعات المعتمدون</h3><div class="ops-note"><b>تُسجل البيانات مرة واحدة فقط.</b> كل تقرير حركة جديد يربط فواتير الخرسانة والبلوك بالمسؤول المحدد تلقائيًا، ويضع بياناته في الإقرار اليومي دون اختيار متكرر.</div><div class="ops-grid"><div class="ops-card ops-col-6"><h3>مسؤول مبيعات الخرسانة</h3><div class="ops-form-grid"><div class="full"><label class="ops-required">الاسم الرباعي</label><input id="opsConcreteName" value="${opsEsc(concrete.name||'')}"></div><div><label>الهوية / الإقامة</label><input id="opsConcreteNid" value="${opsEsc(concrete.nid||'')}" dir="ltr"></div><div><label>الرقم الوظيفي</label><input id="opsConcreteNo" value="${opsEsc(concrete.no||'')}" dir="ltr"></div><div class="full"><label>رقم الجوال</label><input id="opsConcreteTel" value="${opsEsc(concrete.tel||'')}" dir="ltr"></div></div></div><div class="ops-card ops-col-6"><h3>مسؤول مبيعات البلوك</h3><div class="ops-form-grid"><div class="full"><label class="ops-required">الاسم الرباعي</label><input id="opsBlockName" value="${opsEsc(block.name||'')}"></div><div><label>الهوية / الإقامة</label><input id="opsBlockNid" value="${opsEsc(block.nid||'')}" dir="ltr"></div><div><label>الرقم الوظيفي</label><input id="opsBlockNo" value="${opsEsc(block.no||'')}" dir="ltr"></div><div class="full"><label>رقم الجوال</label><input id="opsBlockTel" value="${opsEsc(block.tel||'')}" dir="ltr"></div></div></div></div><button class="ops-btn primary" style="margin-top:10px" onclick="opsSaveSalesEmployees()">حفظ مسؤولي المبيعات</button></div>
06843:   <div class="ops-card ops-col-7"><h3>نقاط الحفظ المحلية <span class="n">${snaps.length}</span></h3>${snaps.length?`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>التاريخ</th><th>الوصف</th><th></th></tr></thead><tbody>${snaps.map(s=>`<tr><td>${opsFmtDateTime(s.createdAt)}</td><td>${opsEsc(s.label)}</td><td><button class="ops-btn blue sm" onclick="opsRestoreSnapshot('${s.id}')">استعادة</button></td></tr>`).join('')}</tbody></table></div>`:opsEmpty('لا توجد نقاط حفظ','')}</div>
06844:   <div class="ops-card ops-col-5"><h3>إعدادات الرقابة</h3><div class="ops-form-grid"><div><label>التنبيه قبل الانتهاء</label><input type="number" id="opsAlertDays" value="${OPS.settings.alertDays}"></div><div><label>الحد الأدنى الافتراضي</label><input type="number" id="opsLowStock" value="${OPS.settings.lowStockDefault}"></div><div class="full"><label>اسم المنشأة</label><input id="opsCompanyName" value="${opsEsc(OPS.settings.companyName)}"></div></div><button class="ops-btn primary" style="margin-top:10px" onclick="opsSaveSettings()">حفظ الإعدادات</button></div></div>`;
06845:   opsInjectFuelControlSettings();
===== LINES 6904-6938 =====
06904: function bhAllOverdue(){return (D.cli||[]).reduce((s,c)=>s+bhClientAggregate(c).overdue,0);}
06905: 
06906: window.rStats=function(){
06907:   const drv=D.emp.filter(e=>e.role.includes('سائق')||e.role.includes('مشغّل')).length;
06908:   const rep=D.emp.filter(e=>e.role.includes('مندوب')||e.role.includes('مسؤول مبيعات')).length;
06909:   const bal=bhAllReceivables(),over=bhAllOverdue();
06910:   $('stats').innerHTML=`<div class="stat"><b>${D.veh.length}</b><span>مركبة ومعدة</span></div><div class="stat"><b>${D.emp.length}</b><span>موظف مسجّل</span></div><div class="stat"><b>${drv}</b><span>سائق / مشغّل</span></div><div class="stat"><b>${rep}</b><span>مسؤول مبيعات</span></div><div class="stat"><b>${D.cli.length}</b><span>عميل</span></div><div class="stat"><b>${money(bal)}</b><span>إجمالي الرصيد المستحق (ر.س)</span></div><div class="stat"><b>${money(over)}</b><span>منه متأخر عن السداد (ر.س)</span></div>`;
06911:   $('cVeh').textContent=D.veh.length;$('cEmp').textContent=D.emp.length;$('cCli').textContent=D.cli.length;
06912: };
06913: 
06914: window.rCli=function(){
06915:   const s=$('fSeg').value,r=$('fRep').value,t=$('tCli');let L=D.cli;
06916:   if(s)L=L.filter(c=>c.seg===s);if(r)L=L.filter(c=>c.rep===r);
06917:   if(!L.length){t.innerHTML=`<tr><td colspan="9"><div class="empty"><span class="big">🧾</span>لا يوجد عملاء — اضغط «إضافة عميل»</div></td></tr>`;return;}
06918:   t.innerHTML=L.map(c=>{const rp=D.emp.find(e=>e.id===c.rep),a=bhClientAggregate(c),due=a.nextDueDate?fmtG(a.nextDueDate):'—',late=a.maxDaysLate;return `<tr><td><b>${esc(c.name)}</b><div style="font-size:11px;color:var(--muted)">${esc(c.cr||'')}</div></td><td><span class="chip ${c.seg==='بلوك'?'blk':'con'}">${esc(c.seg)}</span></td><td>${bhClientQtyText(c)}</td><td class="mono">${money(a.sales)}</td><td class="mono">${money(a.paid)}</td><td class="mono ${a.remaining?'client-balance-positive':'client-balance-clear'}">${money(a.remaining)}</td><td>${due}${late?`<div style="color:var(--warn);font-size:11px">متأخر ${late} يوم</div>`:''}</td><td>${rp?esc(rp.name):'<span style="color:var(--warn)">غير مُسند</span>'}</td><td style="white-space:nowrap"><button class="btn btn-o btn-sm" onclick="cliForm('${c.id}')">تعديل</button><button class="btn btn-d btn-sm" onclick="del('cli','${c.id}')">×</button></td></tr>`;}).join('');
06919: };
06920: 
06921: window.cliForm=function(id){
06922:   const c=id?D.cli.find(x=>x.id===id):{seg:'خرسانة',days:D.cfg.days||3};
06923:   const reps=D.emp.filter(e=>e.role.includes('مندوب')||e.role.includes('مسؤول مبيعات'));
06924:   openMo(id?'تعديل عميل':'إضافة عميل',`<div class="grid g2"><div style="grid-column:1/-1"><label>اسم العميل / المنشأة *</label><input id="m_name" value="${esc(c.name||'')}"></div><div><label>القطاع *</label><select id="m_seg">${SEGS.map(s=>`<option ${c.seg===s?'selected':''}>${s}</option>`).join('')}</select></div><div><label>السجل التجاري / الهوية</label><input id="m_cr" value="${esc(c.cr||'')}" dir="ltr"></div><div><label>مسؤول التواصل</label><input id="m_ct" value="${esc(c.ct||'')}"></div><div><label>رقم الجوال</label><input id="m_tel" value="${esc(c.tel||'')}" dir="ltr"></div><div><label>مهلة السداد من تاريخ التوريد (يوم)</label><input type="number" min="0" id="m_days" value="${c.days??D.cfg.days??3}"></div><div style="grid-column:1/-1"><label>المندوب المسؤول</label><select id="m_rep"><option value="">— غير مُسند —</option>${reps.map(e=>`<option value="${e.id}" ${c.rep===e.id?'selected':''}>${esc(e.name)} — ${esc(e.role)}</option>`).join('')}</select></div><div style="grid-column:1/-1"><label>العنوان / الموقع</label><input id="m_addr" value="${esc(c.addr||'')}"></div><div style="grid-column:1/-1"><label>ملاحظات</label><textarea id="m_note" rows="2">${esc(c.note||'')}</textarea></div></div>`,()=>{if(!V('name')){toast('اكتب اسم العميل','err');return false;}const old=id?D.cli.find(x=>x.id===id):{};const o={...old,id:id||uid(),name:V('name'),seg:V('seg'),cr:V('cr'),ct:V('ct'),tel:V('tel'),days:Number(V('days')||D.cfg.days||3),rep:V('rep'),addr:V('addr'),note:V('note')};delete o.cap;if(id)D.cli[D.cli.findIndex(x=>x.id===id)]=o;else D.cli.push(o);save();rAll();toast(id?'تم التعديل':'تمت الإضافة');});
06925: };
06926: 
06927: window.vehForm=function(id){
06928:   const v=id?D.veh.find(x=>x.id===id):{},drvs=D.emp.filter(e=>e.role.includes('سائق')||e.role.includes('مشغّل'));
06929:   openMo(id?'تعديل مركبة':'إضافة مركبة',`<div class="grid g2"><div><label>رقم اللوحة *</label><input id="m_plate" value="${esc(v.plate||'')}"></div><div><label>النوع *</label><select id="m_type">${VTYPES.map(t=>`<option ${v.type===t?'selected':''}>${t}</option>`).join('')}</select></div><div><label>الماركة والموديل</label><input id="m_make" value="${esc(v.make||'')}"></div><div><label>سنة الصنع</label><input id="m_yr" value="${esc(v.yr||'')}" dir="ltr"></div><div><label>رقم الهيكل (VIN)</label><input id="m_vin" value="${esc(v.vin||'')}" dir="ltr"></div><div><label>رقم المحرك</label><input id="m_eng" value="${esc(v.eng||'')}" dir="ltr"></div><div><label>السعة / القدرة التشغيلية</label><input id="m_cap" value="${esc(v.cap||'')}"></div><div><label>سعة خزان الوقود (لتر)</label><input type="number" min="0" id="m_fuelTank" value="${v.fuelTankLiters||''}"></div><div><label>المعدل المرجعي المتوقع (كم/لتر)</label><input type="number" min="0" step="0.01" id="m_fuelEff" value="${v.expectedKmPerLiter||''}"></div><div><label>القيمة التقديرية (ر.س)</label><input type="number" id="m_val" value="${v.val||''}"></div><div><label>رقم الاستمارة</label><input id="m_reg" value="${esc(v.reg||'')}" dir="ltr"></div><div><label>انتهاء الاستمارة</label><input type="date" id="m_regE" value="${v.regE||''}"></div><div><label>رقم الأصل (ERP)</label><input id="m_acct" value="${esc(v.acct||'')}" dir="ltr"></div><div><label>المجموعة</label><select id="m_grp"><option value="">— بدون —</option>${['الشاحنات','الآلات والمعدات','السيارات ووسائل النقل','أصول شركات شقيقة'].map(g=>`<option ${v.grp===g?'selected':''}>${g}</option>`).join('')}</select></div><div style="grid-column:1/-1"><label>السائق / المشغّل المُسند</label><select id="m_drv"><option value="">— غير مُسند —</option>${drvs.map(e=>`<option value="${e.id}" ${v.drv===e.id?'selected':''}>${esc(e.name)} — ${esc(e.role)}</option>`).join('')}</select></div><div style="grid-column:1/-1"><label>الملحقات المسلّمة مع المركبة</label><textarea id="m_acc" rows="2">${esc(v.acc||'')}</textarea></div><div style="grid-column:1/-1"><label>ملاحظات الحالة عند التسليم</label><textarea id="m_note" rows="2">${esc(v.note||'')}</textarea></div></div>`,()=>{if(!V('plate')){toast('اكتب رقم اللوحة','err');return false;}const o={...v,id:id||uid(),plate:V('plate'),type:V('type'),make:V('make'),yr:V('yr'),vin:V('vin'),eng:V('eng'),cap:V('cap'),fuelTankLiters:Number(V('fuelTank')||0),expectedKmPerLiter:Number(V('fuelEff')||0),val:V('val'),reg:V('reg'),regE:V('regE'),drv:V('drv'),acct:V('acct'),grp:V('grp'),acc:V('acc'),note:V('note')};if(id)D.veh[D.veh.findIndex(x=>x.id===id)]=o;else D.veh.push(o);save();rAll();toast(id?'تم التعديل':'تمت الإضافة');});
06930: };
06931: 
06932: /* Dynamic customer portfolio pagination */
06933: window.clientPortfolioForEmployee=function(e,seg){
06934:   const role=typeof opsNormArabic==='function'?opsNormArabic(e?.role||''):String(e?.role||''),requested=seg==='خرسانة'?'concrete':seg==='بلوك'?'block':seg==='all'?'':seg==='auto'?(role.includes('خرسان')?'concrete':role.includes('بلوك')?'block':''):seg;
06935:   const S=bhSafeOps(),activeIds=new Set((S.deliveries||[]).filter(d=>d.status!=='returned'&&d.employeeId===e.id&&(!requested||bhKind(d)===requested)).map(d=>d.clientId).filter(Boolean));
06936:   const arr=(D.cli||[]).filter(c=>{const assigned=c.rep===e.id||(Array.isArray(c.repIds)&&c.repIds.includes(e.id));if(!assigned&&!activeIds.has(c.id))return false;if(!requested)return true;const has=(S.deliveries||[]).some(d=>d.clientId===c.id&&d.status!=='returned'&&bhKind(d)===requested);return has||(assigned&&(c.seg===(requested==='concrete'?'خرسانة':'بلوك')||c.seg==='الاثنين'));}).map(c=>{const l=bhClientLedger(c.id,requested);return {...c,_portfolioSegment:requested?bhKindLabel(requested):c.seg,_portfolioQty:l.quantity,_portfolioSales:l.sales,_portfolioPaid:l.paid,_portfolioBalance:l.remaining,_portfolioOverdue:l.overdue,_portfolioDue:l.nextDueDate,_portfolioLateDays:l.maxDaysLate};}).sort((a,b)=>String(a.name).localeCompare(String(b.name),'ar'));
06937:   arr.clients=arr;return arr;
06938: };
===== LINES 6944-6965 =====
06944:   const R=ref(),L=clientPortfolioForEmployee(e,seg),spec=e.role.includes('بلوك')?'البلوك':e.role.includes('خرسانة')?'الخرسانة الجاهزة':'المبيعات',tot=L.reduce((o,c)=>{o.sales+=bhNum(c._portfolioSales);o.paid+=bhNum(c._portfolioPaid);o.balance+=bhNum(c._portfolioBalance);o.overdue+=bhNum(c._portfolioOverdue);return o;},{sales:0,paid:0,balance:0,overdue:0}),ctx={emp:e.name,days:D.cfg.days};
06945:   const firstCap=8,nextCap=16,chunks=[];let pos=0;if(!L.length)chunks.push([]);else{chunks.push(L.slice(0,firstCap));pos=firstCap;while(pos<L.length){chunks.push(L.slice(pos,pos+nextCap));pos+=nextCap;}}
06946:   const pages=[];
06947:   chunks.forEach((chunk,idx)=>{const first=idx===0,lastData=idx===chunks.length-1,totalRow=lastData&&L.length?`<tfoot><tr><td colspan="3" class="lbl">الإجمالي</td><td class="num">${money(tot.sales)}</td><td class="num">${money(tot.paid)}</td><td class="num">${money(tot.balance)}</td><td></td></tr></tfoot>`:'';const inner=`${mast()}${tbar('محفظــة عمــلاء',idx?'كشف حركة العملاء — تابع':'إقرار مسؤولية عن حركة العملاء والتحصيل',R)}${first?sec('١','بيانات المسؤول',`<div class="dg">${fv('الاسم',e.name,'lg','w2 hi')}${fv('رقم الهوية / الإقامة',e.nid,'mono')}${fv('المسمى الوظيفي',e.role)}${fv('الرقم الوظيفي',e.no,'mono')}${fv('رقم الجوال',e.tel,'mono')}${fv('قطاع المسؤولية',spec,'gold')}${fv('عدد العملاء',L.length,'lg mono')}${fvRaw('مهلة السداد',`<div class="v lg">${D.cfg.days||3} أيام من تاريخ التوريد</div>`,'w2 dark')}</div>`):''}${sec(first?'٢':'تابع','كشف العملاء وحركة الرصيد',bhPortfolioTable(chunk,first?0:firstCap+(idx-1)*nextCap,totalRow),false,`صفحة ${idx+1} من ${chunks.length+1}`)}<div class="ledn">الرصيد محسوب من قيمة المبيعات ناقص سندات التحصيل المسجلة، مع توزيع التحصيلات على أقدم المبالغ المستحقة أولًا.</div>`;pages.push(page(inner,R,'حركة عملاء'));});
06948:   const finalInner=`${mast()}${tbar('محفظــة عمــلاء','الإقرارات والاعتماد — تابع',R)}<div class="portfolio-summary"><div><b>${money(tot.sales)}</b><span>إجمالي التوريدات</span></div><div><b>${money(tot.paid)}</b><span>إجمالي المدفوع</span></div><div><b>${money(tot.balance)}</b><span>الرصيد المتبقي</span></div><div><b>${money(tot.overdue)}</b><span>الرصيد المتأخر</span></div></div>${sec('٣','الإقرارات والالتزامات',cov(D.txt.cli||DEF.cli,ctx),true)}${clause('مسؤولية التسجيل والتحصيل',D.txt.cliX||DEF.cliX,ctx)}${att(ctx)}${exe('المسؤول المُقِر',e.name,'مدير المبيعات',R)}`;pages.push(page(finalInner,R,'اعتماد محفظة العملاء'));return pages.join('');
06949: };
06950: 
06951: /* Bilingual custody is deliberately split into several complete A4 sheets. */
06952: function bhCovRows(L,from,to){const A=VEH_CLAUSES.ar.split('\n').map(s=>s.trim()).filter(Boolean),B=VEH_CLAUSES[L].split('\n').map(s=>s.trim()).filter(Boolean);return `<div class="bi">${A.slice(from,to).map((a,j)=>{const i=from+j;return `<div class="bir"><div class="n">${String(i+1).padStart(2,'0')}</div><div class="a">${esc(a)}</div><div class="b" dir="${LANGS[L].dir}" lang="${L}">${esc(B[i]||'')}</div></div>`;}).join('')}</div>`;}
06953: window.docVeh=function(v,empId,L){
06954:   L=L||LG;if(L==='ar')return bhOriginalDocVeh(v,empId,L);
06955:   const R=ref(),e=D.emp.find(x=>x.id===(empId||v.drv)),acc=(v.acc||'').split(/[·\n,،]/).map(s=>s.trim()).filter(Boolean),both=true,pages=[];
06956:   const p1=`${mastL(L)}${tbarL('ebCust','ttlCust',R,L)}${secL('١','secEmp',`<div class="dg">${fvL('fName',e?.name,L,'lg','w2 hi')}${fvL('fNid',e?.nid,L,'mono')}${fvL('fRole',e?.role,L)}${fvL('fNo',e?.no,L,'mono')}${fvL('fTel',e?.tel,L,'mono')}${fvL('fLic',e?.lic,L,'mono')}${fvL('fLicE',fmtG(e?.licE),L,'mono')}</div>`,L,false,e?.nat?esc(e.nat):null)}${secL('٢','secVeh',`<div class="dg">${fvL('fPlate',v.plate,L,'lg')}${fvL('fType',v.type,L)}${fvL('fMake',v.make,L,'','w2')}${fvL('fVin',v.vin,L,'mono','w2')}${fvL('fEng',v.eng,L,'mono')}${fvL('fYr',v.yr,L,'mono')}${fvL('fCap',v.cap,L)}${fvL('fReg',v.reg,L,'mono')}${fvL('fRegE',fmtG(v.regE),L,'mono')}${fvLRaw('fVal',v.val?`<div class="v lg gold">${money(v.val)} <span class="u">${t('uSar',L)}</span></div>`:DOT,L)}${fvLRaw('fAccs',acc.length?`<div class="v sm">${acc.map(a=>'▸ '+esc(a)).join(' &nbsp;')}</div>`:DOT,L,'w4')}${fvL('fCond',v.note,L,'','w2')}${fvL('fHand',fmtG(today()),L,'mono')}${fvLRaw('fOdo',DOT,L)}</div>`,L,false,t('owned',L))}${secL('٣','secTerms',bhCovRows(L,0,2),L,false,'1 / 3')}`;pages.push(page(p1,R,t('ebCust','ar')));
06957:   const p2=`${mastL(L)}${tbarL('ebCust','ttlCust',R,L)}<div class="continuation-note">تابع الوثيقة رقم ${R}</div>${secL('٣','secTerms',bhCovRows(L,2,6),L,true,'2 / 3')}`;pages.push(page(p2,R,t('ebCust','ar')));
06958:   const p3=`${mastL(L)}${tbarL('ebCust','ttlCust',R,L)}<div class="continuation-note">صفحة الاعتماد النهائية — ${R}</div>${secL('٣','secTerms',bhCovRows(L,6,8),L,false,'3 / 3')}<div class="att"><span class="lb">${t('secAtt','ar')} · ${t('secAtt',L)}</span><div class="tx">${esc(t('ack','ar'))}</div><div class="tx x" dir="${LANGS[L].dir}" lang="${L}">${esc(t('ack',L))}</div></div>${exeL('sDriver',e?.name,'sWksp',R,L)}`;pages.push(page(p3,R,t('ebCust','ar')));return pages.join('');
06959: };
06960: 
06961: /* Sales entry: no credit ceiling. The unpaid part is the balance due. */
06962: window.opsDeliveryForm=function(){if(!D.cli.length){opsToast('أضف العملاء أولًا','err');return;}opsOpenModal('إنشاء أمر توريد',`<div class="ops-note">المتبقي يُحسب تلقائيًا: قيمة التوريد ناقص النقدي والتحويل. يستحق وفق مهلة العميل، والافتراضي ثلاثة أيام.</div><div class="ops-form-grid"><div><label class="ops-required">العميل</label><select name="clientId" required>${opsSelect(opsClients(),'','— اختر —')}</select></div><div><label class="ops-required">التاريخ</label><input type="date" name="date" value="${opsToday()}" required></div><div><label class="ops-required">المنتج</label><input name="product" required placeholder="خرسانة أو بلوك"></div><div><label>المواصفة / الخلطة</label><input name="spec"></div><div><label class="ops-required">الكمية</label><input type="number" step="any" min="0.01" name="quantity" required></div><div><label>الوحدة</label><select name="unit"><option>م³</option><option>قطعة</option><option>طن</option></select></div><div><label class="ops-required">قيمة التوريد</label><input type="number" step="any" min="0" name="amount" required></div><div><label>مدفوع نقدي</label><input type="number" step="any" min="0" name="cash"></div><div><label>مدفوع تحويل</label><input type="number" step="any" min="0" name="transfer"></div><div><label>رقم الفاتورة / طلب العميل</label><input name="clientOrder"></div><div><label>المركبة</label><select name="vehicleId">${opsSelect(opsVehicles(),'','— اختر —')}</select></div><div><label>السائق</label><select name="employeeId">${opsSelect(opsEmployees(true),'','— اختر —')}</select></div><div class="full"><label class="ops-required">مكان التوريد</label><input name="destination" required></div><div class="full"><label>ملاحظات</label><textarea name="notes" rows="2"></textarea></div></div>`,async fd=>{const amount=bhNum(opsFd(fd,'amount')),cash=bhNum(opsFd(fd,'cash')),transfer=bhNum(opsFd(fd,'transfer'));if(cash+transfer>amount+0.01)throw new Error('المدفوع لا يجوز أن يتجاوز قيمة التوريد');const d={id:opsUid('dlv'),no:opsNextNo('DLV'),clientId:opsFd(fd,'clientId'),date:opsFd(fd,'date'),product:opsFd(fd,'product'),spec:opsFd(fd,'spec'),quantity:bhNum(opsFd(fd,'quantity')),unit:opsFd(fd,'unit'),amount,cash,transfer,credit:Math.max(0,amount-cash-transfer),clientOrder:opsFd(fd,'clientOrder'),vehicleId:opsFd(fd,'vehicleId'),employeeId:opsFd(fd,'employeeId'),destination:opsFd(fd,'destination'),notes:opsFd(fd,'notes'),status:'out',createdAt:new Date().toISOString()};OPS.deliveries.unshift(d);opsAddMovementRecord('DELIVERY_OUT',{date:d.date+'T12:00',employeeId:d.employeeId,entityKind:'client',entityId:d.clientId,quantity:d.quantity,unit:d.unit,amount:d.amount,reference:d.clientOrder||d.no,notes:`${d.product} إلى ${d.destination} — متبقي ${opsMoney(d.credit)} ر.س`,module:'delivery',moduleId:d.id});await opsPersist('أمر توريد '+d.no);opsToast('تم تسجيل التوريد والرصيد المستحق');},'اعتماد التوريد');};
06963: 
06964: window.opsRenderDeliveries=function(){
06965:   const out=OPS.deliveries.filter(x=>x.status==='out'),sales=OPS.deliveries.filter(x=>x.status!=='returned').reduce((s,x)=>s+bhNum(x.amount),0),paid=(D.cli||[]).reduce((s,c)=>s+bhClientAggregate(c).paid,0),balance=bhAllReceivables(),overdue=bhAllOverdue();
===== LINES 6970-6976 =====
06970: /* Print reports in natural multi-page flow without clipping. */
06971: window.opsPrintReport=function(title,fields,rows,qrPayload,signatures,options={}){
06972:   opsCloseModal();const qr=qrPayload&&typeof qrSVG==='function'?qrSVG(`BHFC|${qrPayload}`,110):'',table=rows?.length?`<table><thead><tr>${Object.keys(rows[0]).map(k=>`<th>${opsEsc(k)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${Object.values(r).map(v=>`<td>${opsEsc(v)}</td>`).join('')}</tr>`).join('')}</tbody></table>`:'',sigs=signatures?.length?signatures:[{label:'إعداد'},{label:'مراجعة'},{label:'اعتماد'},{label:'ختم المنشأة',type:'stamp'}],sigHtml=`<div class="report-signatures" style="margin-top:12mm;display:grid;grid-template-columns:repeat(${Math.min(sigs.length,4)},1fr);gap:5mm">${sigs.map(s=>`<div class="${s.type==='stamp'?'ops-stamp-space':''}" style="border:1px solid #9aa7ad;border-radius:2mm;padding:3mm;min-height:30mm"><b style="display:block;color:#0b2233">${opsEsc(s.label||'التوقيع')}</b>${s.name?`<span style="display:block;margin-top:2mm">${opsEsc(s.name)}</span>`:''}${s.type==='thumbprint'?'<div style="width:18mm;height:23mm;border:1px solid #777;border-radius:50%;margin:3mm auto 0"></div>':s.type==='stamp'?'<div style="height:17mm;display:grid;place-items:center;color:#866a3b">مساحة الختم</div>':'<div style="height:10mm;border-bottom:1px solid #555;margin-top:2mm"></div><small>التاريخ: ____ / ____ / ______</small>'}</div>`).join('')}</div>`;
06973:   opsEl('sheet').innerHTML=`<div class="ops-print-report ${options.landscape?'ops-landscape':''}"><div style="display:grid;grid-template-columns:1fr 34mm;gap:8mm;align-items:start;border-bottom:2px solid #0b2233;padding-bottom:5mm;margin-bottom:5mm"><div><h1>${opsEsc(title)}</h1><b>${opsEsc(OPS.settings.companyName)}</b><div>تاريخ إصدار التقرير: ${opsFmtDate(opsToday())}</div></div><div style="text-align:center;border:1px solid #ccd5d9;border-radius:2mm;padding:2mm">${qr}<small style="display:block">رمز تحقق مستقل</small></div></div>${fields?.length?`<table style="margin-bottom:6mm"><tbody>${fields.map(x=>`<tr><th style="width:35mm">${opsEsc(x[0])}</th><td class="${x[0]==='نص الإقرار'?'ops-declaration':''}">${opsEsc(x[1])}</td></tr>`).join('')}</tbody></table>`:''}<div class="report-section">${options.beforeTable||''}</div>${table}${sigHtml}</div>`;setTimeout(()=>window.print(),350);
06974: };
06975: 
06976: /* Diesel control: extend existing checks with tank, assignment, efficiency, tax and repair-state tests. */
===== LINES 7007-7013 =====
07007: function bhFactoryControlExceptions(from,to){
07008:   const X=[],inRange=d=>(!from||bhDate(d)>=from)&&(!to||bhDate(d)<=to),push=(priority,area,obs,impact,action,owner)=>X.push([priority,area,obs,impact,action,owner]);
07009:   const sales=(OPS.deliveries||[]).filter(d=>d.status!=='returned'&&inRange(d.date));
07010:   const seen=new Map();sales.forEach(d=>{const no=String(d.clientOrder||d.no||'').trim();if(no){const k=no.toLowerCase();if(seen.has(k))push('عاجل','المبيعات',`رقم فاتورة مكرر ${no}`,`${opsCli(d.clientId)?.name||'عميل غير محدد'} / ${opsCli(seen.get(k).clientId)?.name||'عميل غير محدد'}`,'إيقاف اعتماد التقرير حتى مطابقة أصل الفاتورة وتحديد السجل الصحيح','الحسابات');else seen.set(k,d);}if(!d.employeeId)push('مراجعة','المبيعات',`فاتورة ${no||d.no} دون مسؤول مبيعات`,'تعذر تحديد المسؤولية','إسناد الموظف المسؤول قبل الاعتماد','مدير المبيعات');if(!d.vehicleId)push('مراجعة','التوريد',`توريد ${no||d.no} دون مركبة`,'ضعف ربط الفاتورة بحركة البوابة','ربط المركبة والسائق أو إثبات سبب التوريد الخارجي','مسؤول الحركة');if(d.status==='out'&&bhDaysLate(d.date)>1)push('عاجل','التوريد',`أمر ${d.no} ما زال بالطريق`,`${bhDaysLate(d.date)} يوم دون إغلاق`,'إثبات التسليم أو المرتجع وتحديث حالة الأمر','مسؤول الحركة');});
07011:   ['concrete','block'].forEach(kind=>{const group=sales.filter(d=>bhKind(d)===kind&&bhNum(d.quantity)>0&&bhNum(d.amount)>0),prices=group.map(d=>bhNum(d.amount)/bhNum(d.quantity)).filter(x=>x>0),med=opsMedian(prices);if(med)group.forEach(d=>{const price=bhNum(d.amount)/bhNum(d.quantity),diff=Math.abs(price-med)/med;if(diff>.25)push('مراجعة','تسعير المبيعات',`سعر وحدة غير معتاد في ${d.clientOrder||d.no}`,`${opsMoney(price)} مقابل وسيط ${opsMoney(med)} للوحدة`,'مطابقة العقد أو عرض السعر والخصم المعتمد','مدير المبيعات / الحسابات');});});
07012:   /* الأرصدة الدائنة والدفعات المقدمة حركات سليمة ولا تُعامل كاستثناء رقابي. */
07013:   (OPS.production||[]).filter(p=>inRange(p.date)).forEach(p=>{const q=bhNum(p.quantity),w=bhNum(p.waste),rate=q+w?w/(q+w)*100:0;if(rate>5)push(rate>10?'عاجل':'مراجعة','الإنتاج',`هالك مرتفع في ${p.no||p.product}`,`${rate.toFixed(1)}% من إجمالي الدفعة`,'تحليل سبب الهالك ومطابقة المواد المصروفة والكمية المنتجة','مشرف الإنتاج');if(bhNum(p.downtimeMinutes)>=60)push('مراجعة','الإنتاج',`توقف إنتاج في ${p.no||p.product}`,`${opsNum(p.downtimeMinutes)} دقيقة — ${p.downtimeReason||'سبب غير مسجل'}`,'تحديد السبب الجذري والإجراء الوقائي ومسؤول الإغلاق','مدير المصنع');if(!(p.materials||[]).length)push('مراجعة','الإنتاج',`إنتاج ${p.no||p.product} دون مواد مرتبطة`,`${opsNum(q)} ${p.unit||''}`,'ربط استهلاك الأسمنت والخامات بالدفعة قبل إقفال الفترة','المخازن / الإنتاج');});
===== LINES 7036-7042 =====
07036: /* Bilingual inspection pages are also split so translated checklists never fall outside A4. */
07037: window.docVehInspect=function(v,empId,L){
07038:   L=L||LG;if(L==='ar')return window.__bhOriginalInspect?window.__bhOriginalInspect(v,empId,L):'';
07039:   const R=ref(),e=D.emp.find(x=>x.id===(empId||v.drv)),both=true;
07040:   const tyres=TYRE_POS.ar.map((p,i)=>`<tr><td class="idx">${String(i+1).padStart(2,'0')}</td><td class="nm">${p}<span class="x" dir="${LANGS[L].dir}">${TYRE_POS[L][i]}</span></td><td class="wr"></td><td class="wr"></td><td class="cnd">${condL(L)}</td></tr>`).join('');
07041:   const mrows=arr=>arr.ar.map((m,i)=>`<div class="mrow"><span class="l">${m}<span class="x" dir="${LANGS[L].dir}">${arr[L][i]}</span></span>${condL(L)}</div>`).join('');
07042:   const isMix=/ميكسر|خلاط/.test(v.type||''),isPump=/مضخ/.test(v.type||''),isLift=/لاقط|حراج|شيول|لودر|ونش|كرين/.test(v.type||''),SP=isMix?SPEC_ITEMS.mix:isPump?SPEC_ITEMS.pump:isLift?SPEC_ITEMS.lift:null;
===== LINES 7050-7062 =====
07050: 
07051: /* Daily concrete/block declaration: dynamic rows, actual paid amount and invoice balance. */
07052: window.opsPrintDailySalesDeclaration=function(kind,batchId){
07053:   const batch=batchId?opsById(OPS.imports,batchId):null,date=batch?.reportDate||opsEl('repTo')?.value||opsToday(),lines=opsDailySalesLines(kind,date,batchId),employeeIds=[...new Set(lines.map(x=>x.employeeId).filter(Boolean))],employeeId=(kind==='concrete'?batch?.concreteEmployeeId:batch?.blockEmployeeId)||(employeeIds.length===1?employeeIds[0]:opsSalesResponsible(kind)?.id)||'';
07054:   if(!lines.length){opsToast(`لا توجد مبيعات ${kind==='concrete'?'خرسانة':'بلوك'} في ${opsFmtDate(date)}`,'err');return;}
07055:   const e=opsEmp(employeeId)||{},employee=e.name||'غير مسند',label=kind==='concrete'?'الخرسانة':'البلوك',unit=kind==='concrete'?'م³':'قطعة',ref=`SAL-${kind==='concrete'?'CON':'BLK'}-${date.replace(/-/g,'')}-${batchId?String(batchId).slice(-6):'DAY'}`;
07056:   const mapped=lines.map((d,i)=>{const inv=bhClientLedger(d.clientId,kind).selected.find(x=>x.id===d.id),amount=bhNum(d.amount),paid=inv?inv.immediatePaid+inv.allocatedCollection:Math.min(amount,bhNum(d.cash)+bhNum(d.transfer)),remaining=inv?inv.remaining:Math.max(0,amount-paid),due=inv?.dueDate||bhAddDays(bhDate(d.date),bhPaymentDays(opsCli(d.clientId)));return {i,d,inv,amount,paid,remaining,due};});
07057:   const totals=mapped.reduce((o,x)=>{o.quantity+=bhNum(x.d.quantity);o.amount+=x.amount;o.paid+=x.paid;o.remaining+=x.remaining;return o;},{quantity:0,amount:0,paid:0,remaining:0});
07058:   const rows=mapped.map(x=>({'م':x.i+1,'الفاتورة':x.d.clientOrder||x.d.no,'العميل':opsCli(x.d.clientId)?.name||'—','الصنف / النوع':x.d.product||label,'الكمية':`${opsNum(x.d.quantity)} ${x.d.unit||unit}`,'قيمة التوريد':opsMoney(x.amount),'المدفوع':opsMoney(x.paid),'المتبقي':opsMoney(x.remaining),'الاستحقاق':x.due?opsFmtDate(x.due):'—','الحالة':x.remaining<=0?'مسدد':x.due<opsToday()?`متأخر ${bhDaysLate(x.due)} يوم`:'قائم'}));
07059:   opsPrintReport(`إقرار مبيعات ${label} اليومي`,[['رقم الإقرار',ref],['تاريخ المبيعات',opsFmtDate(date)],['الموظف المسؤول',employee],['المسمى الوظيفي',e.role||opsSalesResponsibleRole(kind)],['الرقم الوظيفي',e.no||'—'],['عدد الفواتير',lines.length],['إجمالي الكمية',`${opsNum(totals.quantity)} ${unit}`],['إجمالي قيمة التوريدات',`${opsMoney(totals.amount)} ر.س`],['إجمالي المدفوع',`${opsMoney(totals.paid)} ر.س`],['إجمالي المتبقي',`${opsMoney(totals.remaining)} ر.س`],['نص الإقرار',`أقر أنا ${employee} بأن جميع مبيعات ${label} الموضحة عن يوم ${opsFmtDate(date)} مثبتة بالعميل والصنف والكمية والقيمة والمدفوع والمتبقي، وأن المستندات والتحصيلات المسجلة سُلّمت للحسابات للمطابقة.`]],rows,`daily-sales:${kind}:${date}:${batchId||employeeId}`,[{label:'اسم وتوقيع الموظف',name:employee},{label:'بصمة الموظف',type:'thumbprint'},{label:'مراجعة المحاسب'},{label:'اعتماد الإدارة والختم',type:'stamp'}],{landscape:true});
07060: };
07061: 
07062: function bhPatchStaticUi(){
===== LINES 7067-7073 =====
07067:   document.querySelectorAll('p.sub').forEach(p=>{p.innerHTML=p.innerHTML.replace(/وسقف الائتمان ومهلة السداد/g,'وقيمة التوريدات والمدفوع والمتبقي ومهلة السداد');});
07068:   document.querySelectorAll('td,p,b,label').forEach(el=>{if(el.childElementCount===0){el.textContent=el.textContent.replace('سقف الائتمان · مهلة السداد · مسؤولية التحصيل','قيمة التوريد · المدفوع · المتبقي · مسؤولية التحصيل').replace('اربط كل عميل بمندوبه، وحدّد سقف ائتمانه ومهلة سداده.','اربط كل عميل بمسؤول المبيعات، وسجّل قيمة التوريد والمدفوع والمتبقي وموعد الاستحقاق.').replace('وسقف الائتمان ومهلة السداد','وقيمة التوريدات والمدفوع والمتبقي ومهلة السداد').replace('سقف الائتمان الافتراضي','سياسة رصيد العملاء');}});
07069:   D.cfg.days=Number(D.cfg.days||3)||3;D.cfg.cap=0;
07070:   const newCli=`ألتزم بإثبات كل توريد باسم العميل الصحيح ورقم الفاتورة والكمية وقيمة التوريد وطريقة السداد.\nألتزم بعدم اعتبار أي تصنيف داخل فاتورة الحركة سدادًا، وإثبات كامل قيمة البيع كمديونية ثم تسجيل السداد بسند مستقل كرصيد مستحق دون إخفاء أو تأجيل.\nألتزم بمتابعة المبالغ غير المسددة خلال مهلة {الأيام} أيام من تاريخ التوريد، ورفع حالة المتأخرات للإدارة.\nألتزم بعدم استلام أي مبلغ دون إيصال رسمي أو سند تحويل قابل للمطابقة، وتسليم المستندات للحسابات.\nألتزم بعدم تعديل اسم العميل أو الكمية أو القيمة أو طريقة السداد بعد الاعتماد إلا بمستند تصحيح واضح.\nألتزم بإبلاغ الإدارة عن أي اختلاف بين تقرير الحركة والفواتير والتحصيلات قبل إصدار الإقرار الأسبوعي.\nأُقر بأن بيانات العملاء والمبيعات والتحصيلات ملك للمنشأة وتخضع للمراجعة والمطابقة في أي وقت.`;
07071:   const newCliX='يقر المسؤول بأن الرصيد الظاهر لكل عميل ناتج عن التوريدات المسجلة مطروحًا منها التحصيلات المثبتة بالمستندات، وأن أي فرق أو تحصيل غير مثبت أو تعديل بلا مستند يجب إبلاغ الإدارة به وتصحيحه قبل اعتماد التقرير.';
07072:   if(!D.txt.cli||/سقف الائتمان/.test(D.txt.cli))D.txt.cli=newCli;if(!D.txt.cliX||/سقف الائتمان|الائتمان/.test(D.txt.cliX))D.txt.cliX=newCliX;
07073:   save();fillCfg();fillTxt();rAll();
===== LINES 7106-7114 =====
07106:   },'اعتماد السداد');
07107: };
07108: 
07109: window.opsDeliveryForm=function(){if(!D.cli.length){opsToast('أضف العملاء أولًا','err');return;}opsOpenModal('إنشاء أمر توريد / بيع',`<div class="ops-note">تُسجل قيمة البيع كاملة كمديونية. أي سداد يُسجل لاحقًا بسند مستقل.</div><div class="ops-form-grid"><div><label class="ops-required">العميل</label><select name="clientId" required>${opsSelect(opsClients(),'','— اختر —')}</select></div><div><label class="ops-required">التاريخ</label><input type="date" name="date" value="${opsToday()}" required></div><div><label class="ops-required">المنتج</label><input name="product" required></div><div><label>المواصفة / الخلطة</label><input name="spec"></div><div><label class="ops-required">الكمية</label><input type="number" step="any" min="0.01" name="quantity" required></div><div><label>الوحدة</label><select name="unit"><option>م³</option><option>قطعة</option><option>طن</option></select></div><div><label class="ops-required">قيمة البيع</label><input type="number" step="any" min="0" name="amount" required></div><div><label>رقم الفاتورة / طلب العميل</label><input name="clientOrder"></div><div><label>المركبة</label><select name="vehicleId">${opsSelect(opsVehicles(),'','— اختر —')}</select></div><div><label>السائق</label><select name="employeeId">${opsSelect(opsEmployees(true),'','— اختر —')}</select></div><div class="full"><label>مكان التوريد</label><input name="destination"></div><div class="full"><label>ملاحظات</label><textarea name="notes" rows="2"></textarea></div></div>`,async fd=>{const amount=bh3Num(opsFd(fd,'amount')),d={id:opsUid('dlv'),no:opsNextNo('DLV'),clientId:opsFd(fd,'clientId'),date:opsFd(fd,'date'),product:opsFd(fd,'product'),spec:opsFd(fd,'spec'),quantity:bh3Num(opsFd(fd,'quantity')),unit:opsFd(fd,'unit'),amount,cash:0,transfer:0,credit:amount,clientOrder:opsFd(fd,'clientOrder'),vehicleId:opsFd(fd,'vehicleId'),employeeId:opsFd(fd,'employeeId'),destination:opsFd(fd,'destination'),notes:opsFd(fd,'notes'),accountingRule:'FULL_DEBT_UNTIL_COLLECTION',status:'out',createdAt:new Date().toISOString()};OPS.deliveries.unshift(d);opsAddMovementRecord('DELIVERY_OUT',{date:d.date+'T12:00',employeeId:d.employeeId,entityKind:'client',entityId:d.clientId,quantity:d.quantity,unit:d.unit,amount:d.amount,reference:d.clientOrder||d.no,notes:`${d.product} — مديونية كاملة حتى تسجيل السداد`,module:'delivery',moduleId:d.id});await opsPersist('أمر توريد '+d.no);},'اعتماد التوريد');};
07110: 
07111: window.rCli=function(){const s=$('fSeg').value,r=$('fRep').value,t=$('tCli');let L=D.cli;if(s)L=L.filter(c=>c.seg===s);if(r)L=L.filter(c=>c.rep===r);if(!L.length){t.innerHTML=`<tr><td colspan="9"><div class="empty"><span class="big">🧾</span>لا يوجد عملاء</div></td></tr>`;return;}t.innerHTML=L.map(c=>{const rp=D.emp.find(e=>e.id===c.rep),a=window.bhClientAggregate(c),due=a.nextDueDate?fmtG(a.nextDueDate):'—';return `<tr><td><b>${esc(c.name)}</b><div style="font-size:11px;color:var(--muted)">${esc(c.code?'رقم العميل: '+c.code:(c.cr||''))}</div></td><td><span class="chip ${c.seg==='بلوك'?'blk':'con'}">${esc(c.seg)}</span></td><td>${typeof bhClientQtyText==='function'?bhClientQtyText(c):'—'}</td><td class="mono">${money(a.sales)}</td><td class="mono">${money(a.paid)}</td><td class="mono ${a.remaining?'client-balance-positive':'client-balance-clear'}">${money(a.remaining)}</td><td>${due}${a.maxDaysLate?`<div style="color:var(--warn);font-size:11px">متأخر ${a.maxDaysLate} يوم</div>`:''}</td><td>${rp?esc(rp.name):'<span style="color:var(--warn)">غير مُسند</span>'}</td><td><button class="btn btn-o btn-sm" onclick="cliForm('${c.id}')">تعديل</button></td></tr>`;}).join('');};
07112: 
07113: window.opsRenderDeliveries=function(){const out=OPS.deliveries.filter(x=>x.status==='out'),sales=OPS.deliveries.filter(x=>x.status!=='returned').reduce((s,x)=>s+bh3Num(x.amount),0),paid=(OPS.collections||[]).reduce((s,x)=>s+bh3Num(x.amount),0),balance=(D.cli||[]).reduce((s,c)=>s+window.bhClientAggregate(c).remaining,0),overdue=(D.cli||[]).reduce((s,c)=>s+window.bhClientAggregate(c).overdue,0),clients=(D.cli||[]).map(c=>({c,l:window.bhClientAggregate(c)})).sort((a,b)=>b.l.remaining-a.l.remaining);opsEl('opsContent').innerHTML=opsHead('التوريد والعملاء','كل تقرير أسبوعي يضاف إلى تاريخ العميل؛ السداد وحده يخفض الرصيد',`<button class="ops-btn primary" onclick="opsDeliveryForm()">بيع / توريد</button><button class="ops-btn gold" onclick="opsCollectionForm()">تسجيل سداد</button><button class="ops-btn ghost" onclick="go('cli')">سجل العملاء</button>`)+`<div class="ops-kpis">${opsKpi(OPS.deliveries.length,'فواتير وتوريدات','blue')}${opsKpi(out.length,'بالطريق',out.length?'warn':'')}${opsKpi(opsMoney(sales),'إجمالي المبيعات','purple')}${opsKpi(opsMoney(paid),'إجمالي السداد')}${opsKpi(opsMoney(balance),'الرصيد القائم',balance?'warn':'')}${opsKpi(opsMoney(overdue),'المتأخر',overdue?'danger':'')}</div><div class="ops-card"><h3>حركة المبيعات <span class="n">${OPS.deliveries.length}</span></h3>${OPS.deliveries.length?`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>الفاتورة</th><th>التاريخ</th><th>رقم العميل</th><th>العميل</th><th>الصنف</th><th>الكمية</th><th>قيمة البيع</th><th>المسدد حتى الآن</th><th>المتبقي</th></tr></thead><tbody>${OPS.deliveries.slice().sort((a,b)=>String(b.date).localeCompare(String(a.date))).map(d=>{const inv=window.bhClientLedger(d.clientId).invoices.find(x=>x.id===d.id),c=opsCli(d.clientId);return `<tr><td class="mono">${opsEsc(d.clientOrder||d.no)}</td><td>${opsFmtDate(d.date)}</td><td class="mono">${opsEsc(c?.code||d.customerCode||'—')}</td><td><b>${opsEsc(c?.name||'—')}</b></td><td>${opsEsc(d.product)}</td><td class="num">${opsNum(d.quantity)} ${opsEsc(d.unit)}</td><td class="num">${opsMoney(d.amount)}</td><td class="num">${opsMoney(inv?.allocatedCollection||0)}</td><td class="num">${opsMoney(inv?.remaining??d.amount)}</td></tr>`;}).join('')}</tbody></table></div>`:opsEmpty('لا توجد مبيعات','')}</div><div class="ops-grid" style="margin-top:12px"><div class="ops-card ops-col-7"><h3>سندات السداد <span class="n">${OPS.collections.length}</span></h3>${OPS.collections.length?`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>السند</th><th>التاريخ</th><th>العميل</th><th>الفاتورة</th><th>المبلغ</th><th>الطريقة</th><th>المستلم</th></tr></thead><tbody>${OPS.collections.slice(0,200).map(c=>`<tr><td class="mono">${opsEsc(c.receipt||c.no)}</td><td>${opsFmtDate(c.date)}</td><td>${opsEsc(opsCli(c.clientId)?.name||'—')}</td><td>${opsEsc(c.invoiceId?(opsById(OPS.deliveries,c.invoiceId)?.clientOrder||'محدد'):'الأقدم تلقائيًا')}</td><td class="num">${opsMoney(c.amount)}</td><td>${opsEsc(c.method)}</td><td>${opsEsc(opsEmp(c.employeeId)?.name||'—')}</td></tr>`).join('')}</tbody></table></div>`:opsEmpty('لا توجد سندات سداد','')}</div><div class="ops-card ops-col-5"><h3>أرصدة العملاء التراكمية</h3><div class="ops-alerts">${clients.length?clients.map(({c,l})=>`<div class="ops-alert ${l.overdue?'bad':l.remaining?'info':''}"><span class="bar"></span><div><b>${opsEsc(bh3ClientLabel(c))}</b><span>مبيعات ${opsMoney(l.sales)} — سداد ${opsMoney(l.paid)} — متبقي ${opsMoney(l.remaining)}</span></div><time>${l.maxDaysLate?l.maxDaysLate+' يوم':'قائم'}</time></div>`).join(''):opsEmpty('لا يوجد عملاء','')}</div></div></div>`;};
07114: 
===== LINES 7187-7200 =====
07187: 
07188: /* Main vehicle registry: operational status and diesel expectation are master-data fields. */
07189: window.vehForm=function(id){
07190:   const v=id?D.veh.find(x=>x.id===id):{},drvs=D.emp.filter(e=>e.role.includes('سائق')||e.role.includes('مشغّل'));
07191:   openMo(id?'تعديل مركبة / معدة':'إضافة مركبة / معدة',`<div class="grid g2"><div><label>رقم اللوحة / التشغيل *</label><input id="m_plate" value="${esc(v.plate||'')}"></div><div><label>النوع *</label><select id="m_type">${VTYPES.map(t=>`<option ${v.type===t?'selected':''}>${t}</option>`).join('')}</select></div><div><label>الماركة والموديل</label><input id="m_make" value="${esc(v.make||'')}"></div><div><label>سنة الصنع</label><input id="m_yr" value="${esc(v.yr||'')}" dir="ltr"></div><div><label>رقم الهيكل (VIN)</label><input id="m_vin" value="${esc(v.vin||'')}" dir="ltr"></div><div><label>رقم المحرك</label><input id="m_eng" value="${esc(v.eng||'')}" dir="ltr"></div><div><label>السعة / القدرة التشغيلية</label><input id="m_cap" value="${esc(v.cap||'')}"></div><div><label>سعة خزان الوقود (لتر)</label><input type="number" min="0" id="m_fuelTank" value="${v.fuelTankLiters||''}"></div><div><label>المعدل المرجعي المتوقع (كم/لتر)</label><input type="number" min="0" step="0.01" id="m_fuelEff" value="${v.expectedKmPerLiter||''}"></div><div><label>القيمة التقديرية (ر.س)</label><input type="number" id="m_val" value="${v.val||''}"></div><div><label>رقم الاستمارة</label><input id="m_reg" value="${esc(v.reg||'')}" dir="ltr"></div><div><label>انتهاء الاستمارة</label><input type="date" id="m_regE" value="${v.regE||''}"></div><div><label>رقم الأصل (ERP)</label><input id="m_acct" value="${esc(v.acct||'')}" dir="ltr"></div><div><label>المجموعة</label><select id="m_grp"><option value="">— بدون —</option>${['الشاحنات','الآلات والمعدات','السيارات ووسائل النقل','أصول شركات شقيقة'].map(g=>`<option ${v.grp===g?'selected':''}>${g}</option>`).join('')}</select></div><div><label>الحالة التشغيلية</label><select id="m_operationalStatus">${BH4_VEHICLE_STATUSES.map(([k,l])=>`<option value="${k}" ${String(v.operationalStatus||'')===k?'selected':''}>${l}</option>`).join('')}</select></div><div><label>متابعة تقرير الديزل</label><select id="m_dieselExpected">${BH4_DIESEL_EXPECT.map(([k,l])=>`<option value="${k}" ${String(v.dieselExpected||'auto')===k?'selected':''}>${l}</option>`).join('')}</select></div><div style="grid-column:1/-1"><label>السائق / المشغّل المُسند</label><select id="m_drv"><option value="">— غير مُسند —</option>${drvs.map(e=>`<option value="${e.id}" ${v.drv===e.id?'selected':''}>${esc(e.name)} — ${esc(e.role)}</option>`).join('')}</select></div><div style="grid-column:1/-1"><label>سبب الحالة / ملاحظة تشغيلية</label><textarea id="m_statusNote" rows="2">${esc(v.statusNote||'')}</textarea></div><div style="grid-column:1/-1"><label>الملحقات المسلّمة مع المركبة</label><textarea id="m_acc" rows="2">${esc(v.acc||'')}</textarea></div><div style="grid-column:1/-1"><label>ملاحظات الحالة عند التسليم</label><textarea id="m_note" rows="2">${esc(v.note||'')}</textarea></div></div>`,()=>{if(!V('plate')){toast('اكتب رقم اللوحة أو التشغيل','err');return false;}const o={...v,id:id||uid(),plate:V('plate'),type:V('type'),make:V('make'),yr:V('yr'),vin:V('vin'),eng:V('eng'),cap:V('cap'),fuelTankLiters:Number(V('fuelTank')||0),expectedKmPerLiter:Number(V('fuelEff')||0),val:V('val'),reg:V('reg'),regE:V('regE'),drv:V('drv'),acct:V('acct'),grp:V('grp'),operationalStatus:V('operationalStatus'),dieselExpected:V('dieselExpected')||'auto',statusNote:V('statusNote'),statusUpdatedAt:new Date().toISOString(),acc:V('acc'),note:V('note')};if(id)D.veh[D.veh.findIndex(x=>x.id===id)]=o;else D.veh.push(o);save();rAll();toast(id?'تم التعديل':'تمت الإضافة');});
07192: };
07193: 
07194: window.rVeh=function(){
07195:   const t=$('tVeh'),g=$('fGrp')?.value||'',L=g?D.veh.filter(v=>v.grp===g):D.veh,head=document.querySelector('#p-veh thead tr');if(head)head.innerHTML='<th>اللوحة/التشغيل</th><th>النوع</th><th>الماركة/الموديل</th><th>رقم الأصل</th><th>القيمة</th><th>الحالة التشغيلية</th><th>السائق المُسند</th><th></th>';
07196:   if(!L.length){t.innerHTML=`<tr><td colspan="8"><div class="empty"><span class="big">🚛</span>لا توجد مركبات أو معدات</div></td></tr>`;return;}
07197:   t.innerHTML=L.map(v=>{const d=D.emp.find(e=>e.id===v.drv),expect=bh4DieselExpected(v);return `<tr><td><b class="mono">${esc(v.plate||'—')}</b><div class="muted">${esc(v.yr||'')}</div></td><td>${esc(v.type||'')}</td><td>${esc(v.make||'')}</td><td class="mono">${esc(v.acct||v.vin||'—')}</td><td class="mono">${money(v.val)}</td><td>${bh4StatusChip(v)}<div class="muted">${expect===true?'متوقع بالديزل':expect===false?'غير متوقع بالديزل':'التوقع غير محدد'}</div></td><td>${d?esc(d.name):'<span style="color:var(--muted)">غير مُسند</span>'}</td><td style="white-space:nowrap"><button class="btn btn-o btn-sm" onclick="vehForm('${v.id}')">تعديل</button><button class="btn btn-g btn-sm" onclick="prVeh('${v.id}')">🖨️</button><button class="btn btn-d btn-sm" onclick="del('veh','${v.id}')">×</button></td></tr>`;}).join('');
07198: };
07199: 
07200: /* Collection entry accepts debt payments and advance payments. */
===== LINES 7205-7213 =====
07205:   opsOpenModal('تسجيل سداد أو دفعة مقدمة',`<div class="ops-note"><b>القاعدة:</b> يمكن تسجيل سداد على مديونية قائمة أو دفعة مقدمة. أي مبلغ زائد يظهر رصيدًا دائنًا ويُخصم تلقائيًا من فواتير العميل التالية.</div><div class="ops-form-grid"><div><label class="ops-required">العميل</label><select name="clientId" required onchange="bh3RefreshCollectionInvoices(this.value)">${(D.cli||[]).map(c=>`<option value="${c.id}">${opsEsc(bh3ClientLabel(c))}</option>`).join('')}</select></div><div><label class="ops-required">التاريخ</label><input type="date" name="date" value="${opsToday()}" required></div><div><label>نوع الحركة</label><select name="paymentType"><option value="debt">سداد مديونية</option><option value="advance">دفعة مقدمة / رصيد دائن</option></select></div><div><label>الفاتورة — اختياري</label><select name="invoiceId">${bh3CollectionOptions(first,'')}</select></div><div class="full"><div class="muted" id="bh3ClientBalance">${l.creditBalance>0?`الرصيد الحالي: دائن ${opsMoney(l.creditBalance)} ر.س`:l.remaining>0?`الرصيد الحالي: مدين ${opsMoney(l.remaining)} ر.س`:'الرصيد الحالي: مسدد'}</div></div><div><label class="ops-required">المبلغ</label><input type="number" step="any" min="0.01" name="amount" required></div><div><label>طريقة السداد</label><select name="method"><option>تحويل بنكي</option><option>نقدي</option><option>شيك</option><option>شبكة</option></select></div><div><label class="ops-required">رقم السند / التحويل</label><input name="receipt" required></div><div><label>المستلم</label><select name="employeeId">${opsSelect(opsEmployees(true),'','— اختر —')}</select></div><div class="full"><label>ملاحظات</label><textarea name="notes" rows="2"></textarea></div><div class="full"><label>صورة السند</label><input type="file" name="files" multiple></div></div>`,async fd=>{const clientId=opsFd(fd,'clientId'),amount=bh4N(opsFd(fd,'amount')),receipt=opsFd(fd,'receipt'),paymentType=opsFd(fd,'paymentType')||'debt';if((OPS.collections||[]).some(x=>x.clientId===clientId&&receipt&&String(x.receipt||'').trim()===receipt&&bh4N(x.amount)===amount))throw new Error('رقم السند مسجل سابقًا لنفس العميل ونفس المبلغ');const files=await opsSaveFiles(fd.getAll('files')),invoiceId=paymentType==='advance'?'':opsFd(fd,'invoiceId'),c={id:opsUid('col'),no:opsNextNo('COL'),clientId,date:opsFd(fd,'date'),amount,method:opsFd(fd,'method'),receipt,employeeId:opsFd(fd,'employeeId'),invoiceId,paymentType,isAdvance:paymentType==='advance',allocationMode:paymentType==='advance'?'advance_fifo':invoiceId?'invoice_then_fifo':'fifo',notes:opsFd(fd,'notes'),attachments:files,createdAt:new Date().toISOString()};OPS.collections.unshift(c);opsAddMovementRecord('COLLECTION',{date:c.date+'T12:00',employeeId:c.employeeId,entityKind:'client',entityId:c.clientId,amount:c.amount,reference:c.receipt||c.no,notes:`${c.paymentType==='advance'?'دفعة مقدمة':'سداد مديونية'} — ${c.method}${c.invoiceId?' — فاتورة '+(opsById(OPS.deliveries,c.invoiceId)?.clientOrder||''):''}${c.notes?' — '+c.notes:''}`,module:'collection',moduleId:c.id,attachments:files});await opsPersist((c.paymentType==='advance'?'دفعة مقدمة ':'سداد عميل ')+c.no);const nl=window.bhClientLedger(clientId);opsToast(nl.creditBalance>0?`تم التسجيل — رصيد العميل دائن ${opsMoney(nl.creditBalance)} ر.س`:'تم تسجيل السداد وتحديث رصيد العميل');},'اعتماد الحركة');
07206: };
07207: 
07208: window.rCli=function(){const s=$('fSeg').value,r=$('fRep').value,t=$('tCli');let L=D.cli;if(s)L=L.filter(c=>c.seg===s);if(r)L=L.filter(c=>c.rep===r);if(!L.length){t.innerHTML=`<tr><td colspan="9"><div class="empty"><span class="big">🧾</span>لا يوجد عملاء</div></td></tr>`;return;}t.innerHTML=L.map(c=>{const rp=D.emp.find(e=>e.id===c.rep),a=window.bhClientLedger(c.id),due=a.nextDueDate?fmtG(a.nextDueDate):'—',balance=a.creditBalance>0?`<span class="client-credit">دائن ${money(a.creditBalance)}</span>`:a.remaining>0?`<span class="client-debit">مدين ${money(a.remaining)}</span>`:'<span class="client-credit">مسدد</span>';return `<tr><td><b>${esc(c.name)}</b><div style="font-size:11px;color:var(--muted)">${esc(c.code?'رقم العميل: '+c.code:(c.cr||''))}</div></td><td><span class="chip ${c.seg==='بلوك'?'blk':'con'}">${esc(c.seg)}</span></td><td>${typeof bhClientQtyText==='function'?bhClientQtyText(c):'—'}</td><td class="mono">${money(a.sales)}</td><td class="mono">${money(a.paid)}</td><td class="mono">${balance}</td><td>${a.remaining?due:'—'}${a.maxDaysLate?`<div style="color:var(--warn);font-size:11px">متأخر ${a.maxDaysLate} يوم</div>`:''}</td><td>${rp?esc(rp.name):'<span style="color:var(--muted)">غير مُسند</span>'}</td><td><button class="btn btn-o btn-sm" onclick="cliForm('${c.id}')">تعديل</button></td></tr>`;}).join('');};
07209: 
07210: window.rStats=function(){const drv=D.emp.filter(e=>e.role.includes('سائق')||e.role.includes('مشغّل')).length,rep=D.emp.filter(e=>e.role.includes('مندوب')||e.role.includes('مسؤول مبيعات')).length,deb=bh4AllDebits(),cred=bh4AllCredits(),over=(D.cli||[]).reduce((s,c)=>s+bh4N(window.bhClientLedger(c.id).overdue),0);$('stats').innerHTML=`<div class="stat"><b>${D.veh.length}</b><span>مركبة ومعدة</span></div><div class="stat"><b>${D.emp.length}</b><span>موظف مسجّل</span></div><div class="stat"><b>${drv}</b><span>سائق / مشغّل</span></div><div class="stat"><b>${rep}</b><span>مسؤول مبيعات</span></div><div class="stat"><b>${D.cli.length}</b><span>عميل</span></div><div class="stat"><b>${money(deb)}</b><span>أرصدة العملاء المدينة</span></div><div class="stat"><b>${money(cred)}</b><span>دفعات مقدمة / أرصدة دائنة</span></div><div class="stat"><b>${money(over)}</b><span>متأخر عن السداد</span></div>`;$('cVeh').textContent=D.veh.length;$('cEmp').textContent=D.emp.length;$('cCli').textContent=D.cli.length;};
07211: 
07212: window.opsRenderDeliveries=function(){const out=OPS.deliveries.filter(x=>x.status==='out'),sales=OPS.deliveries.filter(x=>x.status!=='returned').reduce((s,x)=>s+bh4N(x.amount),0),paid=(OPS.collections||[]).reduce((s,x)=>s+bh4N(x.amount),0),balance=bh4AllDebits(),credit=bh4AllCredits(),overdue=(D.cli||[]).reduce((s,c)=>s+bh4N(window.bhClientLedger(c.id).overdue),0),clients=(D.cli||[]).map(c=>({c,l:window.bhClientLedger(c.id)})).sort((a,b)=>(b.l.remaining-b.l.creditBalance)-(a.l.remaining-a.l.creditBalance));opsEl('opsContent').innerHTML=opsHead('التوريد والعملاء','المبيعات تُثبت مديونية؛ السداد والدفعة المقدمة حركات مستقلة',`<button class="ops-btn primary" onclick="opsDeliveryForm()">بيع / توريد</button><button class="ops-btn gold" onclick="opsCollectionForm()">سداد / دفعة مقدمة</button><button class="ops-btn ghost" onclick="go('cli')">سجل العملاء</button>`)+`<div class="ops-kpis">${opsKpi(OPS.deliveries.length,'فواتير وتوريدات','blue')}${opsKpi(opsMoney(sales),'إجمالي المبيعات','purple')}${opsKpi(opsMoney(paid),'إجمالي المقبوضات')}${opsKpi(opsMoney(balance),'أرصدة مدينة',balance?'warn':'')}${opsKpi(opsMoney(credit),'أرصدة دائنة / مقدمات','blue')}${opsKpi(opsMoney(overdue),'المتأخر',overdue?'danger':'')}</div><div class="ops-card"><h3>حركة المبيعات <span class="n">${OPS.deliveries.length}</span></h3>${OPS.deliveries.length?`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>الفاتورة</th><th>التاريخ</th><th>رقم العميل</th><th>العميل</th><th>الصنف</th><th>الكمية</th><th>قيمة البيع</th><th>المخصص من السداد</th><th>المتبقي</th></tr></thead><tbody>${OPS.deliveries.slice().sort((a,b)=>String(b.date).localeCompare(String(a.date))).map(d=>{const inv=window.bhClientLedger(d.clientId).invoices.find(x=>x.id===d.id),c=opsCli(d.clientId);return `<tr><td class="mono">${opsEsc(d.clientOrder||d.no)}</td><td>${opsFmtDate(d.date)}</td><td class="mono">${opsEsc(c?.code||d.customerCode||'—')}</td><td><b>${opsEsc(c?.name||'—')}</b></td><td>${opsEsc(d.product)}</td><td class="num">${opsNum(d.quantity)} ${opsEsc(d.unit)}</td><td class="num">${opsMoney(d.amount)}</td><td class="num">${opsMoney(inv?.allocatedCollection||0)}</td><td class="num">${opsMoney(inv?.remaining??d.amount)}</td></tr>`;}).join('')}</tbody></table></div>`:opsEmpty('لا توجد مبيعات','')}</div><div class="ops-grid" style="margin-top:12px"><div class="ops-card ops-col-7"><h3>سندات السداد والدفعات المقدمة <span class="n">${OPS.collections.length}</span></h3>${OPS.collections.length?`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>السند</th><th>التاريخ</th><th>العميل</th><th>النوع</th><th>الفاتورة</th><th>المبلغ</th><th>الطريقة</th></tr></thead><tbody>${OPS.collections.slice(0,200).map(c=>`<tr><td class="mono">${opsEsc(c.receipt||c.no)}</td><td>${opsFmtDate(c.date)}</td><td>${opsEsc(opsCli(c.clientId)?.name||'—')}</td><td>${c.isAdvance||c.paymentType==='advance'?'<span class="ops-status info">دفعة مقدمة</span>':'سداد مديونية'}</td><td>${opsEsc(c.invoiceId?(opsById(OPS.deliveries,c.invoiceId)?.clientOrder||'محدد'):'توزيع تلقائي')}</td><td class="num">${opsMoney(c.amount)}</td><td>${opsEsc(c.method)}</td></tr>`).join('')}</tbody></table></div>`:opsEmpty('لا توجد حركات سداد','')}</div><div class="ops-card ops-col-5"><h3>أرصدة العملاء التراكمية</h3><div class="ops-alerts">${clients.length?clients.map(({c,l})=>`<div class="ops-alert ${l.overdue?'bad':l.remaining?'info':''}"><span class="bar"></span><div><b>${opsEsc(bh3ClientLabel(c))}</b><span>مبيعات ${opsMoney(l.sales)} — مقبوضات ${opsMoney(l.paid)} — ${opsEsc(bh4CustomerBalanceText(l))}</span></div><time>${l.creditBalance>0?'دائن':l.maxDaysLate?l.maxDaysLate+' يوم':'قائم'}</time></div>`).join(''):opsEmpty('لا يوجد عملاء','')}</div></div></div>`;};
07213: 
===== LINES 7278-7284 =====
07278:   if(!OPS.meta.sequences.DTR)OPS.meta.sequences.DTR=1;
07279:   return true;
07280: }
07281: function bh5Drivers(){return (D.emp||[]).filter(e=>e.act!==false&&(/سائق|مشغّل/.test(e.role||'')));}
07282: function bh5AssignedVehicles(driverId){return (D.veh||[]).filter(v=>v.drv===driverId);}
07283: function bh5VehLabel(v){if(!v)return '—';return `${v.plate||v.acct||'—'} — ${v.type||''}${v.make?' — '+v.make:''}`;}
07284: function bh5DriverLabel(e){if(!e)return '—';return `${e.name}${e.no?' — '+e.no:''}`;}
===== LINES 7949-7967 =====
07949: 
07950: window.empForm=function(id){
07951:   bh10Ensure();const e=id?opsEmp(id):{act:true},roles=typeof ROLES!=='undefined'?ROLES:['سائق','مشغّل معدات','فني صيانة','أمين مستودع','محاسب','إداري'];
07952:   opsOpenModal(id?'تعديل ملف موظف':'إضافة موظف / شخص',`<div class="ops-form-grid"><div class="full"><label class="ops-required">الاسم الرباعي</label><input name="name" value="${opsEsc(e.name||'')}" required></div><div><label class="ops-required">رقم الهوية / الإقامة</label><input name="nid" value="${opsEsc(e.nid||'')}" required></div><div><label>الجنسية</label><input name="nat" value="${opsEsc(e.nat||'')}"></div><div><label class="ops-required">المسمى الوظيفي</label><select name="role">${roles.map(r=>`<option ${e.role===r?'selected':''}>${opsEsc(r)}</option>`).join('')}</select></div><div><label>الرقم الوظيفي</label><input name="no" value="${opsEsc(e.no||'')}"></div><div><label>الجوال</label><input name="tel" value="${opsEsc(e.tel||'')}"></div><div><label>تاريخ التعيين</label><input type="date" name="hire" value="${e.hire||''}"></div><div><label>الحالة</label><select name="act"><option value="1" ${e.act!==false?'selected':''}>على رأس العمل</option><option value="0" ${e.act===false?'selected':''}>موقوف</option></select></div><div><label>رخصة القيادة</label><input name="lic" value="${opsEsc(e.lic||'')}"></div><div><label>انتهاء الرخصة</label><input type="date" name="licE" value="${e.licE||''}"></div><div><label>مركز التكلفة</label><input name="costCenter" value="${opsEsc(e.costCenter||'التشغيل')}"></div><div><label>المعدة الافتراضية لتوزيع الراتب</label><select name="defaultVehicleId">${opsSelect((D.veh||[]).map(v=>({id:v.id,label:bh10VehicleLabel(v)})),bh10DefaultVehicleForEmployee(e),'— غير موزع —')}</select></div><div class="full bh10-section-note"><b>إعداد الراتب الافتراضي</b> يستخدم لتعبئة سجل الشهر الجديد فقط. كل شهر يُحفظ كسجل مستقل حتى تبقى تكلفة الفترات السابقة صحيحة.</div><div><label>الراتب الأساسي</label><input type="number" min="0" step="any" name="baseSalary" value="${e.baseSalary||''}"></div><div><label>بدل السكن</label><input type="number" min="0" step="any" name="housingAllowance" value="${e.housingAllowance||''}"></div><div><label>بدل النقل</label><input type="number" min="0" step="any" name="transportAllowance" value="${e.transportAllowance||''}"></div><div><label>بدلات أخرى</label><input type="number" min="0" step="any" name="otherAllowance" value="${e.otherAllowance||''}"></div><div><label>تكاليف صاحب العمل الشهرية</label><input type="number" min="0" step="any" name="employerCosts" value="${e.employerCosts||''}"></div><div><label>نسبة التوزيع الافتراضية %</label><input type="number" min="0" max="100" step="any" name="defaultAllocationPct" value="${e.defaultAllocationPct??(bh10DefaultVehicleForEmployee(e)?100:0)}"></div></div>`,async fd=>{const existing=id?opsEmp(id):null,o={...(existing||{}),id:id||opsUid('emp'),name:opsFd(fd,'name'),nid:opsFd(fd,'nid'),nat:opsFd(fd,'nat'),role:opsFd(fd,'role'),no:opsFd(fd,'no'),tel:opsFd(fd,'tel'),hire:opsFd(fd,'hire'),lic:opsFd(fd,'lic'),licE:opsFd(fd,'licE'),cash:existing?.cash||'',act:opsFd(fd,'act')==='1',costCenter:opsFd(fd,'costCenter'),defaultVehicleId:opsFd(fd,'defaultVehicleId'),baseSalary:bh10N(opsFd(fd,'baseSalary')),housingAllowance:bh10N(opsFd(fd,'housingAllowance')),transportAllowance:bh10N(opsFd(fd,'transportAllowance')),otherAllowance:bh10N(opsFd(fd,'otherAllowance')),employerCosts:bh10N(opsFd(fd,'employerCosts')),defaultAllocationPct:bh10N(opsFd(fd,'defaultAllocationPct')),updatedAt:new Date().toISOString()};if(id)D.emp[D.emp.findIndex(x=>x.id===id)]=o;else D.emp.push(o);save();rAll();await opsPersist((id?'تعديل موظف ':'إضافة موظف ')+o.name);opsToast(id?'تم تحديث ملف الموظف':'تمت إضافة الموظف ويمكن تسجيل راتبه الآن');},id?'حفظ ملف الموظف':'إضافة الموظف');
07953: };
07954: 
07955: window.bh10FillSalaryDefaults=function(employeeId){const e=opsEmp(employeeId),f=document.getElementById('opsForm');if(!e||!f)return;['baseSalary','housingAllowance','transportAllowance','otherAllowance','employerCosts'].forEach(k=>{if(f.elements[k])f.elements[k].value=bh10N(e[k])||'';});if(f.elements.vehicleId)f.elements.vehicleId.value=bh10DefaultVehicleForEmployee(e);if(f.elements.allocationPct)f.elements.allocationPct.value=e.defaultAllocationPct??(bh10DefaultVehicleForEmployee(e)?100:0);};
07956: window.bh10SalaryForm=function(id,employeeId=''){
07957:   bh10Ensure();const x=id?OPS.salaryRecords.find(z=>z.id===id):{},employees=(D.emp||[]).filter(e=>e.act!==false||e.id===x.employeeId);if(!employees.length){opsToast('أضف اسم الموظف أولًا','err');empForm();return;}const selected=employeeId||x.employeeId||employees[0].id,e=opsEmp(selected)||{};
07958:   opsOpenModal(id?'تعديل سجل راتب':'تسجيل راتب وتوزيعه على معدة',`<div class="bh10-section-note"><b>قاعدة التكلفة:</b> تكلفة المعدة تشمل الأساسي والبدلات والإضافي وتكاليف صاحب العمل، بعد تطبيق نسبة التوزيع والفترة. استقطاعات الموظف تظهر في صافي الراتب ولا تخفض تكلفة صاحب العمل.</div><div class="ops-form-grid"><div class="full"><label class="ops-required">الموظف / السائق</label><select name="employeeId" required onchange="bh10FillSalaryDefaults(this.value)">${opsSelect(employees.map(z=>({id:z.id,label:`${z.name} — ${z.role||'موظف'}${z.no?' — '+z.no:''}`})),selected,'— اختر —')}</select></div><div><label class="ops-required">شهر الاستحقاق</label><input type="month" name="month" value="${x.month||bh10Month()}" required></div><div><label>الحالة</label><select name="status"><option value="draft" ${x.status==='draft'?'selected':''}>مسودة — لا تدخل التكلفة</option><option value="approved" ${!x.status||x.status==='approved'?'selected':''}>معتمد</option><option value="paid" ${x.status==='paid'?'selected':''}>مدفوع</option></select></div><div><label>المعدة المحمل عليها الراتب</label><select name="vehicleId">${opsSelect((D.veh||[]).map(v=>({id:v.id,label:bh10VehicleLabel(v)})),x.vehicleId||bh10DefaultVehicleForEmployee(e),'— إداري / غير موزع —')}</select></div><div><label>نسبة التوزيع على المعدة %</label><input type="number" min="0" max="100" step="any" name="allocationPct" value="${x.allocationPct??e.defaultAllocationPct??(bh10DefaultVehicleForEmployee(e)?100:0)}"></div><div><label class="ops-required">الراتب الأساسي</label><input type="number" min="0" step="any" name="baseSalary" value="${x.baseSalary??e.baseSalary??''}" required></div><div><label>بدل السكن</label><input type="number" min="0" step="any" name="housingAllowance" value="${x.housingAllowance??e.housingAllowance??''}"></div><div><label>بدل النقل</label><input type="number" min="0" step="any" name="transportAllowance" value="${x.transportAllowance??e.transportAllowance??''}"></div><div><label>بدلات أخرى</label><input type="number" min="0" step="any" name="otherAllowance" value="${x.otherAllowance??e.otherAllowance??''}"></div><div><label>إضافي / حوافز</label><input type="number" min="0" step="any" name="overtime" value="${x.overtime||''}"></div><div><label>تكاليف صاحب العمل</label><input type="number" min="0" step="any" name="employerCosts" value="${x.employerCosts??e.employerCosts??''}"></div><div><label>استقطاعات الموظف</label><input type="number" min="0" step="any" name="deductions" value="${x.deductions||''}"></div><div><label>تاريخ الدفع</label><input type="date" name="paidDate" value="${x.paidDate||''}"></div><div class="full"><label>ملاحظات / مرجع المسير</label><textarea name="notes" rows="2">${opsEsc(x.notes||'')}</textarea></div><div class="full"><label><input type="checkbox" name="updateDefaults" value="yes" ${id?'':'checked'} style="width:auto"> حفظ القيم كإعداد افتراضي للشهور التالية</label></div></div>`,async fd=>{const emp=opsEmp(opsFd(fd,'employeeId')),month=opsFd(fd,'month'),vehicleId=opsFd(fd,'vehicleId'),pct=vehicleId?bh10N(opsFd(fd,'allocationPct')):0;if(!emp)throw new Error('حدد الموظف');if(vehicleId&&pct<=0)throw new Error('حدد نسبة توزيع أكبر من صفر للمعدة');const otherPct=OPS.salaryRecords.filter(z=>z.id!==id&&z.employeeId===emp.id&&z.month===month&&z.status!=='cancelled').reduce((s,z)=>s+bh10N(z.allocationPct),0);if(otherPct+pct>100.0001)throw new Error(`إجمالي توزيع الموظف في هذا الشهر سيصبح ${opsNum(otherPct+pct)}%؛ الحد الأقصى 100%`);const o={...x,id:id||opsUid('pay'),no:x.no||opsNextNo('PAY'),employeeId:emp.id,month,status:opsFd(fd,'status'),vehicleId,allocationPct:pct,baseSalary:bh10N(opsFd(fd,'baseSalary')),housingAllowance:bh10N(opsFd(fd,'housingAllowance')),transportAllowance:bh10N(opsFd(fd,'transportAllowance')),otherAllowance:bh10N(opsFd(fd,'otherAllowance')),overtime:bh10N(opsFd(fd,'overtime')),employerCosts:bh10N(opsFd(fd,'employerCosts')),deductions:bh10N(opsFd(fd,'deductions')),paidDate:opsFd(fd,'paidDate'),notes:opsFd(fd,'notes'),updatedAt:new Date().toISOString(),createdAt:x.createdAt||new Date().toISOString()};if(id)OPS.salaryRecords[OPS.salaryRecords.findIndex(z=>z.id===id)]=o;else OPS.salaryRecords.unshift(o);if(fd.get('updateDefaults')==='yes'){Object.assign(emp,{baseSalary:o.baseSalary,housingAllowance:o.housingAllowance,transportAllowance:o.transportAllowance,otherAllowance:o.otherAllowance,employerCosts:o.employerCosts,defaultVehicleId:o.vehicleId,defaultAllocationPct:o.allocationPct});save();rAll();}await opsPersist((id?'تعديل':'إضافة')+' سجل راتب '+o.no);opsToast('تم حفظ الراتب وتوزيعه محاسبيًا');setTimeout(()=>bh10PayrollRegister(),100);},id?'حفظ التعديل':'اعتماد سجل الراتب');
07959: };
07960: 
07961: window.bh10CancelSalary=async function(id){const x=OPS.salaryRecords.find(z=>z.id===id);if(!x||!confirm(`إلغاء سجل الراتب ${x.no}؟ سيظل محفوظًا للمراجعة ولن يدخل في تكلفة المعدات.`))return;x.status='cancelled';x.cancelledAt=new Date().toISOString();await opsPersist('إلغاء سجل راتب '+x.no);bh10PayrollRegister();};
07962: window.bh10PayrollRegister=function(month=''){
07963:   bh10Ensure();month=month||bh10Month();const rows=OPS.salaryRecords.filter(x=>!month||x.month===month).sort((a,b)=>String(b.month).localeCompare(String(a.month))||String(b.createdAt).localeCompare(String(a.createdAt))),approved=rows.filter(x=>['approved','paid'].includes(x.status)),payrollByEmployeeMonth=new Map();approved.forEach(x=>{const k=x.employeeId+'|'+x.month,old=payrollByEmployeeMonth.get(k);if(!old||bh10SalaryEmployerCost(x)>bh10SalaryEmployerCost(old))payrollByEmployeeMonth.set(k,x);});const payrollRows=[...payrollByEmployeeMonth.values()],employer=payrollRows.reduce((s,x)=>s+bh10SalaryEmployerCost(x),0),net=payrollRows.reduce((s,x)=>s+bh10SalaryNet(x),0),allocated=approved.reduce((s,x)=>s+bh10SalaryEmployerCost(x)*bh10N(x.allocationPct)/100,0);
07964:   opsOpenModal('الرواتب والأسماء وتوزيع تكلفة السائقين',`<div class="ops-actions" style="margin-bottom:10px"><button type="button" class="ops-btn primary" onclick="opsCloseModal();setTimeout(()=>empForm(),60)">إضافة اسم / موظف</button><button type="button" class="ops-btn gold" onclick="opsCloseModal();setTimeout(()=>bh10SalaryForm(),60)">تسجيل راتب شهر</button><button type="button" class="ops-btn ghost" onclick="opsCloseModal();setTimeout(()=>bh10EquipmentCostReportForm(),60)">تكلفة المعدات</button></div><div class="ops-form-grid"><div><label>عرض شهر</label><input type="month" value="${month}" onchange="bh10PayrollRegister(this.value)"></div></div><div class="ops-kpis" style="margin:12px 0">${opsKpi((D.emp||[]).length,'أسماء مسجلة')}${opsKpi(approved.length,'سجلات معتمدة','blue')}${opsKpi(opsMoney(employer),'تكلفة صاحب العمل','purple')}${opsKpi(opsMoney(net),'صافي الرواتب')}${opsKpi(opsMoney(allocated),'موزع على المعدات','warn')}${opsKpi(opsMoney(employer-allocated),'غير موزع')}</div>${rows.length?`<div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>المسير</th><th>الموظف</th><th>الشهر</th><th>الأساسي</th><th>البدلات والإضافي</th><th>تكلفة صاحب العمل</th><th>الصافي</th><th>المعدة / النسبة</th><th>الحالة</th><th></th></tr></thead><tbody>${rows.map(x=>{const e=opsEmp(x.employeeId),extra=bh10SalaryGross(x)-bh10N(x.baseSalary);return `<tr><td class="mono">${opsEsc(x.no)}</td><td><b>${opsEsc(e?.name||'موظف محذوف')}</b><div class="muted">${opsEsc(e?.role||'')} — ${opsEsc(e?.no||'')}</div></td><td class="mono">${opsEsc(x.month)}</td><td class="num">${opsMoney(x.baseSalary)}</td><td class="num">${opsMoney(extra)}</td><td class="num"><b>${opsMoney(bh10SalaryEmployerCost(x))}</b></td><td class="num">${opsMoney(bh10SalaryNet(x))}</td><td>${opsEsc(bh10VehicleLabel(opsVeh(x.vehicleId)))}<div class="muted">${opsNum(x.allocationPct)}%</div></td><td><span class="bh10-salary-status ${opsEsc(x.status)}">${opsEsc(bh10SalaryStatus(x.status))}</span></td><td><button type="button" class="ops-btn ghost sm" onclick="opsCloseModal();setTimeout(()=>bh10SalaryForm('${x.id}'),60)">تعديل</button>${x.status!=='cancelled'?` <button type="button" class="ops-btn ghost sm" onclick="bh10CancelSalary('${x.id}')">إلغاء</button>`:''}</td></tr>`;}).join('')}</tbody></table></div>`:opsEmpty('لا توجد رواتب في الشهر المختار','أضف الأشخاص ثم سجل الراتب الشهري ووزعه على المعدة')}`,()=>false,'إغلاق');opsEl('opsModalSave').onclick=opsCloseModal;
07965: };
07966: 
07967: const BH10_COST_CATEGORIES=['تأمين','استمارة وفحص دوري','إطارات','زيوت وسوائل','غسيل ونظافة','سحب ونقل','مخالفات','إيجار أو تمويل','رسوم حكومية','استهلاك محاسبي','مصروف تشغيل آخر'];
===== LINES 8004-8010 =====
08004: 
08005: <style id="binhamid-professional-reports-workshop-v11-styles">
08006: .bh11-driver-preview{border:1px solid #d8e3e8;border-radius:10px;background:#f5f9fb;padding:10px;min-height:64px}.bh11-driver-preview b{display:block;color:#123f59;margin-bottom:3px}.bh11-driver-preview span{color:#617681;font-size:11px}.bh11-workshop-note{border-right:4px solid #b4893a;background:#fff9ed;color:#664e23;border-radius:9px;padding:10px 12px;line-height:1.75;margin-bottom:11px}.bh11-workshop-note b{color:#173f56}
08007: .ops-print-report.bh11-report{direction:rtl;font-family:'IBM Plex Sans Arabic',Arial,sans-serif;color:#172027;background:#fff;padding:0!important;min-height:277mm;position:relative;overflow:visible}.bh11-report-shell{display:grid;grid-template-columns:12mm minmax(0,1fr);min-height:277mm;border:1px solid #c8d1d6}.bh11-report-rail{background:linear-gradient(180deg,#0b2233,#153c58 50%,#0b2233);color:#e8d4aa;display:flex;flex-direction:column;align-items:center;padding:6mm 0 4mm;position:relative}.bh11-report-rail:after{content:'';position:absolute;inset:0 0 0 auto;width:1.2px;background:#cda95f}.bh11-report-rail .rail-mark{font-family:'Reem Kufi',sans-serif;font-size:7pt;writing-mode:vertical-rl;letter-spacing:1.4pt;margin-top:auto;color:#89a9bd}.bh11-report-rail .rail-ref{font-family:monospace;font-size:6pt;writing-mode:vertical-rl;direction:ltr;letter-spacing:.7pt;max-height:150mm;overflow:hidden}.bh11-report-main{padding:7mm 8mm 7mm 7mm;position:relative;min-width:0}.bh11-report-watermark{position:absolute;inset:45mm 20mm auto;display:flex;justify-content:center;pointer-events:none;opacity:.035;transform:rotate(-8deg)}.bh11-report-watermark img{width:105mm;max-height:125mm;object-fit:contain}.bh11-report-main>*:not(.bh11-report-watermark){position:relative;z-index:1}.bh11-report-mast{display:grid;grid-template-columns:34mm 1fr 32mm;gap:5mm;align-items:center;padding-bottom:3mm;border-bottom:2.2px solid #0b2233;position:relative}.bh11-report-mast:after{content:'';position:absolute;bottom:-2.2px;right:0;width:42%;height:2.2px;background:#d2aa5e}.bh11-report-logo img{display:block;max-width:34mm;max-height:19mm;object-fit:contain}.bh11-report-company b{display:block;font-family:'Reem Kufi',sans-serif;color:#0b2233;font-size:12pt;line-height:1.35}.bh11-report-company span{display:block;color:#65717a;font-size:6.8pt;line-height:1.55}.bh11-report-qr{text-align:center;border:1px solid #d3dce0;border-radius:2mm;padding:1.5mm;color:#71808a;font-size:6pt}.bh11-report-qr svg{width:22mm!important;height:22mm!important;display:block;margin:auto}.bh11-report-title{display:grid;grid-template-columns:1fr 47mm;border:1px solid #d8c69e;margin:3mm 0;border-radius:1mm;overflow:hidden}.bh11-report-title .title-copy{background:linear-gradient(180deg,#fffaf1,#f5ead7);padding:2.4mm 4mm}.bh11-report-title .eyebrow{color:#966f2d;font-size:6pt;letter-spacing:1.5pt}.bh11-report-title h1{font-family:'Reem Kufi',sans-serif!important;color:#0b2233!important;font-size:15pt!important;line-height:1.25!important;margin:.6mm 0 0!important}.bh11-report-title .title-meta{background:#0b2233;color:#fff;padding:2.2mm 3mm;display:grid;align-content:center;gap:1mm}.bh11-report-title .title-meta div{display:flex;justify-content:space-between;gap:2mm;font-size:6.5pt}.bh11-report-title .title-meta span{color:#82a2b7}.bh11-report-title .title-meta b{color:#f0dcb4;font-family:monospace;direction:ltr}.bh11-field-grid{display:grid;grid-template-columns:1fr 1fr;gap:1.3mm 2mm;margin-bottom:3mm}.bh11-field{border:1px solid #dbe2e5;background:rgba(255,255,255,.9);padding:2mm 2.5mm;min-height:12mm}.bh11-field.full{grid-column:1/-1}.bh11-field .field-label{color:#6e7b82;font-size:6.6pt;font-weight:700;margin-bottom:.7mm}.bh11-field .field-value{color:#162b38;font-size:8.5pt;line-height:1.6;white-space:pre-wrap}.bh11-field.declaration{border:1.5px solid #b99550;background:#fffaf0}.bh11-report-section{margin:3mm 0}.bh11-report-section h2{font-family:'Reem Kufi',sans-serif;font-size:10.5pt;color:#0b2233;margin:0 0 1.5mm;border-right:3px solid #b4893a;padding-right:2mm}.bh11-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:1.5mm;margin-bottom:2.5mm}.bh11-kpi{border:1px solid #d9e1e4;background:rgba(249,251,252,.95);padding:2mm;text-align:center;min-height:15mm}.bh11-kpi b{display:block;color:#0b2233;font-size:10pt}.bh11-kpi span{display:block;color:#697a83;font-size:6.3pt;margin-top:.6mm}.bh11-kpi.warn{border-color:#dfbf79;background:#fff9ed}.bh11-kpi.bad{border-color:#d7a1a1;background:#fff5f5}.bh11-report table{width:100%;border-collapse:collapse;margin:1.5mm 0 3mm!important}.bh11-report th{background:#123b55!important;color:#fff!important;border:1px solid #0b2a3d!important;padding:1.5mm!important;font-size:6.7pt!important;line-height:1.35}.bh11-report td{border:1px solid #d5dde1!important;padding:1.4mm 1.6mm!important;font-size:6.8pt!important;line-height:1.45;vertical-align:top}.bh11-report tbody tr:nth-child(even){background:#f8fafb}.bh11-report .num{text-align:left;direction:ltr;font-family:monospace}.bh11-report-before{margin-top:1mm}.bh11-report-signatures{display:grid;grid-template-columns:repeat(4,1fr);gap:2mm;margin-top:6mm;break-inside:avoid;page-break-inside:avoid}.bh11-signature{border:1px solid #aebac0;min-height:29mm;padding:2.5mm;position:relative}.bh11-signature.stamp{border:1.5px dashed #b4893a;background:#fffdf8;text-align:center}.bh11-signature .sig-role{font-family:'Reem Kufi',sans-serif;color:#0b2233;font-size:7pt}.bh11-signature .sig-name{font-size:7pt;color:#4f5f67;margin-top:1mm}.bh11-signature .sig-line{border-bottom:1px solid #697982;height:11mm}.bh11-signature small{font-size:5.8pt;color:#7b878d}.bh11-report-footer{border-top:1px solid #cbd4d8;margin-top:4mm;padding-top:2mm;display:flex;justify-content:space-between;gap:4mm;color:#758188;font-size:5.8pt}.bh11-empty{color:#667780;border:1px dashed #c8d2d7;padding:3mm;text-align:center}.bh11-status{display:inline-block;border-radius:10px;padding:.4mm 1.5mm;background:#edf3f6;color:#385b6e;font-weight:700;font-size:6pt}.bh11-status.good{background:#e8f4ed;color:#1f6a49}.bh11-status.warn{background:#fff3d9;color:#876000}.bh11-status.bad{background:#fae8e8;color:#a12f2f}
08008: .ops-print-report.bh11-report.ops-landscape .bh11-report-shell{grid-template-columns:10mm minmax(0,1fr)}.ops-print-report.bh11-report.ops-landscape .bh11-report-main{padding:5mm 6mm}.ops-print-report.bh11-report.ops-landscape .bh11-kpis{grid-template-columns:repeat(6,1fr)}.ops-print-report.bh11-report.ops-landscape td{font-size:6.2pt!important;padding:1.1mm!important}.ops-print-report.bh11-report.ops-landscape th{font-size:6.1pt!important;padding:1.2mm!important}
08009: @media print{.bh11-report-main{overflow:visible}.bh11-report thead{display:table-header-group}.bh11-report tr{break-inside:avoid;page-break-inside:avoid}.bh11-report-section{break-inside:auto}.bh11-report h2{break-after:avoid;page-break-after:avoid}.bh11-report-signatures{break-inside:avoid!important}.bh11-report-rail{print-color-adjust:exact;-webkit-print-color-adjust:exact}.bh11-report th,.bh11-report-title .title-meta{print-color-adjust:exact;-webkit-print-color-adjust:exact}}
08010: @media(max-width:760px){.bh11-kpis{grid-template-columns:repeat(2,1fr)}.bh11-field-grid{grid-template-columns:1fr}.bh11-field.full{grid-column:auto}.bh11-report-signatures{grid-template-columns:1fr 1fr}}
===== LINES 8016-8022 =====
08016: function bh11N(v){const n=Number(v||0);return Number.isFinite(n)?n:0;}
08017: function bh11InRange(value,from,to){const d=String(value||'').slice(0,10);return !!d&&(!from||d>=from)&&(!to||d<=to);}
08018: function bh11EmpSnapshot(e){return e?{id:e.id,name:e.name||'',role:e.role||'',nid:e.nid||'',no:e.no||'',tel:e.tel||''}:null;}
08019: function bh11WorkshopStaff(){const active=(D.emp||[]).filter(e=>e.act!==false),preferred=active.filter(e=>/ميكاني|فني|ورش|صيانه|صيانة|مشرف/.test(opsNormArabic(e.role||'')));return preferred.length?preferred:active;}
08020: function bh11DriverForAssetKey(key){const p=opsAssetParts(key),v=p.kind==='vehicle'?opsVeh(p.id):null,e=v?opsEmp(v.drv):null;return {parts:p,vehicle:v,driver:e};}
08021: window.bh11MaintenanceAssetChanged=function(key){const x=bh11DriverForAssetKey(key),form=document.getElementById('opsForm'),box=document.getElementById('bh11DriverPreview');if(!form||!box)return;if(form.elements.driverId)form.elements.driverId.value=x.driver?.id||'';if(form.elements.requesterId&&x.driver?.id&&[...form.elements.requesterId.options].some(o=>o.value===x.driver.id))form.elements.requesterId.value=x.driver.id;box.innerHTML=x.vehicle?(x.driver?`<b>${opsEsc(x.driver.name)}</b><span>${opsEsc(x.driver.role||'سائق / مشغّل')} — ${opsEsc(x.driver.no||x.driver.nid||'بدون رقم وظيفي')}</span>`:`<b>لا يوجد سائق مسند للمركبة</b><span>اختر مقدم البلاغ يدويًا، ثم حدّث إسناد السائق من سجل المركبات.</span>`):'<b>أصل ثابت غير مرتبط بسائق</b><span>اختر مقدم البلاغ يدويًا.</span>';};
08022: 
===== LINES 8027-8035 =====
08027: 
08028: window.opsPrintMaintenanceRequest=function(id){const m=opsById(OPS.maintenance,id);if(!m)return;const v=m.entityKind==='vehicle'?opsVeh(m.entityId):null,driver=opsEmp(m.driverId)||m.driverSnapshot||opsEmp(v?.drv),requester=opsEmp(m.requesterId)||m.requesterSnapshot,technician=opsEmp(m.technicianId)||m.technicianSnapshot,approved=m.status!=='pending_approval',fields=[['رقم الطلب / أمر العمل',m.no],['المركبة / الأصل',opsEntityLabel(m.entityKind,m.entityId)],['رقم الأصل',v?.acct||'—'],['السائق / المشغّل',driver?.name||'غير مسند'],['رقم السائق الوظيفي / الهوية',driver?.no||driver?.nid||'—'],['تاريخ البلاغ',opsFmtDate(m.openDate)],['قراءة العداد / التشغيل',m.meter||'—'],['الأولوية',m.priority||'عادي'],['مقدم البلاغ',requester?.name||'—'],['الميكانيكي / مسؤول الورشة',technician?.name||'—'],['وصف العطل والأعراض',m.problem||'—'],['الإجراء المؤقت',m.containment||'—'],['التقدير الأولي الداخلي',`${opsMoney(m.estimatedCost||0)} ر.س`],['حالة الطلب',approved?`معتمد بتاريخ ${opsFmtDate(m.approvalDate)} بواسطة ${m.approvedBy||'الإدارة'}`:'بانتظار الفحص والاعتماد']];if(approved)fields.push(['طريقة التنفيذ المعتمدة',m.repairMode||'الورشة الداخلية'],['الحد المالي المعتمد',`${opsMoney(m.approvedBudget||0)} ر.س`],['نطاق العمل المعتمد',m.approvalNotes||'—']);fields.push(['ضابط المورد / الجهة الخارجية','لا يُحدد مورد أو ورشة خارجية في طلب الإصلاح. يتم الاختيار لاحقًا بموجب طلب عرض سعر وترسية أو أمر توريد مستقل.']);opsPrintReport(approved?'أمر عمل إصلاح معتمد':'طلب فحص وإصلاح داخلي',fields,[],`maintenance:${m.id}`,[{label:'السائق / مقدم البلاغ',name:driver?.name||requester?.name||''},{label:'الميكانيكي / مسؤول الورشة',name:technician?.name||''},{label:'اعتماد الإدارة',name:m.approvedBy||''},{label:'ختم المنشأة',type:'stamp'}],{documentLabel:approved?'أمر عمل ورشة':'طلب إصلاح داخلي'});};
08029: 
08030: function bh11DocKind(title,options){if(options?.documentLabel)return options.documentLabel;if(/إقرار/.test(title))return 'إقرار رسمي';if(/طلب عرض|توريد/.test(title))return 'مشتريات وتوريد';if(/إصلاح|صيانة/.test(title))return 'نموذج الورشة والصيانة';if(/تقرير|كشف/.test(title))return 'تقرير رقابي معتمد';return 'وثيقة تشغيلية';}
08031: function bh11FieldFull(label,value){return /وصف|نطاق|ملاحظ|ملاحظه|نص|إقرار|شروط|إجراء|السائقون|نتيجة|ضابط/.test(String(label))||String(value||'').length>85;}
08032: window.opsPrintReport=function(title,fields,rows,qrPayload,signatures,options={}){opsCloseModal();const refField=(fields||[]).find(x=>/رقم (التقرير|الطلب|الإقرار|السجل|الأمر)|رقم الطلب/.test(String(x[0])))?.[1],ref=String(refField||`BH-RPT-${opsToday().replace(/-/g,'')}`),qr=qrPayload&&typeof qrSVG==='function'?qrSVG(`${D.cfg.name}|${qrPayload}`,110):'',ids=[D.cfg.uni?`الرقم الموحد: ${D.cfg.uni}`:'',D.cfg.cr?`السجل التجاري: ${D.cfg.cr}`:'',D.cfg.vat?`الرقم الضريبي: ${D.cfg.vat}`:'',D.cfg.tel?`هاتف: ${D.cfg.tel}`:''].filter(Boolean).join(' — '),kind=bh11DocKind(title,options),fieldHtml=(fields||[]).map(x=>{const full=bh11FieldFull(x[0],x[1]),declaration=/نص الإقرار|ضابط/.test(String(x[0]));return `<div class="bh11-field ${full?'full':''} ${declaration?'declaration':''}"><div class="field-label">${opsEsc(x[0])}</div><div class="field-value">${opsEsc(x[1])}</div></div>`;}).join(''),table=rows?.length?`<div class="bh11-report-section"><h2>التفاصيل والسجلات</h2><table><thead><tr>${Object.keys(rows[0]).map(k=>`<th>${opsEsc(k)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${Object.values(r).map(v=>`<td>${opsEsc(v)}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`:'',sigs=signatures?.length?signatures:[{label:'إعداد'},{label:'مراجعة'},{label:'اعتماد الإدارة'},{label:'ختم المنشأة',type:'stamp'}],sigHtml=`<div class="bh11-report-signatures">${sigs.map(s=>`<div class="bh11-signature ${s.type==='stamp'?'stamp':''}"><div class="sig-role">${opsEsc(s.label||'التوقيع')}</div><div class="sig-name">${opsEsc(s.name||'')}</div>${s.type==='stamp'?'<div style="height:15mm;display:grid;place-items:center;color:#9b793c">مساحة الختم الرسمي</div>':'<div class="sig-line"></div><small>التوقيع والتاريخ</small>'}</div>`).join('')}</div>`,logo=D.cfg.logo?`<img src="${D.cfg.logo}" alt="">`:'',hijriText=typeof hijri==='function'?hijri():'—';opsEl('sheet').innerHTML=`<div class="ops-print-report bh11-report ${options.landscape?'ops-landscape':''}"><div class="bh11-report-shell"><aside class="bh11-report-rail"><div class="rail-ref">${opsEsc(ref)}</div><div class="rail-mark">${opsEsc(kind)}</div></aside><main class="bh11-report-main"><div class="bh11-report-watermark">${logo}</div><header class="bh11-report-mast"><div class="bh11-report-logo">${logo}</div><div class="bh11-report-company"><b>${opsEsc(D.cfg.name||OPS.settings.companyName)}</b><span>${opsEsc(ids||D.cfg.addr||'وثائق وتقارير التشغيل والرقابة')}</span></div><div class="bh11-report-qr">${qr}<span>رمز تحقق الوثيقة</span></div></header><div class="bh11-report-title"><div class="title-copy"><div class="eyebrow">${opsEsc(kind)}</div><h1>${opsEsc(title)}</h1></div><div class="title-meta"><div><span>المرجع</span><b>${opsEsc(ref)}</b></div><div><span>ميلادي</span><b>${opsFmtDate(opsToday())}</b></div><div><span>هجري</span><b>${opsEsc(hijriText)}</b></div></div></div>${fieldHtml?`<div class="bh11-field-grid">${fieldHtml}</div>`:''}<div class="bh11-report-before">${options.beforeTable||''}</div>${table}${sigHtml}<footer class="bh11-report-footer"><span>وثيقة صادرة من نظام الرقابة والتشغيل — تحفظ مع مرفقاتها ومستنداتها المؤيدة.</span><span>${opsEsc(ref)} · ${opsFmtDate(opsToday())}</span></footer></main></div></div>`;setTimeout(()=>window.print(),350);};
08033: 
08034: function bh11SpareItem(x){const n=opsNormArabic(x?.name||'');return x?.category==='قطع غيار'||/^1003/.test(String(x?.code||''))||/فلتر|زيت|قطع غيار|سير|رولمان|بطاري|بوجيه|كلتش|فرامل|تيل|طرمبه|مساعد|رديتر|كاوتش|اطار/.test(n);}
08035: function bh11RfqStatus(x){return ({requested:'طلب توريد مفتوح',quotes:'عروض تحت المقارنة',awarded:'أمر توريد مترسٍ',partial:'استلام جزئي',received:'مستلم بالكامل',cancelled:'ملغي'})[x?.status]||x?.status||'—';}
===== LINES 8110-8124 =====
08110: function bh12ParseDailyWorkbook(wb){const sales=[],collections=[],warnings=[];let detectedDate='';for(const sheetName of wb.SheetNames){const rows=XLSX.utils.sheet_to_json(wb.Sheets[sheetName],{header:1,defval:'',raw:false}),sheetDate=opsExcelDate(rows)||'';if(sheetDate&&!detectedDate)detectedDate=sheetDate;sales.push(...bh12ParseSales(rows,sheetName,sheetDate));collections.push(...bh12ParseCollections(rows,sheetName,sheetDate));}const movement=opsParseMovementWorkbook(wb,'daily.xlsx'),stock=movement.stock||[];warnings.push(...(movement.warnings||[]));if(!collections.length)warnings.push('لا يوجد قسم تحصيلات في هذا الملف؛ ستبقى كل المبيعات مديونية حتى استيراد تحصيلات أو تسجيل سند سداد.');return {sales,collections,stock,warnings,detectedDate:detectedDate||sales[0]?.date||stock[0]?.date||''};}
08111: window.bh12ParseDailyWorkbook=bh12ParseDailyWorkbook;window.bh12LookupClient=bh12LookupClient;window.bh12ResolveClient=bh12ResolveClient;window.bh12SaleKey=bh12SaleKey;window.bh12CollectionKey=bh12CollectionKey;
08112: 
08113: window.cliForm=function(id){bh12Ensure();const c=id?D.cli.find(x=>x.id===id):{seg:'خرسانة',days:D.cfg.days||3},reps=D.emp.filter(e=>e.role.includes('مندوب')||e.role.includes('مسؤول مبيعات')),linked=bh12ClientCodes(c).filter(x=>x!==bh12Code(c.code));openMo(id?'تعديل عميل':'إضافة عميل',`<div class="grid g2"><div><label>كود العميل *</label><input id="m_code" value="${esc(c.code||'')}" dir="ltr" placeholder="مثال: C-10025"></div><div><label>اسم العميل / المنشأة *</label><input id="m_name" value="${esc(c.name||'')}"></div><div><label>القطاع *</label><select id="m_seg">${SEGS.map(s=>`<option ${c.seg===s?'selected':''}>${s}</option>`).join('')}</select></div><div><label>السجل التجاري / الهوية</label><input id="m_cr" value="${esc(c.cr||'')}" dir="ltr"></div><div><label>مسؤول التواصل</label><input id="m_ct" value="${esc(c.ct||'')}"></div><div><label>رقم الجوال</label><input id="m_tel" value="${esc(c.tel||'')}" dir="ltr"></div><div><label>مهلة السداد من تاريخ البيع (يوم)</label><input type="number" min="0" id="m_days" value="${c.days??D.cfg.days??3}"></div><div style="grid-column:1/-1"><label>المندوب المسؤول</label><select id="m_rep"><option value="">— غير مُسند —</option>${reps.map(e=>`<option value="${e.id}" ${c.rep===e.id?'selected':''}>${esc(e.name)} — ${esc(e.role)}</option>`).join('')}</select></div>${linked.length?`<div style="grid-column:1/-1" class="bh12-rule"><b>أكواد مرتبطة محفوظة:</b> ${linked.map(x=>`<span class="bh12-code">${esc(x)}</span>`).join(' ')}</div>`:''}<div style="grid-column:1/-1"><label>العنوان / الموقع</label><input id="m_addr" value="${esc(c.addr||'')}"></div><div style="grid-column:1/-1"><label>ملاحظات</label><textarea id="m_note" rows="2">${esc(c.note||'')}</textarea></div></div>`,()=>{const code=bh12Code(V('code')),name=V('name');if(!code){toast('اكتب كود العميل؛ هو مفتاح الربط مع Excel','err');return false;}if(!name){toast('اكتب اسم العميل','err');return false;}const duplicate=(D.cli||[]).find(x=>x.id!==id&&bh12ClientCodes(x).includes(code));if(duplicate){toast(`الكود مرتبط بالفعل بالعميل: ${duplicate.name}`,'err');return false;}const old=id?D.cli.find(x=>x.id===id):{},oldCode=bh12Code(old?.code),o={...old,id:id||uid(),code,name,seg:V('seg'),cr:V('cr'),ct:V('ct'),tel:V('tel'),days:Number(V('days')||D.cfg.days||3),rep:V('rep'),addr:V('addr'),note:V('note'),aliases:Array.isArray(old.aliases)?old.aliases:[],sourceCustomerCodes:Array.isArray(old.sourceCustomerCodes)?old.sourceCustomerCodes:[]};if(oldCode&&oldCode!==code&&!o.sourceCustomerCodes.includes(oldCode))o.sourceCustomerCodes.push(oldCode);if(id)D.cli[D.cli.findIndex(x=>x.id===id)]=o;else D.cli.push(o);bh12Ensure();OPS.settings.customerCodeMap[code]=o.id;if(oldCode)OPS.settings.customerCodeMap[oldCode]=o.id;save();rAll();opsPersist((id?'تعديل':'إضافة')+' العميل '+code).catch(console.warn);toast(id?'تم التعديل وحفظ الربط':'تمت إضافة العميل وحفظ كود الربط');});};
08114: 
08115: window.opsImportDailySummary=async function(file){
08116:   if(!file)return;try{bh12Ensure();const wb=XLSX.read(new Uint8Array(await file.arrayBuffer()),{type:'array',cellDates:false}),plan=bh12ParseDailyWorkbook(wb),sales=plan.sales,collections=plan.collections,stock=plan.stock;if(!sales.length&&!collections.length&&!stock.length)throw new Error('لم يتم العثور على مبيعات أو تحصيلات أو حركة مخزون صالحة');const defaultDate=plan.detectedDate||opsToday(),spare=stock.filter(x=>bh12StockCategory(x)==='قطع غيار'),raw=stock.filter(x=>bh12StockCategory(x)==='مواد خام'),finished=stock.filter(x=>bh12StockCategory(x)==='منتج تام'),uniqueCustomers=new Map();[...sales,...collections].forEach(x=>{const key=x.customerCode||bh12NameKey(x.customer);if(key&&!uniqueCustomers.has(key))uniqueCustomers.set(key,{code:x.customerCode,name:x.customer});});const matched=[...uniqueCustomers.values()].filter(x=>bh12LookupClient(x.code,x.name)).length,newCount=uniqueCustomers.size-matched,codeRows=sales.filter(x=>x.customerCode).length,collectionValue=collections.reduce((s,x)=>s+bh12N(x.amount),0),salesValue=sales.reduce((s,x)=>s+bh12N(x.amount),0),salesPreview=sales.slice(0,40).map(x=>`<tr><td class="mono">${opsEsc(x.invoice||'—')}</td><td>${x.customerCode?`<span class="bh12-code">${opsEsc(x.customerCode)}</span>`:'—'}</td><td>${opsEsc(x.customer||'—')}</td><td>${opsEsc(x.item)}</td><td class="num">${opsMoney(x.amount)}</td><td><span class="ops-status warn">مديونية كاملة</span></td></tr>`).join(''),collectionPreview=collections.slice(0,40).map(x=>`<tr><td>${opsFmtDate(x.date||defaultDate)}</td><td>${x.customerCode?`<span class="bh12-code">${opsEsc(x.customerCode)}</span>`:'—'}</td><td>${opsEsc(x.customer||'—')}</td><td class="num">${opsMoney(x.amount)}</td><td>${opsEsc(x.method||'—')}</td><td class="mono">${opsEsc(x.receipt||x.invoice||'—')}</td></tr>`).join('');
08117:     opsOpenModal('اعتماد ملخص العمل اليومي — مبيعات وتحصيلات ومخزون',`<div class="bh12-rule"><b>القاعدة المحاسبية:</b> كل صف مبيعات يُسجل بكامل قيمته مديونية على العميل. لا تُعتبر أعمدة «نقدًا / تحويل / آجل» تحصيلًا؛ الذي يخفض الرصيد هو صف مستقل داخل قسم التحصيلات أو سند سداد مسجل في النظام.</div><div class="bh12-rule warn"><b>ربط العملاء:</b> كود العميل هو المفتاح الأساسي ويُحفظ دائمًا. العميل الجديد يُضاف مباشرة إلى سجل العملاء، والكود المعروف يرحّل الحركة إلى نفس الحساب حتى لو اختلف شكل الاسم.</div><div class="ops-form-grid"><div><label class="ops-required">تاريخ التقرير الافتراضي</label><input type="date" name="dailyDate" value="${defaultDate}" required></div></div><div class="ops-kpis" style="margin:12px 0">${opsKpi(sales.length,'صفوف مبيعات','blue')}${opsKpi(opsMoney(salesValue),'قيمة مديونية جديدة','purple')}${opsKpi(collections.length,'صفوف تحصيل')}${opsKpi(opsMoney(collectionValue),'قيمة التحصيلات')}${opsKpi(codeRows,'مبيعات بكود عميل','blue')}${opsKpi(matched,'عملاء مرتبطون')}${opsKpi(newCount,'عملاء سيضافون',newCount?'warn':'')}${opsKpi(spare.length,'حركات قطع غيار','purple')}${opsKpi(raw.length,'حركات خامات')}${opsKpi(finished.length,'حركات منتجات تامة')}</div>${sales.length?`<h3>المبيعات — كلها مديونية عند التسجيل</h3><div class="ops-table-wrap bh12-small-table"><table class="ops-table"><thead><tr><th>الفاتورة</th><th>كود العميل</th><th>العميل</th><th>الصنف</th><th>القيمة</th><th>القيد</th></tr></thead><tbody>${salesPreview}</tbody></table></div>`:''}${collections.length?`<h3 style="margin-top:12px">التحصيلات — تخفض حساب العميل</h3><div class="ops-table-wrap bh12-small-table"><table class="ops-table"><thead><tr><th>التاريخ</th><th>كود العميل</th><th>العميل</th><th>المبلغ</th><th>الطريقة</th><th>السند / الفاتورة</th></tr></thead><tbody>${collectionPreview}</tbody></table></div>`:'<div class="ops-note warn">هذا الملف لا يحتوي قسم تحصيلات. سيتم تسجيل المبيعات كمديونية فقط ولن ينخفض رصيد أي عميل.</div>'}${spare.length?`<h3 style="margin-top:12px">قطع الغيار الموجهة إلى مخزن الورشة</h3><div class="ops-table-wrap"><table class="ops-table"><thead><tr><th>الكود</th><th>القطعة</th><th>الحركة</th><th>الكمية</th></tr></thead><tbody>${spare.slice(0,50).map(x=>`<tr><td class="mono">${opsEsc(x.code)}</td><td>${opsEsc(x.item)}</td><td>${x.direction==='in'?'وارد':'صرف'}</td><td>${opsNum(x.quantity)} ${opsEsc(x.unit)}</td></tr>`).join('')}</tbody></table></div>`:''}${plan.warnings.length?`<div class="ops-note warn" style="margin-top:10px">${plan.warnings.map(opsEsc).join('<br>')}</div>`:''}`,async fd=>{
08118:       const reportDate=opsFd(fd,'dailyDate'),seen=new Set([...(OPS.imports||[]).flatMap(x=>x.rowKeys||[]),...(OPS.deliveries||[]).flatMap(x=>[x.sourceDailyKey,x.sourceRowKey]).filter(Boolean),...(OPS.collections||[]).map(x=>x.sourceDailyKey).filter(Boolean),...(OPS.stockMoves||[]).map(x=>x.sourceDailyKey).filter(Boolean)]),freshSales=[],freshCollections=[],freshStock=[];
08119:       sales.forEach(x=>{const key=bh12SaleKey(x,reportDate);if(!seen.has(key)){seen.add(key);freshSales.push({...x,key,date:x.date||reportDate});}});collections.forEach(x=>{const key=bh12CollectionKey(x,reportDate);if(!seen.has(key)){seen.add(key);freshCollections.push({...x,key,date:x.date||reportDate});}});stock.forEach(x=>{const category=bh12StockCategory(x),locationId=category==='قطع غيار'?'loc-workshop':'loc-store',key=bh12StockKey(x,reportDate,locationId);if(!seen.has(key)){seen.add(key);freshStock.push({...x,key,category,locationId,date:reportDate});}});if(!freshSales.length&&!freshCollections.length&&!freshStock.length)throw new Error('كل صفوف الملف مستوردة سابقًا');
08120:       const batch={id:opsUid('imp'),fileName:file.name,reportDate,importedAt:new Date().toISOString(),salesCount:freshSales.length,collectionCount:freshCollections.length,collectionValue:freshCollections.reduce((s,x)=>s+bh12N(x.amount),0),stockCount:freshStock.length,workshopCount:freshStock.filter(x=>x.category==='قطع غيار').length,salesValue:freshSales.reduce((s,x)=>s+bh12N(x.amount),0),newCustomerCount:0,linkedCustomerCodes:[],accountingRule:'ALL_SALES_DEBT_COLLECTIONS_ONLY_REDUCE_BALANCE',rowKeys:[]};
08121:       freshSales.forEach(x=>{const resolved=bh12ResolveClient(x.customerCode,x.customer,x.kind),client=resolved.client;if(resolved.created)batch.newCustomerCount++;if(x.customerCode)batch.linkedCustomerCodes.push(x.customerCode);const amount=bh12N(x.amount),isConcrete=x.kind==='خرسانة',employeeId=opsSalesResponsible(isConcrete?'concrete':'block')?.id||'',d={id:opsUid('dlv'),no:opsNextNo('DLV'),clientId:client.id,customerCode:x.customerCode||client.code||'',date:x.date||reportDate,product:x.item,spec:'',quantity:bh12N(x.quantity),unit:isConcrete?'م³':'قطعة',kind:x.kind,amount,cash:0,transfer:0,credit:amount,clientOrder:x.invoice,vehicleId:'',employeeId,destination:client.addr||'',notes:`مستورد من ملخص العمل اليومي — كامل القيمة مديونية حتى تسجيل تحصيل مستقل`,sourceDeclaredCash:bh12N(x.declaredCash),sourceDeclaredTransfer:bh12N(x.declaredTransfer),sourceDeclaredCredit:bh12N(x.declaredCredit),sourceDailyKey:x.key,sourceImportId:batch.id,accountingRule:'FULL_DEBT_UNTIL_COLLECTION',status:'delivered',createdAt:new Date().toISOString()};OPS.deliveries.unshift(d);opsAddMovementRecord('DELIVERY_DONE',{date:d.date+'T12:00',employeeId:d.employeeId,entityKind:'client',entityId:d.clientId,quantity:d.quantity,unit:d.unit,amount:d.amount,reference:d.clientOrder||d.no,notes:`${d.product} — مديونية كاملة؛ السداد من قسم التحصيلات فقط`,module:'daily-summary-sales',moduleId:d.id});batch.rowKeys.push(x.key);});
08122:       freshCollections.forEach(x=>{const resolved=bh12ResolveClient(x.customerCode,x.customer,''),client=resolved.client;if(resolved.created)batch.newCustomerCount++;if(x.customerCode)batch.linkedCustomerCodes.push(x.customerCode);const duplicate=(OPS.collections||[]).some(c=>c.clientId===client.id&&String(c.date).slice(0,10)===String(x.date).slice(0,10)&&bh12N(c.amount)===bh12N(x.amount)&&x.receipt&&String(c.receipt||'').trim()===String(x.receipt).trim());if(duplicate){batch.rowKeys.push(x.key);return;}const invoice=x.invoice?(OPS.deliveries||[]).find(d=>d.clientId===client.id&&String(d.clientOrder||d.no).trim()===String(x.invoice).trim()):null,c={id:opsUid('col'),no:opsNextNo('COL'),clientId:client.id,customerCode:x.customerCode||client.code||'',date:x.date||reportDate,amount:bh12N(x.amount),method:x.method||'غير محدد',receipt:x.receipt||'',employeeId:'',invoiceId:x.isAdvance?'':invoice?.id||'',paymentType:x.isAdvance?'advance':'debt',isAdvance:!!x.isAdvance,allocationMode:x.isAdvance?'advance_fifo':invoice?'invoice_then_fifo':'fifo',notes:`${x.notes||''}${x.invoice&&!invoice?' — فاتورة المصدر '+x.invoice+' لم تطابق؛ وُزع التحصيل على الأقدم':''}`.trim(),attachments:[],sourceDailyKey:x.key,sourceImportId:batch.id,createdAt:new Date().toISOString()};OPS.collections.unshift(c);opsAddMovementRecord('COLLECTION',{date:c.date+'T12:00',entityKind:'client',entityId:c.clientId,amount:c.amount,reference:c.receipt||c.no,notes:`تحصيل مستورد — ${c.method}${x.invoice?' — فاتورة '+x.invoice:''}`,module:'daily-summary-collections',moduleId:c.id});batch.rowKeys.push(x.key);});
08123:       const reconciles=new Map();freshStock.forEach(x=>{const item=opsFindOrCreateItem(x.item,x.category,{code:x.code,unit:x.unit});item.category=x.category;if(x.category==='قطع غيار'){item.workshopOnly=true;item.sourceDailyReport=true;}const existing=(OPS.stockMoves||[]).find(s=>s.itemId===item.id&&s.locationId===x.locationId&&s.direction===x.direction&&String(s.date).slice(0,10)===reportDate&&bh12N(s.quantity)===bh12N(x.quantity)&&!s.sourceDailyKey&&!s.dailyReconciledBy);if(existing){existing.sourceDailyKey=x.key;existing.dailyReconciledBy=batch.id;existing.notes=`${existing.notes||''} — مطابق لملخص العمل اليومي`.trim();batch.rowKeys.push(x.key);}else{const unitCost=x.value&&x.quantity?bh12N(x.value)/bh12N(x.quantity):bh12N(item.unitCost),sm={id:opsUid('stm'),no:opsNextNo('STK'),direction:x.direction,itemId:item.id,locationId:x.locationId,date:reportDate,quantity:bh12N(x.quantity),unitCost,totalCost:bh12N(x.value)||bh12N(x.quantity)*unitCost,reference:x.reference||'',party:x.party||'',employeeId:'',notes:`${x.category==='قطع غيار'?'مخزن الورشة':'المخزن العام'} — مستورد من ${file.name}`,status:'confirmed',sourceImportId:batch.id,sourceDailyKey:x.key,createdAt:new Date().toISOString()};OPS.stockMoves.unshift(sm);opsAddMovementRecord(x.direction==='in'?'STOCK_IN':'STOCK_OUT',{date:reportDate+'T12:00',sourceId:x.direction==='out'?x.locationId:'',destinationId:x.direction==='in'?x.locationId:'',entityKind:'item',entityId:item.id,quantity:sm.quantity,unit:item.unit,amount:sm.totalCost,reference:sm.reference||sm.no,notes:sm.notes,module:'daily-summary-stock',moduleId:sm.id});batch.rowKeys.push(x.key);}if(x.closing!==null)reconciles.set(item.id+'|'+x.locationId,{item,locationId:x.locationId,actual:bh12N(x.closing),opening:x.opening});});
08124:       reconciles.forEach(({item,locationId,actual,opening})=>{const book=opsStock(item.id,locationId),diff=actual-book,count={id:opsUid('cnt'),no:opsNextNo('CNT'),date:reportDate,locationId,itemId:item.id,book,actual,difference:diff,employeeId:'',notes:`مطابقة رصيد ملخص العمل اليومي${opening!==null?' — افتتاحي '+opening:''}`,sourceImportId:batch.id,createdAt:new Date().toISOString()};OPS.counts.unshift(count);if(Math.abs(diff)>0.000001){const sm={id:opsUid('stm'),no:opsNextNo('STK'),direction:diff>0?'in':'out',itemId:item.id,locationId,date:reportDate,quantity:Math.abs(diff),unitCost:bh12N(item.unitCost),totalCost:Math.abs(diff)*bh12N(item.unitCost),reference:count.no,party:'تسوية ملخص اليوم',employeeId:'',notes:count.notes,status:'confirmed',sourceImportId:batch.id,createdAt:new Date().toISOString()};OPS.stockMoves.unshift(sm);opsAddMovementRecord('STOCK_ADJUST',{date:reportDate+'T12:00',sourceId:diff<0?locationId:'',destinationId:diff>0?locationId:'',entityKind:'item',entityId:item.id,quantity:diff,unit:item.unit,amount:diff*bh12N(item.unitCost),reference:count.no,notes:count.notes,module:'daily-summary-stock',moduleId:sm.id});}});
===== LINES 8182-8198 =====
08182: function bh13Company(){return String(D?.cfg?.name||OPS?.settings?.companyName||'مصنع بن حامد');}
08183: function bh13Date(){return typeof opsToday==='function'?opsToday():new Date().toISOString().slice(0,10);}
08184: function bh13DateDisplay(){return typeof opsFmtDate==='function'?opsFmtDate(bh13Date()):bh13Date();}
08185: function bh13Kind(title,options={}){if(options.documentLabel)return options.documentLabel;const t=String(title||'');if(/إقرار|تعهد|استلام عهدة/.test(t))return 'إقرار رسمي معتمد';if(/عرض سعر|شراء|توريد|استلام مخزني/.test(t))return 'مشتريات وتوريد';if(/إصلاح|صيانة|ورشة|عطل/.test(t))return 'الورشة والصيانة';if(/راتب|رواتب|مسير/.test(t))return 'الرواتب وشؤون العاملين';if(/عميل|تحصيل|مديونية|كشف حساب/.test(t))return 'الحسابات والعملاء';if(/تقرير المدير|الإدارة العليا/.test(t))return 'تقرير الإدارة العليا';if(/تقرير|كشف|ملخص/.test(t))return 'تقرير رقابي معتمد';if(/سند|إذن|أمر/.test(t))return 'مستند تشغيلي معتمد';if(/بطاقة|QR|تعريف/.test(t))return 'بطاقة تعريف أصل';return 'وثيقة تشغيلية معتمدة';}
08186: function bh13FieldFull(label,value){return /وصف|نطاق|ملاحظ|ملاحظه|نص|إقرار|شروط|إجراء|السائقون|نتيجة|ضابط|بيان/.test(String(label))||String(value??'').length>85;}
08187: function bh13Logo(){return D?.cfg?.logo?`<img src="${D.cfg.logo}" alt="">`:'';}
08188: function bh13Ids(){return [D?.cfg?.uni?`الرقم الموحد: ${D.cfg.uni}`:'',D?.cfg?.cr?`السجل التجاري: ${D.cfg.cr}`:'',D?.cfg?.vat?`الرقم الضريبي: ${D.cfg.vat}`:'',D?.cfg?.tel?`هاتف: ${D.cfg.tel}`:''].filter(Boolean).join(' — ');}
08189: function bh13Reference(fields){const hit=(fields||[]).find(x=>/رقم (التقرير|الطلب|الإقرار|السجل|الأمر|السند|الكشف)|رقم الطلب|المرجع/.test(String(x?.[0]||'')))?.[1];return String(hit||`BH-DOC-${bh13Date().replace(/-/g,'')}`);}
08190: function bh13Signature(s){const type=s?.type||'',cls=type==='stamp'?'stamp':type==='thumbprint'?'thumbprint':'';let area='<div class="sig-line"></div><small>التوقيع والتاريخ</small>';if(type==='stamp')area='<div style="height:15mm;display:grid;place-items:center;color:#9b793c">مساحة الختم الرسمي</div>';if(type==='thumbprint')area='<div class="bh13-thumbprint">موضع<br>البصمة</div>';return `<div class="bh11-signature ${cls}"><div class="sig-role">${bh13Esc(s?.label||'التوقيع')}</div><div class="sig-name">${bh13Esc(s?.name||'')}</div>${area}</div>`;}
08191: 
08192: window.opsPrintReport=function(title,fields,rows,qrPayload,signatures,options={}){
08193:   if(typeof opsCloseModal==='function')opsCloseModal();
08194:   const ref=bh13Reference(fields),kind=bh13Kind(title,options),company=bh13Company(),logo=bh13Logo(),ids=bh13Ids(),hijriText=typeof hijri==='function'?hijri():'—',qr=qrPayload&&typeof qrSVG==='function'?qrSVG(`${company}|${qrPayload}`,110):'',keys=rows?.length?Object.keys(rows[0]):[];
08195:   const fieldHtml=(fields||[]).map(x=>{const full=bh13FieldFull(x[0],x[1]),declaration=/نص الإقرار|إقرار|ضابط|تعهد/.test(String(x[0]));return `<div class="bh11-field ${full?'full':''} ${declaration?'declaration':''}"><div class="field-label">${bh13Esc(x[0])}</div><div class="field-value">${bh13Esc(x[1])}</div></div>`;}).join('');
08196:   const table=rows?.length?`<section class="bh11-report-section"><h2>${bh13Esc(options.sectionTitle||'التفاصيل والسجلات')}</h2><table><thead><tr>${keys.map(k=>`<th>${bh13Esc(k)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${keys.map(k=>`<td>${bh13Esc(r[k])}</td>`).join('')}</tr>`).join('')}</tbody></table></section>`:'';
08197:   const sigs=signatures?.length?signatures:[{label:'إعداد'},{label:'مراجعة'},{label:'اعتماد الإدارة'},{label:'ختم المنشأة',type:'stamp'}],sigHtml=`<div class="bh11-report-signatures">${sigs.map(bh13Signature).join('')}</div>`;
08198:   const sheet=typeof opsEl==='function'?opsEl('sheet'):document.getElementById('sheet');if(!sheet)return;
===== LINES 8224-8237 =====
08224: })();
08225: </script>
08226: <style id="binhamid-first-run-setup-v14-styles">
08227: .bh14-setup-note{border-right:4px solid #176448;background:#edf8f3;color:#244e42;border-radius:10px;padding:10px 12px;margin-bottom:11px;font-size:11px;line-height:1.8}.bh14-setup-note b{color:#14533e}.bh14-setup-progress{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:10px 0 13px}.bh14-setup-progress div{border:1px solid #d9e3e6;border-radius:10px;background:#f8fafb;padding:9px;text-align:center}.bh14-setup-progress b{display:block;color:#0b2233;font-size:17px}.bh14-setup-progress span{display:block;color:#6b7c84;font-size:9.5px;margin-top:2px}.bh14-guide{display:grid;grid-template-columns:1fr 1fr;gap:9px}.bh14-step{border:1px solid #d8e1e5;border-radius:11px;padding:11px;background:#fff;position:relative;min-height:112px}.bh14-step .n{position:absolute;left:9px;top:9px;width:24px;height:24px;border-radius:50%;background:#0b2233;color:#f0dcb4;display:grid;place-items:center;font:bold 11px Arial}.bh14-step h4{font-family:'Reem Kufi',sans-serif;color:#153c58;font-size:13px;margin:0 0 4px;padding-left:28px}.bh14-step p{color:#65767e;font-size:10px;line-height:1.65;margin:0 0 8px}.bh14-safe-badge{display:inline-flex;align-items:center;gap:5px;border-radius:20px;background:#e7f5ee;color:#176448;padding:4px 8px;font-size:9.5px;font-weight:800}.bh14-danger-card{border:1px solid #e2b5b5!important;background:#fffafa!important}.bh14-danger-card h2{color:#8e2929!important}.bh14-danger-card .sub{color:#7b5555!important}@media(max-width:720px){.bh14-setup-progress{grid-template-columns:repeat(2,1fr)}.bh14-guide{grid-template-columns:1fr}}
08228: </style>
08229: <script id="binhamid-first-run-setup-v14-script">
08230: (function(){
08231: 'use strict';
08232: const BH14_VERSION='2026.07.15-control-v14-first-run-setup-not-reset';
08233: function bh14Val(fd,key){return typeof opsFd==='function'?opsFd(fd,key):String(fd.get(key)||'').trim();}
08234: function bh14Counts(){return {vehicles:(D.veh||[]).length,employees:(D.emp||[]).length,clients:(D.cli||[]).length,imports:(OPS.imports||[]).length+(OPS.fuelImports||[]).length};}
08235: function bh14Progress(){const c=bh14Counts();return `<div class="bh14-setup-progress"><div><b>${c.employees}</b><span>موظفون وسائقون</span></div><div><b>${c.vehicles}</b><span>معدات وأصول</span></div><div><b>${c.clients}</b><span>عملاء</span></div><div><b>${c.imports}</b><span>ملفات مستوردة</span></div></div>`;}
08236: 
08237: window.opsFirstRunSetup=function(){